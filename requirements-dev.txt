# Development and test dependencies.
# Runtime dependencies live in requirements.txt; install both to work on the code:
#     .venv\Scripts\pip install -r requirements.txt -r requirements-dev.txt
#
# The suite is written with unittest and runs under either runner:
#     python -m unittest discover -s tests
#     python -m pytest -q
# Section 15 of 2026-07-25_CONSOLE_DESIGN.md specifies pytest, so it is pinned
# here to keep the documented command working in a fresh checkout.
pytest>=8.0.0
