# The two roots. Both are REQUIRED and both must be an absolute path.
# config.py reads them with no default and refuses to import if either is
# missing, empty or relative, so a fresh checkout stops with a message instead of
# building a folder tree in the wrong place. Replace both placeholders below.
#
# INTELLIBILLS_PRACTICE_ROOT is the synced practice folder and holds what is
# safe to sync: documents, backups, exports and the two registries.
# INTELLIBILLS_UNSYNCED_ROOT must be outside any synced folder and holds what
# is not: the live database and the process logs. Do not quote either value
# and do not escape the backslashes.
INTELLIBILLS_PRACTICE_ROOT=C:\Users\<you>\OneDrive - <Your Practice Name>
INTELLIBILLS_UNSYNCED_ROOT=C:\<a folder outside OneDrive>\Intellibills

# Incoming mail, being the mailbox clients email their receipts to. IMAP_HOST,
# IMAP_USERNAME and IMAP_PASSWORD are REQUIRED and have no default; IMAP_PORT is
# the one of the four that defaults, to 993. Placeholders below, in the same
# style as the outgoing block: this file is a template and the live values
# belong in .env, which is gitignored.
IMAP_HOST=mail.<your-domain>
IMAP_PORT=993
IMAP_USERNAME=capture@<your-domain>
IMAP_PASSWORD=your-email-password

# Outgoing mail, for the alerts the pipeline sends to clients. All four are
# REQUIRED and none has a default. config.py reads them with no fallback and
# refuses to import if any is missing or empty, so a fresh checkout stops with a
# message naming the variable instead of quietly sending as somebody else.
#
# SMTP_HOST is the outgoing mail server.
# SMTP_PORT is its port; the code connects with SMTP_SSL, so 465 on most hosts.
# SMTP_USERNAME is the mailbox alerts are sent from. It is the From address the
# recipient sees, and it is printed inside the body of the reply an unrecognised
# sender gets, so it must be a mailbox that firm is willing to publish and to
# receive replies at.
# SMTP_PASSWORD is that mailbox's password.
SMTP_HOST=mail.<your-domain>
SMTP_PORT=465
SMTP_USERNAME=alerts@<your-domain>
SMTP_PASSWORD=your-mailbox-password

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Extraction provider. Optional; defaults to openai_vision, which is the only
# engine registered today. An unrecognised value stops the pipeline at startup.
EXTRACTION_ENGINE=openai_vision

# Poll interval in seconds (300 = 5 minutes)
POLL_INTERVAL_SECONDS=300

