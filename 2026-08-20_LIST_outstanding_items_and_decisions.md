# Outstanding items and decisions

## 88 open, 76 closed, 164 raised

**88 plus 76 must equal 164, the highest number ever used.** ~~89 plus 75 must equal 164.~~ **Corrected 2026-09-05 when item 152 closed, decided by Paul the same day.** ~~90 plus 74 must equal 164.~~ **Corrected 2026-09-05 when item 164 closed, on Paul dropping `email_delta` from the live database and the drop being read back by a second session.** ~~91 plus 73 must equal 164.~~ **Corrected 2026-09-05 when item 163 closed, built by Claude Code within the hour of the brief being written.** ~~90 plus 73 must equal 163.~~ **Corrected 2026-09-05 when item 164 was raised, on the live database still holding a dropped table.** ~~89 plus 73 must equal 162.~~ **Corrected 2026-09-05 when item 163 was raised, on the pipeline's own copy of the VAT rates.** ~~87 plus 73 must equal 160.~~ **Corrected 2026-09-04 when items 161 and 162 were raised, both found in `COA_MASTER_v2.xlsx` while renaming `vat_default` and both left as found.** ~~88 plus 72.~~ **Corrected 2026-09-04 when item 160 closed, both its points settled the hour it was raised.** ~~87 plus 72 must equal 159.~~ **Corrected 2026-09-04 when item 160 was raised, on the VAT rate table.** ~~88 plus 71.~~ **Corrected 2026-09-04 when item 159 closed on a suite run.** ~~87 plus 71 must equal 158.~~ **Corrected 2026-09-04 when item 159 was raised, by step 10h's reconciliation of `EMAIL_PROCESSING_MICROSTEPS.md`.** ~~88 plus 70.~~ **Corrected 2026-09-04 when item 158 closed on a run rather than on a compile.** ~~87 plus 70 must equal 157.~~ **Corrected 2026-09-04 when item 158 was raised, by step 10h's reconciliation of `CLAUDE.md`.** ~~88 plus 69.~~ **Corrected 2026-09-04 when item 157 closed. All four items raised from Claude Code's flags that hour were fixed the same hour, on Paul's instruction.** ~~89 plus 68.~~ **Corrected 2026-09-04 when item 155 closed, on Paul's instruction, minutes after 154 and 156.** ~~91 plus 66 must equal 157.~~ **Corrected 2026-09-04 when items 154 and 156 closed, both fixed on Paul's instruction within the hour they were raised.** ~~89 plus 66 must equal 155.~~ **Corrected 2026-09-04 when items 156 and 157 were raised, on the second half of Claude Code's ten flags.** ~~87 plus 66 must equal 153.~~ **Corrected 2026-09-04 when items 154 and 155 were raised, both flagged by Claude Code in its report on `PROMPT_claude_code_2026-09-04_classifier_chart.md` and both verified here by reading the two files.** ~~86 plus 66 must equal 152.~~ **Corrected 2026-09-04 when item 153 was raised.** ~~85 plus 66 must equal 151.~~ **Corrected 2026-09-03 when item 152 was raised.** ~~87 plus 64.~~ **Corrected 2026-09-02 when items 65 and 132 closed, both fixed by change log item 51 of `IntelliBooks\App\Docs\IntelliBooks-Change-Log.md`. Items 66 and 84 each had half fixed in the same change and both stay open, narrowed rather than closed.** ~~98 plus 53 must equal 151.~~ **Corrected 2026-09-01 when items 39 to 46, 106, 108 and 109 closed, being eleven in one edit.** ~~97 plus 53 must equal 150.~~ **Corrected 2026-09-01 when item 151 was raised.** ~~96 plus 53 must equal 149.~~ **Corrected 2026-09-01 when item 150 was raised.** ~~95 plus 53 must equal 148.~~ **Corrected 2026-09-01 when item 149 was raised.** ~~96 plus 52.~~ **Corrected 2026-09-01 when 148 closed.** ~~95 plus 52 must equal 147.~~ **Corrected 2026-08-31 when item 148 was raised.** ~~97 plus 50.~~ **Corrected 2026-08-31 when 146 and 147 closed.** If it does not, an item was removed without being recorded. Checked 2026-08-22. **Closed on 2026-08-21: items 1 to 30, 32, 47, 55, 72, 79, 98, 99, 102, 104, and 139 to 144.** Items 53, 107, 110, 129, 136 and 137 were closed earlier. **Sections 1, 2, 4 and 6 are empty. Section 3 holds items 145, 150, 151, 153, 161 and 162.** ~~Section 3 holds items 145, 150, 151, 153, 161, 162 and 164.~~ **Corrected 2026-09-05 when 164 closed.** ~~Section 3 holds items 145, 150, 151, 153, 161, 162, 163 and 164.~~ **Corrected 2026-09-05 when 163 closed.** ~~Section 3 holds items 145, 150, 151, 153, 161, 162 and 163.~~ **Corrected 2026-09-05 when 164 was raised.** ~~Section 3 holds items 145, 150, 151, 153, 161 and 162.~~ **Corrected 2026-09-05 when 163 was raised.** ~~Section 3 holds items 145, 150 and 151.~~ **Corrected 2026-09-04. Item 153 was raised into section 3 and this sentence was not updated with it; 161 and 162 were raised the same day.** ~~Sections 1, 2 and 4 are empty.~~ **Corrected 2026-09-01 when section 6 emptied.** ~~Section 3 holds three items, 145 to 147.~~ **Corrected 2026-08-31: 146 and 147 were built and closed that day.** ~~Sections 1 to 4 are all empty.~~ **Corrected 2026-08-23 by amendment 161.** That sentence was true when it was written and stopped being true the same day, when amendment 156 raised item 145 into section 3. Flagged by Claude Code in `2026-08-23_REPORT_claude_code_commit_141_to_160.md`.


**Opened 2026-08-20 by the consultant session. Rewritten twice the same day: once after a sweep of the session added twenty items, and again after a four-part sweep of every unread document added the section 8 items. Amended 2026-08-21 twice: section 9 added, being 19 items found by enumerating folders and reading live data files rather than documents; then section 10 added, being 28 items found by reading everything section 9 had recorded as unread, and four items above corrected or withdrawn as a result.**
**Date read from a file timestamp, not from a session header. Amendment 109.**

**A closed item keeps its number and moves to the Closed section at the end.** Numbers are never reused, so the highest number is the count of items ever raised.

**When you close an item, ask two things before moving on. Added 2026-08-21.**

1. **Does anything live still point at it as open?** Search `2026-07-25_CONSOLE_DESIGN.md` outside the amendment record for "item N". A step saying "blocked by outstanding item N" or "see outstanding item N" will send the next session looking for a question that has been answered. The amendment record itself is a dated trail and may name an item that was open when the row was written.
2. **Is the answer somewhere the question will next be asked?** The Closed section is not that place. A session that sees a lock file on disk, or reads `IntelliBooks.bat`, is not reading this file. **Three items had to be answered a second time in `CLAUDE.md` for exactly that reason**, being 10, 26 and 104, and item 10 had been answered five times before that.

**This and section 16 of `2026-07-25_CONSOLE_DESIGN.md` are the only two lists.** Section 16
is the chronological build order: everything decided. **This file is everything not decided,
not scheduled, or waiting on somebody.** If an item here becomes a decision it leaves this
file and becomes a step in section 16.

**Ordered by what it blocks, not by where it came from.** The two lists at the end of
`IntelliBooks\App\Docs\IntelliBooks-Change-Log.md` were removed on 2026-08-20 and their
contents are here. Section 7 of `2026-08-20_HANDOVER_consultant_chat_8.md` is superseded by
this file. Neither should be added to again.

---

## 1. Blocking a scheduled step

| # | Item | Blocks |
|---|---|---|

## 2. Waiting on Paul

| # | Item |
|---|---|

## 3. Defects flagged and not fixed

| # | Item |
|---|---|
| 145 | **The MTD ITSA quarterly export does not exist, and building it is scheduled nowhere.** Amendment 100 records the mismatch and states the fix in its own words: "the fix is a second report rather than a correction: a quarterly update needs its own output shaped to the MTD categories, and the master already carries `mtd_itsa_category` on 67 accounts ready for it, read by nothing". **Nothing schedules it.** No step or sub-step in section 16 of `2026-07-25_CONSOLE_DESIGN.md`, and `PROMPT_intellibooks_desktop_changes.md`, which is the standing brief the IntelliBooks Desktop session works from, contains no occurrence of "MTD" at all. Meanwhile `hmrcPeriods()` at line 2276 of `IntelliBooks-Desktop-v3.html` puts MTD quarters in the dropdown on the card headed **HMRC Summary Export**, and the only button beside it, **Export CSV**, calls `exportHMRC()` at 2310, which totals into the 16 SA103F boxes. **Harmless only because `exportHMRC()` has never been run**, and wrong the first time an MTD client's quarter is exported. **Needs Paul's ruling on one thing:** whether the second export goes into the Desktop brief now or is deferred with a date. Raised 2026-08-22 by amendment 156. |
| 150 | **Nothing reads `mapsToCode`. There is no comparative anywhere in IntelliBooks, so last year against this year cannot be produced at all.** **Raised 2026-09-01 by the consultant session, deliberately, so that a decision taken and half built is not left inside a step marked BUILT.** The record half of step 11 of section 9 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md` was built the same day: an archived account can carry a pointer to the account that replaced it, one hop only, merges allowed and splits not expressible. **Paul's instruction, 2026-09-01: build the record and leave the use of it until something needs it.** **What is missing is the use.** Something has to read the pointer and produce a prior-year column: for each account this year, last year's figure, plus the figures of any archived accounts pointing at it, added together. **An archived account with no pointer keeps its own line marked retired**, which is Paul's decision and is what a set of accounts shows when the basis of analysis changes. **Not urgent and not a defect:** the pointer cannot be wrong while nothing reads it, and no client has two years in IntelliBooks yet. **It becomes real the first time a client's chart changes between years**, which is the moment the record was built for |
| 151 | **The archive does not carry the accounts production mapping, so how a filed year mapped onto Sage Final Accounts, or onto whatever else a client is filed through, is not kept.** **Paul, 2026-09-01: probably a future step.** The archive built the same day keeps the account-level trial balance, each account's SA103F box, the box totals, the reconciliation and the balance sheet, all read back from the file rather than worked out again. **What it does not keep is the second mapping.** `mapping_coa_sage_final_accounts.csv` in `IntelliCharts\Accounts production mapping tables\` is 672 rows of `master_code, master_name, entity_type, sage_fa_code, sage_fa_account, sage_fa_category`, it is firm-wide, and there is no per-client per-year copy of it anywhere. **Change it and last year's accounts production mapping is gone.** **Paul's requirement, in his words: the trial balance should carry the Final Accounts columns of whatever software it was exported to.** So it is not only Sage Final Accounts; a client filed through another product needs that product's codes on the same rows. **The archive already has an empty `accountsProductionMapping` slot waiting for it, so nothing has to be restructured.** **Two things undecided:** whether IntelliBooks reads that mapping itself, which it does not today and would have to, and how a product other than Sage Final Accounts declares its codes. **Raised 2026-09-01 by the consultant session, on Paul's own reasoning that a snapshot of the mappings used is what is needed rather than the ability to reconstruct them** |
| 153 | **The partner code blocks are ten wide and nothing guards a spill out of one into another.** ~~The partner drawings block 3210-3219 overlaps `3211 Taxation paid from capital`, so a partnership with two or more partners gets two accounts numbered 3211.~~ **That half is FIXED. Paul's decision, 2026-09-04: `3211` moved to `3240` in `COA_MASTER_v2.xlsx`, with the two rows in `mapping_coa_sage_final_accounts.csv` and the two in `pairings_coa_sage_final_accounts.csv` moved with it, published 2026-09-04 12:10 BST.** Verified after the publish by running `applyPartnerBlocks()` against both republished partnership charts with 1, 2, 3, 5 and 10 partners: no duplicate code in any of the ten. **What remains: an eleventh partner. Found 2026-09-04 by the same test.** Capital introduced is 3200+n and the block 3200-3209 holds ten, so **partner 11's capital account is 3210, which is partner 1's drawings account.** **The design document anticipated the wrong spill.** It records that an eleventh partner's drawings spills into 3220, "which is still Reserves in Sage 50, so it degrades quietly", and 3220 is indeed where partner 11's drawings goes and it is harmless. **Nobody looked at where the capital account goes**, and it lands inside the drawings block. **Rare and not theoretical.** A partnership needs eleven partners to hit it, and the Import chart control refuses and names the duplicate. **`chartFor()` at client creation is unguarded and would create it.** **Two ways out, and neither is urgent.** Widen the blocks, which means finding twenty free codes in the 3200s and moving 3230 to 3232. Or guard both routes: refuse rather than create a duplicate code, which is what the import already does and what `chartFor()` does not. | Found 2026-09-04 by running `applyPartnerBlocks()` against the real `SALE_OF_SERVICES_PARTNERSHIP.csv` in node with two partners. **The two accounts sharing the code are `Drawings - Partner 2` and `Taxation paid from capital`.** `chartFor()` in `IntelliBooks-Desktop-v3.html` gives partner n the codes 3200+n and 3210+n, per the reserved blocks 3200-3209 for capital introduced and 3210-3219 for drawings, and the master carries `3211` inside the second block. **`parseChartCsv()` rejects a duplicate code inside a file and nothing checked the result of the partner substitution**, so this has been silent. **What it would do: `renderReports()` sums by code and would add the two together, and `catOptions()` would offer the operator 3211 twice.** **Not yet triggered.** Only `Client_005 Test Partnership` is a partnership, it has two partners, and its books file has zero categories, so no duplicate exists on disk today. **The Import chart control added by change log item 57 refuses rather than importing a duplicate**, naming the codes, so Test Partnership cannot be given a chart until this is settled. `chartFor()` is unguarded and would still create it at client creation. **Three ways out and the choice is Paul's, because two of them change the master.** One: assign partner codes by skipping codes the chart already holds, so partner 2 takes 3212, which changes the documented 3210+n rule and nothing else. Two: move the drawings block, and the design document notes that 3220 is still Reserves in Sage 50. Three: move `3211` in the master. |
| 161 | **The frozen pane on the `Master` sheet of `COA_MASTER_v2.xlsx` reads `B107`, so the workbook opens scrolled to row 107.** Found 2026-09-04 by the consultant session while renaming `vat_default`, read from the saved file before anything was written, and **left exactly as found**. `2026-08-30_HANDOVER_intellicharts.md` records this same defect being introduced by an `insert_rows` call, corrected to `B2` and verified, **so it has moved again since and nothing records when.** The rename inserted and deleted no rows and read the pane back as `B107` afterwards, so this change is not the cause. The intent is one row and one column frozen, `B2`. |
| 162 | **The `Master` sheet carries one data validation and its own `Rules` sheet says it carries six.** Rules row 52 reads "The dropdowns on the Master sheet read the blocks above, so a wrong value cannot be typed into those columns". **Read from the saved file 2026-09-04: `Master` has exactly one, a `"Yes,No"` list on `G2:G232`, which is `vat_variable`.** There is no dropdown on `vat_default`, `type`, `status`, `applies_to`, `mtd_itsa_category` or `classifier_eligible`. **And the one that exists stops at row 232 while the accounts run to row 241, so nine rows have none.** `publish_master.py` still refuses to publish a value that is not on the Rules sheet, **so this is a typing aid missing rather than a data defect, and no wrong value is in the master today.** Relevant to the `vat_default` rename because a dropdown on that column would have been left stale by it, and there was none to update. |

## 4. Firm and client settings the firm cannot see or control

Found by the settings list of 2026-08-20 and not carried anywhere until now.

| # | Item |
|---|---|

## 5. Decisions not taken

| # | Item |
|---|---|
| 31 | **Whether `2026-08-18_BOUNDARY_two_products.md` stays in Intellibills' repository.** Its own section 1 says the placement is provisional and why it is uncomfortable |
| 33 | **The capitalisation threshold.** `0030 Office equipment` and `0035 Computer equipment` are assets and `7502` and `7552` are the expense route. **The chart cannot decide which a receipt goes to.** The chart of accounts note says it is a policy figure and belongs in the design document |
| 34 | **A statement period straddling 5 April.** 18.2b's own open item: a statement running 1 March to 30 April, and whether it appears in both tax year folders |
| 35 | **Categories in receipts and transactions**, 18.10, which also decides whether category appears in the Difference check at all |
| 36 | **Whether a filed receipt ever gets a correction route**, 18.10. Under 18.6 a receipt is editable while it waits, so what remains is whether a figure recorded wrongly in the pipeline's own record can ever be corrected there |
| 37 | **Where the process logs go**, 18.2a's open item, entangled with whether `logs\` and `exports\` stay in the repository. `logs\runs.ndjson` and `data\run.log` were one letter apart, which is what the question exists to remove |
| 38 | **Whether the demo shares `IntelliCharts\` or takes its own copy**, and whether the phone is demonstrated at all. Both in `2026-08-20_NOTE_demo_version.md`, which is parked |

## 6. Cloud only

**Local multi-firm is not built and will not be built.** Paul's decision, 2026-08-20.
Multi-firm lives only in the cloud.

**Empty since 2026-09-01, and it is not to be added to.** The eight constraints it held, items
39 to 46, are section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, and
all eight closed here on the same edit. The settled point about email that sat at the foot of
this section is section 5 of that document. **A new cloud-only constraint goes there, not here.**
Item 52 below is the one open pointer to the cloud version.

## 7. Deferred by decision

| # | Item |
|---|---|
| 48 | **Beancount export.** Phase 2, with forward-compatibility measures already in place |
| 49 | **Statement parsing.** The Uber parser is a separate repository with its own project and is not scheduled here |
| 50 | **IntelliCharts. Unparked 2026-08-23 on Paul's instruction, and under active design.** ~~Parked. The default chart of accounts is sufficient for Intellibills and IntelliBooks to continue.~~ ~~**Three of the master's columns are read by nothing and that is deliberate**, being `frs102_1a_line`, `frs105_line` and `mtd_itsa_category`: Paul's decision to populate `frs102_1a_line` and `frs105_line` and park their use. Item 145 of this list deals with `mtd_itsa_category`.~~ **Struck 2026-08-23. Amendment 23 of the IntelliCharts note carries the mapping columns into IntelliBooks, two per client, so that sentence is overtaken. It is struck rather than corrected because it went stale here twice in two days: which columns are read and why is section 4 of that note and belongs nowhere else.** **This row is the only pointer to IntelliCharts on this list, added 2026-08-21 by amendment 155. Its open items live in section 8 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md`, which is its own design document and its own list.** Twelve open there as at 2026-08-23, enumerated: 12 open plus 1 closed against a highest number of 13. **Nothing about the chart is duplicated here**, so there is nothing to keep in step. The one exception is item 33, the capitalisation threshold, which stays here because **the chart cannot decide which account a receipt goes to**. **And this row now sits in the wrong section, 7, Deferred by decision, because IntelliCharts is not deferred. It stays there as the pointer until Paul says where it belongs.** |
| 51 | **The demo version.** Parked, with `2026-08-20_NOTE_demo_version.md` as its starting point |
| 52 | **The cloud version.** Parked, with `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root as its starting point and its inbox. ~~Section 6 above is its inbox.~~ **Corrected 2026-09-01 when that document was created and section 6 emptied into it** |
| 149 | **The printing, postage and stationery overlap is kept on purpose, as test material.** **Paul, 2026-09-01, and this is a deferral not a defect.** The master carries four accounts covering the same ground, all pointing at SA103F box 23: **`7500 Printing, postage and stationery`, which is the whole of the other three, plus `7505 Printing`, `7510 Postage & Courier Services` and `7520 Stationery and office supplies`.** They sit unevenly across the charts: Sale of Services has `7500`, `7510` and `7520`; Sale of Goods has `7500` and `7520`; Financial Adviser has all four; Private Hire Driver has none. **Paul is keeping them exactly as they are because he intends to use this set to test three things: maintenance of the master, maintenance of the industry and sector charts, and maintenance of a client's own chart.** A real overlap spread unevenly across four charts is better test material than anything invented. **So nothing is to be tidied here until that testing is done, and a later session finding this overlap should read this entry and leave it alone.** **What is deferred, and it is a real decision when it comes:** whether the charts carry the split, `7505`, `7510` and `7520` with `7500` dropped, or the combined `7500` with the three dropped. **The split is what the master does everywhere else**, it names natures, and it is where the VAT notes live, zero-rated printed matter on `7505` and the exempt Royal Mail universal service on `7510`. **`7500` stays in the master either way, because a client may want the single line.** The return is unaffected whichever way it goes, since all four are box 23. **Raised 2026-09-01 by the consultant session on Paul's instruction, after it offered to tidy the overlap and he said no** |

## 8. Found by the four-part document sweep of 2026-08-20

Four parallel sweeps read `2026-07-31_PLAN_reset_and_restructure.md`, the seven
`REPORT_claude_code_*.md` files, ~~the seven documents in `IntelliBooks\App\Docs\`~~
**seven of the sixteen documents in `IntelliBooks\App\Docs\`, corrected 2026-08-21 by listing
the folder: it holds 21 files, five of them `.bak` copies of the change log, plus a
`Claude CoWork Sessions` sub-folder that had never been listed**, and `CLAUDE.md` with
`RECEIPT_CAPTURE_GUIDE.md`,
`CATEGORISATION.md` and `EMAIL_PROCESSING_MICROSTEPS.md`. **About 370 raw findings.**
Most were closed, superseded by the 2026-08-01 reset, or duplicates of items above.
These are what survived.

**Three findings were verified against today's state and are closed rather than carried:**
the seven test files that patched `config.DATA_DIR` were updated and
`tests/test_path_layout.py:105` now asserts the constant cannot return;
`.git\index.lock` is gone; and the event logs were archived into `Intellibills\Backups\`.

### Defects seen and deliberately left

| # | Item |
|---|---|
| 54 | **`learn_from_correction()` raises `TypeError` if reached.** `worker/categorisation/engine.py:399` passes `vendor_key` to `upsert_client_vendor()`, which has no such parameter and requires `vendor_code`; the same method also passes `vendor_key` to `get_firm_vendor()` and `upsert_firm_vendor()`. Unreachable today because the only outside references are in `docs/specs/`. **Paul has seen it and said to leave it** |
| 56 | **`t.category` is not always a code or empty:** a linked transfer pair carries the literal `"(Transfer)"`. Recorded so nothing is written that assumes otherwise |
| 57 | **A malformed sidecar loses its image**, because `JSON.parse` throws before the image is claimed and the `catch` only warns. And **a loose PDF with no sidecar never enters the books at all**, because line 1233 tests `if(!/^image\//.test(f.type))continue;` |
| 58 | **The Receipts tab has no Category column**, so the field that blocks posting cannot be seen from the screen where posting starts |
| 59 | **`t.category=r.category\|\|""` is unguarded at both cashbook call sites** while `attachReceipt()` guards it, and both are where 18.5a and 18.7 land |
| 60 | **The To Cashbook button is offered on rows posting will refuse**, being any row where `rGross(r)<=0` |
| 61 | **`updRule()` is a second door onto duplicate rules**, and `addRule()` accepts a one or two character pattern with no minimum length check |
| 62 | **`delCategory()` does not check Review items**, so a category a review item points at can be deleted |
| 63 | **`exportPracticeBackup()` reports orphaned books files rather than including them.** A decision, not a side effect |
| 64 | **Two toasts collide in `exportHMRC()`:** the unmapped-categories warning is overwritten by the success toast, and the warning is the more important. Unobserved, because `exportHMRC()` has never been run |
| 66 | **One stale comment in `IntelliBooks-Desktop-v3.html`, at line 2331: it cites amendment 53 as an open question when section 18.9 of `2026-07-25_CONSOLE_DESIGN.md` lists that question as cancelled, so the comment argues against the decision.** ~~Two stale comments: line 1752 cites amendment 53 ... and the root-picker gate text at line 558 describes the pre-item-12 layout as current, and it is the first sentence a new operator reads.~~ **The gate text half was fixed 2026-09-02, change log item 51.** It now says books are in `IntelliBooks\Books\` and that a client's receipts, statements, HMRC summaries and handover packs are in `Clients\[name]\IntelliBooks\`. **Line numbers in the struck wording are stale and are kept as written, per amendment 82** |
| 67 | **A legacy migration read in `loadBooks()` at line 506 reads a layout that no longer exists anywhere.** Looks removable and was not removed |
| 68 | **`renderAll()` loses interface state**, because `renderAccounts()` rebuilds the account filter with no selection restored and runs before `renderBank()`, which reads it |
| 69 | **There is no backup beside `IntelliBooks-Desktop-v3.html` that can be reverted to and still leave a working system**, and reverting fails silently because every read is inside a `catch` that returns quietly |
| 70 | **`RESOLUTIONS_DIR` has an environment override on the pipeline side and none in the app**, so setting it parts the two halves silently. Not live, because the variable is not set |
| 71 | ~~**`tests/test_resolution_view.py:283` still seeds a three-digit `nominal_code`** as a literal in its own temporary database~~ **Corrected 2026-08-21 by reading all 40 files in `tests\`: it is five files, not one.** `test_resolution_service.py`, `test_resolution_view.py` (two separate methods, not only the one at :283), `test_retroactive_categorise_sidecar.py`, `test_sidecar_category_keys.py` and `test_vendor_import_requires_client_id.py`. The values are `271`, `999`, `500` and `103`. **The last of those five is different in kind and is not a stale literal:** `test_vendor_import_requires_client_id.py:30` and `:35` are CSV fixtures in the format `seed_client_vendors.py:85`'s comment describes, `"103 Fuel,,,"`, so a three-digit chart is the input format of a legacy import path. Whether that path survives at all is undecided |

### From the reset plan, still open

| # | Item |
|---|---|
| 73 | **Whether an export belongs in the client's own folder rather than `Intellibills\Exports\`.** The location was decided; this rider was not. "Paul has called it a real choice" |
| 74 | **`worker/filing.py:103`, statement filing into `Clients\{name}\Statements\{tax year}\{platform}\`, is deliberately not frozen and has no interim contract.** The `statements` table is empty, no `Statements\` folder exists under any client, and nothing reads it. **Do not assume the 18.2b freeze covers everything in `filing.py`** |
| 75 | **`config.py`'s `mkdir` block still creates five folders at import, two of them in OneDrive.** The trap is in `CLAUDE.md`; the behaviour is unchanged |
| 76 | **The lost `Client_003` vendor mapping.** The only thing in the entire reset that was not test data, recoverable from `Intellibills\categorisations_client_vendors_cleaned.csv` if it is ever wanted. No decision recorded |
| 77 | **What "filed" means and what `filed_path` points at once the pipeline stops filing into `Clients\`.** Named in the reset plan and unanswered |
| 78 | **Check 6 of the interim's close condition is written against a mechanism that does not exist**, so it tests what `filed_path` and `filed_at` do today rather than what replaces them. Revisit when 18.3 is specified |
| 80 | **What wrote `data\receipts.db-shm` at 14:00 on 2026-07-31 was never explained**, and the count of `.fuse_hidden` artefacts does not reconcile: 24 found, nine deleted |

### Undecided, from the IntelliBooks documents

| # | Item |
|---|---|
| 81 | **PHV mode across the capture app, statement routing and statement filing** is specified in section 5.3 of `IntelliBooks-System-Specification.md` and unbuilt. It is Intellibills' work and separate from the Uber parser, which parses statement contents |
| 82 | **How a `needs_review` query reaches the client.** Phase 2 in the specification and nowhere else |
| 83 | **How the relationship between two rules for one supplier is surfaced.** The larger question behind change log item 30 |
| 84 | **Whether the `exportHMRC()` toast collision is fixed with one composed string or two calls.** The unmapped-categories warning at `IntelliBooks-Desktop-v3.html:2835` fires synchronously while the success toast at `:2833` fires from a promise, and `toast()` is one element, so the warning is overwritten by the less important message. **Item 64 is the same defect seen from the other side.** ~~and whether the raw client name in three toasts is fixed with a shared helper or three edits~~ **That half was settled 2026-09-02 on Paul's instruction and built: one shared helper, `clientFolderPath()`, used by all five messages that name a client folder path and by `filed_path`. Change log item 51, which also closed items 65 and 132** |
| 85 | **Two accepted security exposures**, recorded in specification section 6 and never revisited: anyone with a client's capture link can add files to that client's inbox, and the Graph application permission is `Files.ReadWrite.All`, which is tenant-wide |

### Outstanding checks nobody has run

| # | Item |
|---|---|
| 86 | **Seven chart-adoption checks for Paul, all outstanding**, listed in section 14 of `IntelliBooks\App\Docs\2026-08-17_REPORT_desktop_renderRules_fix.md`. Checks B, C and D were verified by reading books files on disk, not on screen; check E, a partnership with no partners, was later run and passed |
| 87 | ~~**The five-step `HMRC Summaries` check has never been run**, and `exportHMRC()` has never been run at all~~ **Both halves corrected 2026-08-21 by listing `Clients\` on disk. `exportHMRC()` has been run twice** and its output is there: `Clients\TEST\HMRC Summaries\test-hmrc-2025-04-06-to-2026-04-05.csv`, written 2026-08-17, and `Clients\Test Company\HMRC Summaries\testco-hmrc-2025-04-06-to-2026-04-05.csv`, written 2026-08-18. **The folder name is `HMRC Summaries` in both, which is the single assertion the five-step check exists to make**, so the thing being tested is demonstrated even though the check as written cannot be run: it is written against client `PKPH`, which is gone. **What is still untested is the CSV's contents**, being step 5 of that check, the fifteen SA103F rows and the absence of a `,WARNING` line |
| 88 | **The Review seam has never been tested against a review item the pipeline itself produced.** The first real `needs_review` receipt is the test |
| 89 | **The contents of `handoverPack()`'s six files have never been checked against a real set of books** |
| 90 | **Change log items 19 to 23 have never been tested live**, which is item 9 above, and the weekly Netlify function log review named in the specification has never been established as a routine |

### One class of finding, not one item

| # | Item |
|---|---|
| 91 | **`CATEGORISATION.md` uses three-digit GL codes throughout and gives code `105` two different account names in one file.** Scheduled in step 10h with the other five stale documents, and named here because it is the one that actively teaches the thing the four-digit scheme exists to make detectable |

## 9. Found on 2026-08-21, by opening what the 2026-08-20 sweep had not opened

**Added because the answer to "is everything that should be on these two lists here" was no.**
The 2026-08-20 sweep read named documents. It did not enumerate the folders those documents
sit in, and it read no live data file. **This pass enumerated four folders and read five data
files**, being `C:\LastingImpact\receipt_capture\` (69 markdown files, 20 read and 49 not),
`IntelliBooks\App\Docs\`, `IntelliBooks\Books\`, `worker\` (20 Python modules), and then
`Intellibills\clients.csv`, `Intellibills\firms.csv`, `Intellibills\pipeline-status.json`,
`IntelliBooks-Practice.json` and `IntelliCharts\COA_MASTER_v1.csv`.

### A second chart of accounts, inside the pipeline

| # | Item |
|---|---|
| 92 | **`worker\categorisation\coa.py` is a second chart of accounts, hardcoded in Python, and not one of its codes agrees with the master.** It holds three templates on four-digit codes, `PHV_DRIVER` with 21 accounts, `CONTRACTOR` with 15 and `UNSPECIFIED` with 7, being **22 distinct codes**. Compared against all 122 rows of `IntelliCharts\COA_MASTER_v1.csv`: **eight codes do not exist in the master, fourteen exist under a completely different name, and zero match.** The comparison was run on 2026-08-21 against `COA_MASTER_v1.csv`, which has since been superseded by `COA_MASTER_v2.xlsx` and moved to `IntelliCharts\Cockups\`; the figures below are kept as they were measured, per amendment 82, and whoever acts on this item runs the comparison again against `IntelliCharts\Chart Library\Master_COA.csv`. `1100` is "Motor Vehicles" here and **"Trade debtors"** in the master. `7100` is "Employer NI" here and **"Rent"** in the master. `8100` is "Accountancy Fees" here and **"Irrecoverable debts written off"** in the master. **These are not near misses. They are valid master codes pointing at the wrong account**, which is the one kind of wrong code no validator can catch |
| 93 | **Nothing has been posted from it, and that is a flag setting rather than a design.** Its only caller is `worker\categorisation\engine.py:336`, inside `_ai_suggest()`, reached only from `engine.py:301` behind `self.enable_ai_fallback`. That defaults to `False` at `engine.py:150`, is passed `False` explicitly at the only production construction, `app.py:664`, and `False` in all five test constructions. **Turn that one argument to `True` and layer 4 begins suggesting codes from item 92's list.** Whether the fix is to delete `coa.py`, to make it read the master, or to delete layer 4 altogether, is a decision, which is why this is here and not scheduled |
| 94 | **The prompt sent to OpenAI contradicts itself on the code format.** `engine.py:368` describes the return field as `"GL code (e.g., 103, 281)"`, which is three-digit and therefore legacy by amendment 96, in the same API call that lists four-digit codes as the valid set |
| 95 | **`get_coa_for_client()` at `coa.py:73` has no caller anywhere**, and its docstring promises a future per-client override in a `coa_client_codes` table. **That table is cancelled**: there is no chart inside the pipeline's database and there will not be one |
| 96 | **`business_type` values in use do not match the templates that exist.** `clients.csv` holds `PHV_DRIVER`, `ACCOUNTANCY` and `UNSPECIFIED`. **`ACCOUNTANCY` has no template, so `coa.py:70`'s `.get(business_type, _COA_TEMPLATES["UNSPECIFIED"])` silently returns the 7-account fallback**, which is the same silent-default class as the `UNKNOWN` fault. `CONTRACTOR` is a template no client uses |

### The two registries, read side by side for the first time

| # | Item |
|---|---|
| 97 | **The two client registries have no client in common. The intersection is empty.** `Intellibills\clients.csv` holds `UNKNOWN`, `PKPH`, `INTELLITAX`, `TEST3`, `TEST4` and `SHERUNSIT`. `IntelliBooks-Practice.json` holds `TEST`, `TEST2`, `TESTCO`, `TESTST`, `TESTP` and `ZPO`. **Both sets were enumerated, not filtered.** Amendment 111 point three already decides what comes across, so this is not a new decision; it is the scale of the divergence, which no document states |
| 100 | **`clients.csv` carries `UNKNOWN,Unknown Client,,FIRM001,UNSPECIFIED,UNKNOWN,Default for unmatched receipts` as a data row.** Amendment 115 removes the `UNKNOWN` defaults from `save_receipt()` and the schema. **It does not say what happens to this row**, which is the same default expressed as data, and amendment 111 reserves the `UNKNOWN` id rather than deleting it. **What the row becomes in `clients.json` is undecided** |
| 101 | **The same firm has two live identifiers.** `clients.csv` and `firms.csv` both say `FIRM001`, `config.py:105` sets `DEFAULT_FIRM_ID = "FIRM001"`, and `worker\database\schema.py:78` defaults the `receipts.firm_id` column to `'INTELLITAX'`. **So a receipt written without an explicit firm gets a firm id that matches no row in `firms.csv`.** Amendment 116 removes the schema default; **which of the two strings is the firm is not stated anywhere** |
| 103 | **The `Client_NNN` sequence is not contiguous and nothing owns the next number.** The live ids are `Client_005` to `Client_009`; `Client_001` to `Client_004` are absent, and `Client_003`'s lost vendor mapping is item 76. Amendment 111 point one keeps the form "sequential and system generated". **Whatever generates the next id in step 10d has to be told whether it counts rows or takes the maximum and adds one**, and those give different answers here |

### ~~Two~~ One live-state finding

**Was two. Item 104, the `pipeline.lock`, closed 2026-08-21 and moved to the Closed section**, so the heading was corrected in the same edit rather than left to state a count the table below it no longer supports.

| # | Item |
|---|---|
| 105 | **Change log checks still pending are written against clients that no longer exist.** Three name `PKPH`, including the whole five-step `HMRC Summaries` check at item 87, whose premise is "client PKPH, which is the only client with anything in its books". One names client `Test 3`, code `TEST3`, which is in `clients.csv` but **not** in `IntelliBooks-Practice.json` and has no books file, so it cannot be opened in IntelliBooks at all. **These are not outstanding checks. They are checks that can no longer be run**, and item 87 should not be reported as pending without saying so |

### ~~The remaining unread, stated so the gap is on the record rather than in a chat~~ All three closed

**Was three, being items 106, 108 and 109. All closed 2026-09-01 and the heading corrected in
the same edit rather than left over an empty table.**

**Item 108 closed as done.** It named a fixed set, six documents in `IntelliBooks\App\Docs\`
plus the `Claude CoWork Sessions\` sub-folder, and section 10 below records that exact set as
read on 2026-08-21. The folder was listed again on 2026-09-01: all seven are present and every
one carries a mid-July mtime, so nothing in the set changed after the item was raised.

**Items 106 and 109 closed on Paul's rule of 2026-08-21, amendment 140: a list of things to
close should contain only things that can close.** Both were of the form "N of M files have
never been read whole", which is a fact about what one session knows rather than about the
system. Every new session starts having read none of them, and both sets grow: item 106 counted
69 markdown files in the repository root and there were 79 on 2026-09-01. So neither could ever
close as an item. **Same treatment as item 8, which was a habit and moved to `CLAUDE.md`.**

**Where the answer now lives, because the Closed section is not where the question gets asked
again.** Item 106's substance is already in `CLAUDE.md` under "Spent files leave the root",
which cites the item by name. Item 109's is a sentence added to `CLAUDE.md` on 2026-09-01 under
"The standard of evidence".

## 10. Found on 2026-08-21 by reading everything item 106 to 110 said was unread

**Paul's instruction: "read whatever is left to be read. I want those 2 lists as complete as possible."**
Read: all 37 `PROMPT_*.md`, all 10 unread handovers, `2026-08-03_NOTE_chart_of_accounts_for_paul.md`,
the seven unread documents in `IntelliBooks\App\Docs\` and the whole `Claude CoWork Sessions\`
sub-folder, `app.py` and every module under `worker\`, all 40 files in `tests\`, `docs\specs\`,
the fifteen utility scripts in the repository root, and `.env`, `.env.example`, `.gitignore`,
`requirements.txt` and `requirements-dev.txt`. **Five parallel sweeps produced the raw findings and
every one carried below was then re-verified by the consultant session against the file itself**,
which is how items 71, 87, 107 and 110 above came to be corrected and how one sweep finding was
withdrawn as wrong.

### Three defects nothing has recorded, all verified by reading the code

| # | Item |
|---|---|
| 111 | **`setup_auth.py` cannot run at all, and `RECEIPT_CAPTURE_GUIDE.md` tells Paul to run it.** Line 22 reads `from worker.email.reader import get_token`. **`get_token` is defined nowhere in the repository**, by a search of every `.py` file, which returns only `setup_auth.py`'s own two lines. `config.SHARED_MAILBOX`, used at `:29` and named at `:43`, **does not exist in `config.py`** either. So the script raises `ImportError` at the first line of real code. Its docstring walks through registering an Azure AD app and granting Microsoft Graph delegated `Mail.Read`; **the live pipeline is IMAP**, and `worker\email\reader.py` does `imap.login(config.IMAP_USERNAME, config.IMAP_PASSWORD)`. This is a whole script left behind by the move off Graph. Whether it is deleted or rewritten is a decision; **that the operator guide still points at it belongs in step 10h** |
| 112 | **`app.py:367` to `:371` invents four values silently, and one of them decides which tax year a receipt is filed under.** Inside `_file_unfiled_ok_receipts()`, the recovery path for receipts marked `ok` but not yet filed: `invoice_date = extraction.get("invoice_date") or datetime.now(timezone.utc).date().isoformat()` at `:367`, and **`tax_year = determine_tax_year(invoice_date)` reads it at `:368`**, so a missing date files the receipt under the tax year of the day the recovery ran. Then `supplier` falls back to `"unknown"` at `:369`, `gross` to `0.0` at `:370` and `currency` to `"GBP"` at `:371`. **All four are written into the filed record and the sidecar as though extracted, and none logs anything.** The tell that this is an oversight rather than a choice is in the same function: it logs a warning for a missing extraction row at `:358` and for a missing source file at `:363`. `worker\validation\rules.py` should make the branch unreachable, because a receipt only reaches `ok` with supplier, date and gross present, **but this function re-reads the extraction row from the database rather than using the one that validated**, so the two can differ |
| 113 | **Amendment 94's defect is live, and it fires precisely because of item 101.** `resolve_client_info()` at `worker\database\repository.py:57` returns `("UNKNOWN", "INTELLITAX", "UNKNOWN")` on both of its failure paths, `:60` and `:69`. `app.py:836` unpacks that, then `app.py:839` reads `config.FIRMS.get(firm_id, {}).get("name", firm_id)`. **`config.FIRMS` is loaded from `firms.csv`, which keys on `FIRM001`, so the lookup misses and the fallback returns the literal string `INTELLITAX`.** `app.py:847` then sends that to the client as the firm name in the no-attachment alert and `:848` stores it in `email_alerts.firm_name`. **So the two firm identifiers of item 101 are not merely untidy: their disagreement is what puts a system string into a customer-facing email.** Amendment 94 recorded the guard as missing on this branch on 2026-08-17 and it is still missing. Not fired since the reset, because `email_alerts` is empty |

### Dead code, each confirmed by a search of the whole repository rather than of one file

| # | Item |
|---|---|
| 114 | **`write_review_file()` has been flagged six times in four documents, instructed once to be deleted, and is still there.** It sits at `worker\filing.py:323` and **nothing in the repository calls it**: a search for the name returns its own `def` line and six mentions in documents. Amendment 49 records it. The design document describes it at line 381 and again at line 1795. `PROMPT_claude_code_phase0_step4.md:58` says "Note while you are here, do not fix", `:113` says "Do not delete `write_review_file()` in this commit. Report it", `PROMPT_claude_code_phase0_step5.md:122` says "Do not change `export_bookkeeping.py`, `write_review_file()`, or `_count_review_items()`'s name", and `2026-07-29_HANDOVER_consultant_chat_3.md:146` says "Confirm and delete rather than leave a second writer whose output cannot be worked." **It is now confirmed dead and the deletion has never been scheduled.** Separately: **the design document cites it at `worker/filing.py:142` in both places and it is at `:323`**, so both line references are stale |
| 115 | **`regenerate_codes()` at `regenerate_vendor_codes.py:68` is not called by anything, including by its own file.** The module's `if __name__ == "__main__":` block re-implements the same logic inline rather than calling it, so the one file that exists to run it does not |
| 116 | **Three dead names in `worker\intake\folder_reader.py`, all left over from a filename-prefix design that amendment 112 replaced.** `INTAKE_PATTERN = "rcpt_"` at `:13`, `STATEMENT_PREFIX = "stmt_"` at `:14`, and `IntakeRecord.internal_path` at `:32` and `:46`. Each appears nowhere else in the repository. `scan_inbox()` tells a statement from a receipt by the sidecar's `type` key, which is the decision amendment 112 recorded |
| 117 | **`claimed_client_code` is the field the `UNKNOWN` fault needs, it is written into every sidecar, and it has never carried a value.** Declared at `worker\filing.py:356`, written at `:378`, and **passed the literal `None` at all four call sites in the repository**: `worker\extraction_pipeline.py:236`, `worker\extraction_pipeline.py:278`, `worker\resolution\service.py:703` and `app.py:420`. It is dead on the reading side too: the design document at line 1267 records that IntelliBooks' sidecar shape "has no `confidence`, no `capture_date`, no `asserted`, no `claimed_client_code`". **The claimed-versus-resolved mismatch is exactly what amendment 115's review rule is about, and the field for it already exists.** Whether step 10d populates it or removes it is a decision, and it should not be answered by whoever happens to write the brief |

### One comment that argues against the code beneath it

| # | Item |
|---|---|
| 118 | **`worker\extraction\postprocess.py`'s module docstring describes behaviour the file does not have.** At `:16` to `:18` it says "The broad `try/except Exception: pass` blocks are kept exactly as they were and are load-bearing". **Neither broad block passes.** `apply_vat_inclusive_swap()` at `:129` to `:134` and `resolve_invoice_date()` at `:202` to `:203` both call `logger.warning(..., exc_info=True)`, and the file's own inline comment two lines below the docstring says "Logged rather than swallowed". A future session reading the docstring would believe failures here are silent, and would be wrong in the safe direction, which is why this is here rather than among the defects |

### What amendment 116's rebuild costs the test suite

| # | Item |
|---|---|
| 119 | **Two tests exist only to test the `ALTER TABLE` guards amendment 116 deletes, so they do not fail, they become meaningless.** `tests\test_discard_reason.py`, `test_the_column_exists_on_an_older_database` at `:60`, hand-builds `resolution_events` without a `reason` column and asserts at `:85` that `init_db()` adds it; the only thing that does is `worker\database\schema.py:231` to `:232`. `tests\test_filed_at_column.py`, `test_existing_rows_are_not_back_filled` at `:116`, drops `filed_at` with raw SQL and asserts at `:146` that `init_db()` re-adds it; the only thing that does is `schema.py:219` to `:220`. **Enumerated across all 40 files: these two are the only tests in the suite that depend on the ALTER path**, every other test that touches `pipeline_version`, `receipt_ref_number`, `receipt_time`, `duplicate_of` or `locked_at` works against a table built fresh in the same run |
| 120 | **Amendment 116 removes the defaults from the schema, and the ones that actually take effect are in `repository.py`.** `save_receipt()` at `worker\database\repository.py:206` to `:209` carries `firm_id="INTELLITAX"`, `client_id="UNKNOWN"`, `client_code="UNKNOWN"` and `source="email"` as Python keyword defaults. **Python supplies the value before SQL is reached, so removing the column defaults alone changes nothing**: the column becomes `NOT NULL` but never receives a NULL to reject. `tests\test_resolution_service.py:268`, `test_a_receipt_with_no_extraction_returns_not_found_saying_so`, is the only call in the whole suite that omits all three, and it breaks only if the Python defaults go too. **Amendment 116 should say which, because "becomes required" has no effect otherwise** |
| 121 | **Two tests cannot fail for the reason they state, and one of them says so about itself.** `tests\test_default_firm_id.py:99`, `test_a_row_without_a_firm_id_column_gets_the_constant`, compares `client["firm_id"]` against `config.DEFAULT_FIRM_ID`, reading the live constant on both sides of the assertion. **The file's own docstring at `:21` to `:31` records that reverting `config.py:120` to the literal `"FIRM001"` left all 281 tests green and that "The constant was decorative"**, which is why `SentinelDefaultFirmIdTest` was written; the toothless test was left in beside it. `tests\test_extractor_name.py:28` to `:31`, `test_name_matches_engine_recorded_on_a_result`, carries a comment about the failure path recording a different engine string from the success path, and a body that asserts only `OpenAIVisionExtractor().name == "openai_vision"`, word for word the test above it at `:25`. The regression it is named for is actually covered by `tests\test_failure_path_engine.py` |

### What was lost when documents were merged, and it is not what item 107 said

| # | Item |
|---|---|
| 122 | **The merge of change log items 1 to 11 compressed each to a one-line "Status: done" and dropped the riders those items carried. Two of the riders are substantive.** Verified by reading both files side by side. `IntelliBooks-Change-Log-Original-Items-1-11.md` item 8 reads "Export offers BOTH discrete-quarter and cumulative year-to-date totals (agreed 16 Jul; HMRC's cumulative quarterly updates make YTD the likely need, **bridging trials to confirm**)" and "Product-specific templates (Absolute Taxfiler, 123 Sheets, TaxCalc) added only if testing shows the generic CSV is awkward for one of them". **The word "bridging" appears in the current `IntelliBooks-Change-Log.md` only in Item 8's own title.** So the trial that decides whether the HMRC CSV is fit for a real bridging product is recorded in a superseded file and nowhere current, and item 87 above shows the export has now been run twice without it |
| 123 | **The same merge dropped an accounting rule and an accounting meaning from item 7, and both are Paul's to rule on rather than a build session's.** `IntelliBooks-Change-Log-Original-Items-1-11.md` item 7 reads "cash-paid receipts from the owner's pocket also post to Personal (notional); **a cash account is only created for clients who RECEIVE cash income (a real float to account for)**" and "Account balance represents net owner funding (capital introduced less drawings), the year-end equity note". **Neither phrase appears anywhere in the current `IntelliBooks-Change-Log.md`.** Whether the first is an operator convention or a rule the app should enforce when an account type is chosen has never been asked |
| 124 | **`SETUP-v5.md` is a seventh materially stale document and step 10h names six.** Its own header says "sections 2 and 4 below predate the item 12 folder restructure" and then **the step text was never corrected**: section 2 still reads "Each gets a books file at `Clients\[name]\IntelliBooks\[CODE]-books.json`". It also still directs the operator to a **Import Filed Receipts Folder** button, which `HANDOFF-Status.md` records as "removed as redundant". A document that flags its own staleness in a header and leaves the wrong instruction below it is worse than a plainly stale one, because the header reads as though it has been dealt with |
| 125 | **`IntelliBooks\App\Docs\HANDOFF-Status.md` is stale in the other direction and shows an item as open that is closed.** It lists "One fresh end-to-end phone test on the current architecture, still outstanding". **That was done on 2026-08-20**, when `rcpt_testst_mt1q8xwh.jpg` ran end to end for the first time on this layout and filed as `Clients\TESTST\Receipts\2026-27\2026-05-08_imo-car-wash-57-high-path-merton_4.50.jpg`. The file should be corrected or retired, and it is a candidate for step 10h |

### Raised once, never carried forward, and each verified absent from every later document

| # | Item |
|---|---|
| 126 | **Two defects in `export_bookkeeping.py` were found, deferred twice, then dropped.** `2026-07-28_HANDOVER_consultant_chat_2.md`: "Two real defects were found in it and left alone: bare `e.*` columns outside the `GROUP BY`, and `MAX()` on text aliased as `latest_*`, **so a receipt whose most recent attempt failed can export as `ok`**." Restated in `2026-07-29_HANDOVER_consultant_chat_3.md` alongside the open question of whether the script should carry the GL code at all. Absent from handovers 4, 5, 6, 7 and 8. **The second defect exports a wrong status, which for a bookkeeping export is the serious kind** |
| 127 | **Two of the three items section 18.10 named as coming up soonest were never taken up.** `2026-07-31_HANDOVER_consultant_chat_4.md`: "Those three are: categories in receipts and transactions, extending `chart_of_accounts_DRAFT.csv`, and whether a filed receipt gets a correction route." Restated in `2026-08-02_HANDOVER_consultant_chat_5.md`. **The third of the three, the chart, then took ten recorded decisions across handovers 6 and 7. Neither of the other two appears in either.** The correction route is the one that matters, because it is what happens after a receipt is already filed |
| 128 | **Two OpenAI credential questions, raised three times and dropped, and they gate step 19.** `HANDOVER_consultant_chat.md`: "Whether an org-level OpenAI Admin key on this workstation is acceptable. If not, design doc 9.3 is skipped and the local token ledger stands alone", and "Whether to issue a dedicated OpenAI API key or project for this app, needed for clean cost attribution." Repeated in handovers 2 and 3 with "Both bear on step 19", then absent from 4 onwards |
| 130 | **A staff-facing "N items need review" alert was raised once and never again.** `2026-07-24_HANDOVER_TO_NEXT_SESSION.md`: "Raised as a possible cheap stopgap before the full dashboard exists (currently there is no staff-facing notification at all)... Not decided or scoped." It may be superseded in spirit by the console's intake panel in section 8.6; nothing says so |
| 131 | **Step 10b's reconciliation check is decided in principle, specified nowhere, and scheduled nowhere.** ~~the restatement has not been traced~~ **Traced 2026-08-21. Amendment 73 restated it:** the question becomes whether the client folder matches IntelliBooks' delivery log at `IntelliBooks\Delivery\{CODE}.log`, and section 13A left the pipeline's build order with it. **So what is open is the specification, not the scope.** And a second thing amendment 73 says outright, which is separate: "Intellibills may still want a smaller check of its own archive against its own database; **that is not specified and is not this step**" |

### Hardcoded firm values outside the pipeline's own modules

| # | Item |
|---|---|
| 133 | **`.env.example` ships live production values where a new deployment expects placeholders.** Line 2 reads `IMAP_HOST=mail.lastingimpact.co.uk` and line 4 `IMAP_USERNAME=capture@lastingimpact.co.uk`. It is the file every fresh working copy copies, **including the demo clone of `2026-08-20_NOTE_demo_version.md`**, so it is the one place a firm value propagates by design rather than by accident |
| 134 | **Three more firm and personal literals, none in the pipeline's live path.** `setup_auth.py:13`, the docstring step "Your M365 account must have Full Access to **bills@intellitax.co.uk**", which is also the stale mailbox of item 102. `import_vendor_csv.py:72`, a usage string containing the full path `C:\Users\PDK7\OneDrive - Intellitax Accounting Limited\Intellibills\...`. And `seed_client_vendors.py:135`, `if detail.endswith(" Paul Keating:")`, **a hardcoded personal name used to parse a bank export**, which is a firm value in everything but name |
| 135 | **`app.py:839`'s firm-name fallback is the mechanism of item 113 and is worth naming as a pattern in its own right.** `config.FIRMS.get(firm_id, {}).get("name", firm_id)` means an unregistered firm is displayed to a client under its own id rather than failing or logging. In the cloud multi-firm version that is the state every firm passes through during onboarding |

### One thing the `Clients\` listing raises that no document covers

| # | Item |
|---|---|
| 138 | **`Clients\` mixes client codes and client names as folder names, and both conventions are in live use.** Listed 2026-08-21: `PKPH`, `TEST` and `TESTST` are codes; `Paul Keating`, `She Run's It! Ldn Ltd` and `Test Company` are names. Section 18.2b freezes the filing path, and `worker\filing.py` resolves a name through `_resolve_client_name()`, so **the code-named folders are either older than the freeze or were made by something else**. It matters because `Clients\TEST\` and a future client whose name is Test would collide, and because `She Run's It! Ldn Ltd` contains an apostrophe and a space in a path two products build by string join |


---

## 11. Currently unused fields

**Added 2026-08-21 on Paul's instruction, and the instruction matters more than the list.**
**A field that exists and is not used is not a defect and is not to be flagged as one.**
Whether an unused field is a risk is Paul's call, not a session's, and the question is not to
be raised as though it were a fault.

**So nothing here forces a decision.** These are recorded so they are found rather than
rediscovered, and so that anything which later starts reading one knows the value has not
been maintained. **No row in this section asks to be used or deleted.**

**Added 2026-08-22, and it sharpens what this section is for.** A field's **absence** needs a
recorded reason as much as its presence does. Asked why `sa103f_box` is carried in
`IntelliBooks-Desktop-v3.html` and `mtd_itsa_category` is not, this session gave three wrong
answers in succession, each inventing a rationale for the absence, while the real reason sat in
amendment 100's Why column. **Nothing in the HTML says why the column is not there, so a reason
got supplied.** Paul's point, and it inverts amendment 148: the risk of an unused field is not
that somebody codes against it, it is that an absent one reads as a decision. **He also refused
the easy conclusion** that carrying the field would have prevented it, because presence would
have produced the right answer by the same assumption-making that produced the wrong ones. See
amendment 156.

| # | Field | Where | State |
|---|---|---|---|
| 24 | **`firms.csv`'s `email` column.** Loaded into `config.FIRMS` at `config.py:141` and read by nothing. `config.FIRMS` has one reader in the whole system, `app.py:839`, which takes `name`. Also row F6 of `2026-08-20_LIST_settings_firm_and_client.md`, where it is marked identity, being one of the three fields a firm currently is | `Intellibills\firms.csv`, column `email` | Holds `bills@intellitax.co.uk`, last written 2026-07-22 16:32:19 UTC. **Item 102 was the same field and is folded in here** |

---

## Confidence

**High on every item, because each was read from the file, amendment or database that records
it on 2026-08-20**, and each names that source. Item 15 was read back at lines 2406 to 2418
rather than taken from the note that flagged it.

~~**Three things carried as claims rather than facts.**~~ **Two, corrected 2026-08-21.** The
count of files in the repository root, item 32, where both handovers disagree and neither
figure has been verified. ~~The cause of the stale lock, item 26, which nothing records.~~
**Closed 2026-08-21: Paul runs the pipeline on demand and closes it immediately, so the lock
is left behind every time. Items 26 and 104 both rested on the assumption that a lock outliving
a run meant a failure.** And items 28 and 29 rest on the 2026-07-17 capture app snapshot,
which Paul has confirmed is the deployed app.

**How twenty items came to be missing, since it bears on how much to trust this file.** The
first version was built from the two lists at the end of the change log, section 7 of the
handover, and the day's findings. **It was not built from this repository's own design
document open items, nor from the flagged lists inside change log items 36 to 39.** Those held
twenty items between them, of which four turned out to be decisions rather than open questions
and are now section 16 step 10g. **A list built from named sources is only as complete as the
naming.**

**On section 8's reliability, and it is the day's recurring lesson in a new place.** Those
items come from documents, and a document records what was true when it was written. The reset
plan's own status table still reads "not started" for stages 5 and 6, which were completed
after it was written. **So everything carried from a document dated before 2026-08-20 was
checked against the current state, and three findings were closed that way rather than
carried.** Items 76 to 80 are the ones that could not be checked from here and are marked as
unchecked by that wording.

**Nothing here is scheduled.** Anything that becomes a decision leaves this file for section
16 of `2026-07-25_CONSOLE_DESIGN.md`.

---

## Closed

**Numbers are never reused.** A closed item keeps its number and moves here. What was decided about it lives in `2026-07-25_CONSOLE_DESIGN.md`, not in this file.

| # | Item |
|---|---|
| 154 | **Closed 2026-09-04. Fixed on Paul's instruction the same hour it was raised.** Amendment 198. `tests\resolution_fixtures.py` now saves and redirects `CHARTS_DIR` with the other fourteen `config` paths, to `self.path / "Charts"`, and **deliberately does not create it**, which is `config.py`'s own rule for that folder: IntelliCharts publishes the bundle and the pipeline never makes one. A test built on that fixture that reaches layer 5 now gets `load_chart()`'s missing-bundle path, an ERROR and an empty list, instead of reading the live bundle in OneDrive. ~~Harmless today because nothing in that fixture turns `enable_ai_fallback` on.~~ **That was the reason to fix it rather than to leave it: the leak was one test away and would not have announced itself.** ~~The suite was not run from this session and cannot be: the `.venv` is a Windows environment and `CLAUDE.md` forbids importing `config.py` from the Linux sandbox.~~ **Paul ran it on his machine at 13:14 BST: 367 passed, 191 subtests, unchanged from Claude Code's run.** Amendment 200. |
| 156 | **Closed 2026-09-04. Both halves fixed on Paul's instruction the same hour.** Amendment 198. `coa_options` is deleted from `worker\categorisation\engine.py`, and **the JSON schema's `code` description no longer carries an example at all**: it read `"GL code (e.g., 103, 281)"` and now reads that the code is taken exactly from the list of valid codes in the prompt above it. **No four-digit example was substituted, which was the second decision in the fix.** An example lifted from one chart is absent from another client's, so **the list of eligible accounts is the only thing in that call that should name a code.** The `name` description is reworded the same way. Nothing in `tests\` asserts the prompt or the schema, confirmed by search, so **nothing tested this before and nothing tests it now.** **Paul ran the suite at 13:14 BST: 367 passed, 191 subtests.** Nothing in it touches this change, which is what the search for a test asserting the prompt or the schema had already said. **Still open and not part of this: layer 5 has never been run against the real classifier at all.** Amendments 197 and 200. |
| 155 | **Closed 2026-09-04. Fixed on Paul's instruction, by deleting rather than by repointing.** Amendment 198. `find_coa_account_by_name()` is gone from `worker\database\repository.py`, its call is gone from `_resolve_category()` in `worker\resolution\service.py`, and **the `if code:` branch at the old line 1090 is gone with it**: it logged a warning about a disagreement between 12.3 step 6 and 11.3 and could never run, because the lookup returned None for every name. **The disagreement itself is kept, as a comment where the branch was.** `_resolve_category()` now takes only the note, and its docstring says in its first line that the code is always None. **Behaviour is unchanged and that is the point**: the deleted query hit a table cancelled by amendment 96 and confirmed cancelled by 124, so it had returned None since the day it was written. **Why deleted and not pointed at the chart.** The chart now lives in the bundle and `worker\categorisation\chart.py` could read it, but **whether a caption typed in IntelliBooks may be matched to an account by name, with or without `coa_alt_names.csv`, is a decision nobody has taken**, and a dead lookup is not a place to keep an open question. **Two docstrings that stated the cancelled table as fact are corrected**, one in `list_gl_code_options_from_vendors()`. **One thing left deliberately**: `if code or category_name:` still tests `code`, which is now provably None. It is left because it says what the tuple carries, and removing it would presume the decision above. **This fix broke one test, and the message was corrected rather than the test.** `test_a_name_with_no_chart_of_accounts_stores_the_name_and_learns_nothing` in `tests\test_resolution_backfeed.py` asserts the validation note contains the phrase "chart of accounts", and the reworded note had dropped it: 366 passed, 1 failed. **The three behavioural assertions in that test all passed**, the name kept, no code and no vendor learning, **so only the wording assertion failed, and a test edited to match new code is how a real regression gets waved through.** The note now reads that the category "was stored as a name without a code, because a note's category is not matched against the chart of accounts", which is true and carries the phrase. **Then 367 passed, 191 subtests, at 13:14 BST.** Amendment 200. |
| 157 | **Closed 2026-09-04. Fixed on Paul's instruction.** Amendment 199. `worker\categorisation\__init__.py` and `worker\intake\__init__.py` created, both empty, which is what five of the other seven packages under `worker\` hold; only `worker\resolution\__init__.py` carries a docstring. **All nine packages now have one.** Both directories were namespace packages, which worked, so **nothing about the suite changes and that is the expected result.** Checked by importing `worker.categorisation` and `worker.intake` and printing each `__file__`: both now resolve to the new file, and **neither import pulled in `config` or `engine`**, which an empty `__init__.py` is the reason for. |
| 158 | **Closed 2026-09-04 on a run, not on a compile.** Amendment 202. Six utility scripts in the repository root opened `data/receipts.db`, a path amendment 76 removed, and **an sqlite connection to a missing file succeeds and creates an empty database**, so each reported no receipts rather than saying it could not find any. `query_receipts.py`, `view_receipts.py`, `schema_info.py`, `check_client_match.py`, `check_ids.py` and `export_bookkeeping.py` now each do `db = config.DB_PATH` and `sys.exit()` with the path if the file is absent. `schema_info.py` lists all eleven tables rather than four. `export_bookkeeping.py` creates `exports\` rather than assuming it, a second fault in the same script that was not in this item. **Paul ran two of the six at 13:41 BST.** `query_receipts.py` printed **six receipts**, which is the count read off `receipts.db` an hour earlier and is what proves it opened the real database and not one it had just created. `export_bookkeeping.py` printed "Exported 6 receipts to exports\bookkeeping_export.csv". **The run also corroborated sub-step 10d.40 by accident**: the six rows carry all four `source` values, `email`, `phone`, `desktop` and `other`, and no fifth. |
| 159 | **Closed 2026-09-04 on a suite run.** Amendment 205. The IMAP UID watermark was dead in four places: `fetch_new_messages(repo)` took a `repo` it never used, `get_last_uid()`, `save_last_uid()` and the two `delta_link` accessors were called by nothing live, and the `email_delta` table existed to hold keys nothing wrote. **All removed**, and the four documents that described the table corrected. **Paul ran the suite at 14:13 BST: 367 passed, 190 subtests.** **The subtest count fell by one, from 191, and that is the fix proving itself.** `test_no_table_carries_a_client_code` in `tests\test_step10d_pipeline.py` enumerates every table in a freshly created database and subtests each one, so **the number of subtests it contributes is the number of tables `schema.py` creates**: ten now, eleven before. **The pass count did not move.** **Paul also dropped the live table** with a one-line `DROP TABLE`, so his database holds the same ten as a new one, verified by querying it: no `email_delta`. |
| 160 | **Closed 2026-09-04. Both points settled by Paul the hour they were raised.** Amendments 213 and 214. **Point one: `Not set` stays**, a sixth value meaning nobody has decided, distinct from `Outside scope`. **Point two: no account is assigned a temporary relief category.** The special rates exist so an operator can pick one on a transaction, and the flexibility to assign one to an account is kept for a future that may never come. **So the master changes in exactly one way, the naming convention on `vat_default`**: 69 accounts read `Standard` where they read `20%`, 5 read `Zero-rated`, 166 keep the words they have. **And the table needs no back history**, because Paul is not entering 2021 transactions, so the return-to-20% rows are dropped. **A special rate carries a start and an end date; an ordinary rate needs only a start.** **The transaction dropdown is `Auto`, `20%`, `12.5%`, `5%`, `0%`, `Exempt`, `Outside scope`.** **The names stay in the table** because they show what the flexibility is for. **Nothing is built**, and the work is not yet a step in section 16. |
| 163 | **Closed 2026-09-05. Built by Claude Code the same morning the brief was written.** Amendment 220. Brief `PROMPT_claude_code_2026-09-05_vat_rates_from_the_bundle.md`, report `2026-09-05_REPORT_claude_code_vat_rates.md`, commit `6ca14a3` on `feat/console-phase0`, nine files. **`config.VAT_RATES` and `config.VAT_RATES_IMPLIABLE` are deleted and `worker\vat_rates.py` reads the published `vat_rates.csv` from `config.CHARTS_DIR`**, cached on the file's modification time and returning empty with an ERROR rather than raising. **`impliable_rates()` is the positive rates of the UNDATED rows only, Paul's decision of 2026-09-05, which is `(0.05, 0.2)` and is exactly what the deleted constant held**, so no receipt's treatment changes. A dated row is a temporary relief that applied in a window and cannot be implied by a receipt entered today; recognising 12.5% would have rewritten a receipt of 90.00 with VAT of 10.00 to a net of 80.00 when the true net is 90.00. **Suite 367 passed to 389 passed, 190 subtests throughout, the 22 being the new `tests\test_vat_rates.py`.** Verified independently by the consultant session by running the module against the real bundle and by reading the commit object, not from the report. **The `12.5%` gap this item also named is deliberately not closed by adding the rate**: it is closed by the rates coming from the published table, which is what the item was about. |
| 164 | **Closed 2026-09-05 on a query, not on a command having been run.** Amendment 221. Paul dropped `email_delta` from `C:\Intellibills\db\receipts.db` at 10:54 BST, taking `receipts-backup-2026-09-05-pre-email-delta-drop.db` first, and his output read `before 11`, `email_delta rows 0`, `after 10`. **The consultant session then opened the database read-only at 10:56 and counted ten tables with `email_delta` absent and the six receipts intact.** `schema.py` creates ten and the live database holds ten, so the record and the database agree. **Closed on a second session's read deliberately**, because amendment 205's struck sentence had claimed exactly this drop "verified by query" when no query was made, and a paste is not a query either. **Nothing was added to `worker\database\schema.py`**: it creates and never migrates, and there is no second database holding the orphan. |
| 152 | **Closed 2026-09-05. Decided by Paul, and by neither of the two candidates as written.** Amendment 225, and the work is step 10j of `2026-07-25_CONSOLE_DESIGN.md`, 11 sub-steps. **Layer 5 classifies into a list of receipt accounts that Intellibills owns and ships**, seeded from `COA_MASTER_v2.xlsx` and then frozen, **66 rows**: the master's 95 classifier-eligible accounts less 24 that no supplier receipt evidences and less the five capital additions. **The four-digit master code is the identifier**, so candidate (b)'s benefit is taken without inventing an id scheme, and no mapping is needed inside this practice: 927 rows across the eight library charts hold 0 codes that are not master codes. **The four catch-alls stay in and the five capital additions come out**, both Paul's rulings. **The eight library charts are not shipped with Intellibills.** A fallback column on the master names the account each one collapses into where a chart does not hold it. **Item 33 stays open and no longer blocks this.** |
| 1 | **Closed 2026-08-21. Paul decided: a reserved firm id, `receipt_events_UNATTRIBUTED.ndjson`.** Amendment 128, scheduled at sub-step 10d.19. **The firm-agnostic option had a trap in it:** `config.RECEIPTS_LOG` already names `receipt_events.ndjson` and looks like the answer waiting to be used, but 8.6's intake panel reads `receipt_events_*.ndjson` and that name does not match the glob, so it would have hidden the events from the panel built to show them. That constant is deleted instead, which closed item 72 |
| 2 | **Closed 2026-08-21. Paul decided: its own tab in the centre menu group, beside Client Data, named Client Settings.** Amendment 130, at step 10e. Creating a client keeps the name, code and entity type in the Edit window; C16, the period lock date, stays on Client Data |
| 3 | **Closed 2026-08-21. Paul decided: one file, four sections.** Amendment 133. `2026-08-20_LIST_settings_firm_and_client.md` keeps its firm-identity rows and gains a **System settings** section rather than folding it into the exclusions. **A System settings section in a document is not a System Settings page**: section 10 of `2026-08-18_BOUNDARY_two_products.md` still says there is no third level on any page. Item 4 is the work |
| 4 | **Closed 2026-08-21. Done.** Amendment 138. `2026-08-20_LIST_settings_firm_and_client.md` now has four sections, with **System settings** as `S1` to `S11`, seven engineering constants and four secrets moved out of the exclusions. **Three columns on all 37 live rows:** Multi?, External?, and Kind meaning identity or setting. **F18 struck**, because amendment 135 had already removed its subject, and 18.2b's per-firm settings row corrected with it. Firm settings 18 to 17 |
| 5 | **Closed 2026-08-21 by reading `matchScore()`, which took one command.** Amendment 127. `IntelliBooks-Desktop-v3.html:1272` requires the amounts to agree to within half a penny, tighter than 18.4's penny, so **an app-found match cannot disagree on the amount and 18.5b's stated residual case does not exist.** In its place, the same function allows a match with **no supplier agreement at all**, accepting any date within 21 days with no minimum word overlap, so 18.5b is now justified only on the field Paul's own APCOA example says it will misfire on. Step 10g reworded |
| 6 | **Closed 2026-08-21. Paul decided: the key stays `(message_id, attachment_id)`.** Amendment 129. A `message_id` is unique by design, so the key is already unique across firms and adding `firm_id` would **loosen** it. The one case that would want it is a firm with its own capture address, recorded in section 6 above as a cloud constraint |
| 7 | **Closed 2026-08-21. Becomes sub-step 10f.30 with item 9.** Part D of section 4 of `IntelliBooks\App\Docs\2026-08-20_REPORT_desktop_menu_groups.md` needed a review item built by hand because the pipeline has never produced a possible duplicate. **Sub-step 10f.24 makes a real one possible, so the fake is not needed**, and 10f.28 fixes the pill part D looks at. **Parts A, B and C need no setting up and are not waiting on anything.** Nothing records any part as run: change log item 39 still reads "built and syntax-checked. Not yet run by Paul" |
| 8 | **Closed 2026-08-21 on Paul's instruction, and moved to `CLAUDE.md` under "Before starting the pipeline".** ~~Files uncommitted in git, which is what produces the pipeline's startup warning about the working tree~~ **It is a habit and not a task, so as an item it could never close**: there is always uncommitted work at some moment. `config.check_git_status_on_startup()` at `app.py:1207` reports the tree at that moment and `pipeline_version` is the git short hash at `config.py:153`, so a run on a dirty tree records a version that does not describe the code. **A list of things to close should hold only things that can close** |
| 9 | **Closed 2026-08-21. Becomes sub-step 10f.30 with item 7, on Paul's decision to leave these tests until the Receipts tab changes.** Change log items 19 to 23, five Receipts-tab changes built in July and never tested live. **Items 19, 21 and 23 are exercised by ordinary use; item 20 needs the pipeline deliberately left stale; item 22's tax-year dropdown needs more than one year on disk.** Item 19's mechanism is replaced at 10f.15, so testing it now would prove the interim rather than the design |
| 10 | **Closed 2026-08-21, and it should not have been asked again.** Paul has confirmed repeatedly that **the logon scheduled task is not set until the system goes live**, and that in the meantime he runs the pipeline on demand and closes it. That is the answer, it is now in the file, and no session needs to ask. **The second half was a real finding and is separate:** `IntelliBooks.bat`'s header comment reads "Normal route: logon scheduled task + bookmark", describing a route that does not exist. The file itself behaves correctly, starting the pipeline only if its window is not already open. A false comment, not a false behaviour |
| 11 | **Closed 2026-08-21. The six entries no longer exist, and the client does not survive step 10d either.** Read from disk: `TEST2-books.json` holds **zero transactions and zero receipts**. Amendment 139 retires every test client and every books file, so there is nothing to tidy on either count |
| 12 | **Closed 2026-08-21 by amendment 139.** `TEST` and `TEST2` do not survive step 10d, so the `clientType` set without instruction goes with them. **Confirmed from `IntelliBooks-Practice.json` before closing:** both read `clientType=sole_trader` with `yearEnd` and `mtd` both null, while the other four carry `05/04` and `false`, so both were half-configured. That was also item 79, seen twice |
| 13 | **Closed 2026-08-21. Paul's decision: it does not matter which user account the tasks sit on.** The three are the April HMRC check and the February and August Sage drift backstops, on `pdk7@hotmail.co.uk`. **The argument for moving them was mine and he has overruled it**: a yearly practice task on a personal account is lost if the account lapses, which is a real risk and his to weigh |
| 14 | **Closed 2026-08-21. The answer was already on disk and the stale copy was the one in the Claude project.** `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md` records "The August check ran on 2026-08-17 and reported `no drift`". The run log in the project says the verdict is not established and was last updated two hours earlier the same day, so the later on-disk note governs. Paul confirmed no drift on 2026-08-20. **Nothing to do beyond deciding whether to remove the stale project document** |
| 15 | **Closed 2026-08-21. Paul decided: compare the code, not the name. Scheduled at step 10g by amendment 145.** `IntelliBooks-Desktop-v3.html:2410` builds its guard as `String(v\|\|"").toLowerCase()===c.name.toLowerCase()` and counts transactions, receipts and statement rules with it. **All three hold the four-digit code since the chart adoption, so all three counts are always zero and a category with transactions deletes with no warning.** `c.code` in place of `c.name`, and the comment above the function, "A category has no id: every reference to it anywhere is a copy of this string", is corrected with it. **The code is the id.** An added id would have given a category two identities, which is the same fault as items 19, 21 and 55 in a new place |
| 16 | **Closed 2026-08-21. Paul decided: pass the firm's name.** Amendment 141, sub-step 10d.36. `send_unknown_sender_alert()` at `worker/email/alerts.py:54` takes a `firm_name` the way `send_no_attachment_alert()` at `:11` already does, and the three literals go with it: `support@lastingimpact.co.uk` at `:69`, `Lasting Impact` at `:75` and `:80`. **It is the only automatic email that reaches somebody who is not yet a client**, so it was the first thing an unregistered sender saw and it named the wrong company. Row F7 of the settings list |
| 17 | **Closed 2026-08-21. Paul decided: add a nullable `firm_id`, written and never read. Scheduled at sub-step 10d.39 by amendment 146.** **The unique key stays `(business_type, vendor_code, vendor_name)`**, so the learned pool stays shared and no lookup changes. **The column captures provenance while it is still capturable:** pooling is easy to start and impossible to unwind, because mappings accumulated with no record of who taught them can never be separated. **The original recommendation, adding the firm to the key at 10d.22, was wrong** and Paul's question is what killed it: it only matters if there is a cloud version and only if that version holds several firms in one database, which has never been decided |
| 18 | **Closed 2026-08-21. Paul decided: four values, `email`, `phone`, `desktop`, `other`, and a file with no sidecar goes to Review.** Amendment 147, sub-steps 10d.40 and 10d.11. **Each writer declares its source in the sidecar and `scan_inbox()` reads it** instead of hardcoding `source="capture"` at `worker/intake/folder_reader.py:114`. **The fault was worse than this item said:** `worker/extraction_pipeline.py:220` and `:261` write `"email" if message_id else "folder"` into the sidecar, so one receipt was `capture` in the database and `folder` in the sidecar. **And Paul corrected my count of the routes:** **Add Receipts** is the box you drop or pick a file in, so it is one method and not two |
| 19 | **Closed 2026-08-21. Paul decided: search name and code. Scheduled at step 10g by amendment 143.** `IntelliBooks-Desktop-v3.html:1706` filters on `t.desc`, `t.ref` and `t.category`, and `t.category` holds the four-digit code, so `7300` matches and `Fuel and oil` does not. **10g is the step because it is the only one that touches the Bank Transactions table.** The item originally had this backwards, saying the filter searched the name and could not match a code |
| 20 | **Closed 2026-08-21. Merged into item 55, being the same defect in the same two functions split across two rows.** It read "`renderReports()` and the VAT report are keyed on the category name, at lines 2191 and 2235 as at change log item 38". Item 55 names the consequence, that every transaction falls to the `"expenses"` default and income is reported as expenditure, and now carries both line references |
| 21 | **Closed 2026-08-21. Scheduled at sub-step 10d.38 by amendment 144.** `loadSampleData()` at `IntelliBooks-Desktop-v3.html:2606` onward. **Reading it made this sharper than written:** it writes five category **names** into a field that has held four-digit codes since the chart adoption, being `Motor expenses`, `Sundry expenses`, `Repairs and maintenance`, `Parking and tolls` and `Fuel`, **so all five are wrong rather than two**, every sample receipt lands with a category the chart does not contain and the sample transactions land uncategorised. **At 10d because that is where it starts to matter:** 10d.2 recreates the test clients with empty books and this is the only way to get transactions into a fresh one. The `"(Transfer)"` it also writes is item 56 and stays separate |
| 22 | **Closed 2026-08-21. Already scheduled, and this item said the opposite.** It read "the schema default is scheduled in 10d; the twelve literals are not". **Sub-step 10d.31 schedules both:** "`extractions.currency`: `DEFAULT 'GBP'` removed, and the twelve `"GBP"` literals become one constant, six in `app.py`, four in `worker/resolution/service.py` and two in `worker/extraction/openai_vision.py`". **Counted again 2026-08-21: twelve occurrences on eleven lines, matching those figures.** An item asserting that something is unscheduled, when the sub-step naming it exists, is the same fault as a step that names an amendment and drops its content |
| 23 | **Closed 2026-08-21. Paul decided: delete the field completely.** Amendment 142, scheduled at step 10e. Five places in `IntelliBooks-Desktop-v3.html`, `:348`, `:850`, `:861`, `:884` and the shape comment at `:572`, plus the field in `IntelliBooks-Practice.json`. **All six clients held an empty string, so nothing is lost.** **The reason is accounting:** the box sat beside **VAT registered** and read as though the system knew the scheme, while a client on flat rate generally cannot recover input VAT on purchases and their receipts were being given VAT the standard way. **A field that looks like a setting and behaves like a sticky note is worse than an absent one**, because the absent one prompts the question. It returns with fixed values when Paul asks for it. Row C11 of the settings list is struck |
| 25 | **Closed 2026-08-21. Paul decided: delete it.** Amendment 141, sub-step 10d.37. `EXPORTS_DIR` at `config.py:38` and its `mkdir` at `:95` go, with the two assertions in `tests/test_path_layout.py`. **The folder stays**: `Intellibills\Exports\` holds the categorisations export, the only thing of value the database ever held. **Unlike `RECEIPTS_LOG` at item 72, where the file did not exist either**, here the constant is dead and the folder is not |
| 26 | **Closed 2026-08-21 on Paul's evidence, and it was never an incident.** ~~A stale `Intellibills\pipeline.lock` was removed at startup on 2026-08-20 17:21Z. A run had ended without releasing it and nothing records why~~ **Paul runs the pipeline on demand and closes it immediately, so a lock is left behind every time and cleared by `acquire_lock()` at the next start.** That is his working pattern, not a fault, and it is what "nothing records why" was reaching for. Recorded here because two sessions treated the same behaviour as a defect |
| 27 | **Closed 2026-08-21. Paul settled it: the practice root is not a setting, the client top folder is, and they are different things.** Amendment 151, sub-steps 10e.9 to 10e.15. **The pipeline's configuration is the single authority for the practice root**, because it cannot read a file inside the root to learn where the root is. **IntelliBooks verifies instead of holding a second copy:** `pipeline-status.json` gains the practice root path, and a successful read of it through the folder grant proves the two are in the same tree. **The folder gate checks before it accepts**, so picking a wrong folder no longer creates `IntelliBooks\` and writes a blank practice file. **The Change practice root folder button is deleted**: it was labelled for the practice root and intended for the client top folder, which is F17 on Firm Settings, two fields and an absolute path that need not sit under the practice root |
| 28 | **Closed 2026-08-21. Paul settled ownership.** Amendment 152, sub-steps 10d.44 to 10d.50. **It was three settings, not two:** `vat` reaches the phone the same way at `index.html:200`. **Confirm mode becomes the client's alone and off by default. The PHV platforms become the firm's alone, read-only on the phone.** **And the real fault was not the count:** the setup link can turn a setting on and never off, because it omits a parameter when the value is falsy and the phone keeps its own value when a parameter is absent. The link now carries every firm-owned setting always, and **the upload response carries them too**, which is what makes a firm-owned setting reliable rather than a one-time push |
| 29 | **Closed 2026-08-21 with item 28.** Amendment 152, sub-step 10d.49. The statement week ending day becomes the firm's alone, read-only on the phone, so it stops existing nowhere but the client's browser. **Paul's reason covers both this and the platforms and is not about correctness:** "We cannot rely on clients to get them right and if we did clients would resent it" |
| 30 | **Closed 2026-08-21. The premise was wrong: the consultant session has owned this since 2026-08-17.** Section 1 of `IntelliCharts\archive\PROMPT_consultant_2026-08-17_coa_handover.md` reads "That arrangement ends now. This workstream is yours", and names the chart, the prompts and the verification. **That was three days before this item was written, by the session that owned it**, because the brief sits in `IntelliCharts\` and had not been read. **Settled with it, amendment 154:** IntelliCharts has its own design document, being the master chart note, which now carries an amendment record; the console design document keeps only the consumer side; and the spent-files rule moved six executed prompts into `IntelliCharts\archive\`. **What remains of the 2026-08-17 obligations is four things and Paul has parked three:** the PHV industry chart, the Sage Accounting control-account contradiction, action 4c's proof which is among item 86's outstanding checks, and the `1141` non-breaking space |
| 32 | **Closed 2026-08-21. Paul decided: spent files move into one `archive\` folder, tracked, and the same for future spent files.** Amendment 153, scheduled first at step 10h, with the standing rule in `CLAUDE.md`. ~~58 of the 74 markdown files move, 16 stay.~~ ~~Struck 2026-09-01 by amendment 163. The live figure is step 10h's and not this row's: 63 of 80 move and 17 stay.~~ **That replacement figure was itself wrong, being the first of two corrections written the same afternoon, and it is struck in turn 2026-09-01 by amendment 165.** **Step 10h no longer states a total at all.** It names the 17 files that stay and every other markdown file in the root moves, so there is no figure in this row to keep current and none to look for. **Struck rather than updated because a closed row is a dated record of what was decided, and this figure has now been overtaken three times, but a number sitting unmarked inside a closed row reads as current to the next session that finds it.** **Both handovers were wrong on the count:** 57 and 61 against an actual 76 markdown and CSV, and neither figure had been produced by a command. **The composition was the point rather than the total**, being 39 prompts, 12 handovers and 9 reports. **Counting it properly moved the number twice:** the current handover is spent only once its successor exists, and `PROMPT_intellibooks_desktop_changes.md` is a live standing brief that `CLAUDE.md` records the Desktop session working from. **I had that one in the move list**, and the only reason a live brief was not archived is that this item said it must not be touched |
| 39 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** One capture mailbox. `capture@lastingimpact.co.uk`, redirected to `bills@intellitax.co.uk`, one IMAP account. A real product gives each firm its own capture address, which the 22 July analysis named as an option and never examined |
| 40 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** One OneDrive. `ONEDRIVE_USER` on Netlify is a single value |
| 41 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** One Netlify deployment, one Azure app registration, one `AZ_TENANT_ID` |
| 42 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** One browser folder grant, so one firm is open at a time |
| 43 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** One practice root and **no firm level in any path**, so two firms with a client of the same name collide in `Clients\` |
| 44 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** `email_delta` holds one `delta_link` and one `last_uid` as global singletons. **Rests on the undecided question of whether a cloud firm gets its own database, which is section 2 of that document.** |
| 45 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** `email_alerts` carries `firm_name`, a copied string, and no `firm_id`. **Rests on the undecided question of whether a cloud firm gets its own database, which is section 2 of that document.** |
| 46 | **Closed 2026-09-01. Moved to section 3 of `2026-09-01_DESIGN_cloud_multi_firm.md` in this repository root, created the same day on Paul's decision, and section 6 of this file emptied.** `statements` carries `client_id` and no `firm_id`, unlike `receipts`. **Rests on the undecided question of whether a cloud firm gets its own database, which is section 2 of that document.** |
| 47 | **Closed 2026-08-21 with item 17.** ~~Item 17 above, `categorisations_firm_vendors`, the only one of these that is a leak rather than a limitation~~ ~~one of two~~ **Both leaks it named are now dealt with: item 17 at sub-step 10d.39 and item 143 at step 10f.** This row existed so nobody scheduled item 17 locally, and it has been scheduled locally on purpose, so the row has no subject left. **Items 44, 45 and 46 remain in this section and are the same shape**, all resting on the undecided question of whether a cloud firm gets its own database |
| 53 | **The OpenAI API key beginning `sk-2adW`.** Revoked, confirmed by Paul 2026-08-21. **The `.env` query was included in error:** line 10 was already deleted on 2026-07-31 |
| 55 | **Closed 2026-08-21. Scheduled at step 10g by amendment 144.** `catType[c.name]=c.type` at line 2171 of `IntelliBooks-Desktop-v3.html` read with `\|\|"expenses"` at `:2175`, so with categories keyed on code every transaction falls to the default and **income is reported as expenditure.** Lines 2191 and 2235 carry the same keying, and item 20 was merged into this one. **At 10g because one of the two is the VAT report and 10g is the VAT work**, so leaving it would mean rebuilding VAT reporting on top of the fault |
| 65 | **Closed 2026-09-02. Fixed on Paul's instruction, change log item 51 of `IntelliBooks\App\Docs\IntelliBooks-Change-Log.md`.** `fileReviewReceipt()`'s success toast printed the raw client name where the folder was built with `safeName()`. **It and the four other messages of the same class now build their path with one function, `clientFolderPath()`, beside `safeName()` in `IntelliBooks-Desktop-v3.html`.** `filed_path` uses the same function, so the string the pipeline reads back at `worker/resolution/service.py:351` and the toast the operator reads cannot drift apart. **Latent rather than live when it closed:** `safeName()` strips `\/:*?"<>|` and trims, and no client on disk has such a name, `She Run's It! Ldn Ltd` being the closest and unaffected. Item 132 closed with it; half of item 84 was settled by it |
| 72 | **Closed 2026-08-21 with item 1, and it is deleted rather than fixed.** Amendment 128. `config.RECEIPTS_LOG` at `config.py:52` goes, and the assertion of its value at `tests/test_path_layout.py:83` goes with it, at sub-step 10d.19. **It nearly became the answer to item 1**, which is the argument for deleting a dead constant rather than leaving it to be found and reused |
| 79 | **Closed 2026-08-21 by amendment 139.** No client survives step 10d, so there are no registry settings to carry across. **It was also item 12, and reading `IntelliBooks-Practice.json` confirmed the shape of it:** `yearEnd`, `mtd` and `mtdBasis` are set on four of the six clients and null on `TEST` and `TEST2` |
| 98 | **Closed 2026-08-21 by amendment 139, by removing the migration rather than by settling the discrepancy.** Nothing is carried across, so it no longer matters that amendment 111 named `PKPH-books.json`, which does not exist, and omitted `PSHIPN-books.json`, which does. **`PSHIPN-books.json` is retired with the other six**, and reading it first showed why that costs nothing: zero transactions, zero receipts, and 115 categories written by `chartFor()` |
| 99 | **Closed 2026-08-21 by amendment 139.** `PSHIPN` is retired, so check 1 of `PAUL_CHECKS_2026-07-30.md` has no subject again. **The check has still never been run**, and it has now had two subjects it outlived, `PKPH` and `PSHIPN`, which is worth knowing if anyone plans to rely on it |
| 102 | **Closed 2026-08-21. Merged into item 24, being the same field.** It read "`firms.csv`'s `email` column holds `bills@intellitax.co.uk`, last written 2026-07-22 16:32:19 UTC. The decided capture mailbox is `capture@lastingimpact.co.uk`." **That value is now recorded in item 24's row in section 11, as state rather than as a fault.** Nothing reads the column, so nothing is wrong; the value is noted so that anything which later starts reading it knows it has not been maintained |
| 104 | **Closed 2026-08-21 with item 26, and the same answer covers both.** The lock was still on disk today: `Intellibills\pipeline.lock`, 56 bytes, contents `pid=41528` and `started_at=2026-08-20T16:21:48.877200+00:00`, read whole, with `Intellibills\pipeline-status.json` unmoved since `2026-08-20T16:36:57Z`. **So neither of the two explanations the item offered was right.** It was not a run holding the lock for twenty hours and it was not a pattern of failures: Paul starts the pipeline on demand and closes it immediately, so the lock outlives every session by design of how he works. **The item's own framing is the thing to learn from.** It said "two in two days is a pattern rather than an incident" and was one step short: it is a pattern, and the pattern is the operator, which no amount of reading the code would have shown |
| 106 | **Closed 2026-09-01 under Paul's rule of 2026-08-21, amendment 140: a list of things to close should contain only things that can close.** ~~49 of the 69 markdown files in `C:\LastingImpact\receipt_capture\` have never been opened.~~ **An unread count is a fact about what one session knows, not about the system, and the set grows: 69 markdown files when the item was raised, 79 enumerated on 2026-09-01.** So it could never close on its own terms. **Its substance was already in `CLAUDE.md` under "Spent files leave the root", which cites this item by name, so nothing had to be written there.** The reasoning is in section 9 above |
| 108 | **Closed 2026-09-01 as done.** ~~Also unread in `IntelliBooks\App\Docs\`: `build-brief-template.md`, `HANDOFF-Status.md`, `HANDOVER-Prompt-for-Teams.md`, `Session-Record.md`, `SETUP-v5.md` and `Profile Instructions for Claude.txt`, plus the `Claude CoWork Sessions\` sub-folder.~~ **All seven were read on 2026-08-21, which section 10 records by name.** **The folder was listed again on 2026-09-01 and every one of the seven is present carrying a mid-July mtime**, so the set did not change after the item was raised and the reading covers it. **The two stale documents the reading found, `SETUP-v5.md` and `HANDOFF-Status.md`, are items 124 and 125 and stay open** |
| 109 | **Closed 2026-09-01 under the same rule as item 106, amendment 140.** ~~Eleven of the twenty modules under `worker\` have never been read whole, and `app.py` has been searched and never read.~~ **All of it was read on 2026-08-21 and section 10 is what that reading produced, but the item could never close, because every new session starts having read none of it.** **So it became a sentence in `CLAUDE.md` under "The standard of evidence" on 2026-09-01** rather than a permanent row here. The reasoning is in section 9 above |
| 107 | ~~**`IntelliBooks-Change-Log-Original-Items-1-11.md` holds change log items 1 to 11 and has not been read.** The change log proper begins at item 12. **Any item among the first eleven that was left open is on no list**~~ **Wrong, and it was my error. Withdrawn 2026-08-21.** `IntelliBooks-Change-Log.md` begins at `## Item 1`, not at item 12, and its items 1 to 11 match the original file's one for one by title, as do items 12 to 15. **The merge that file's own header asks for was done.** I asserted the item from that file's stale header rather than from the current change log's headings, which is reasoning from a summary about the very file that would have settled it, and one search for `^## Item` settled it. **The real finding is item 124 below: the merge kept the items and dropped their riders** |
| 110 | ~~**`docs\specs\` holds `categorisation_engine.py`, 25,621 bytes, dated early May 2026, sitting inside a documentation folder.** Whether it is a superseded spec artefact or code that could be imported by mistake has not been established~~ **Established 2026-08-21 and closed: it is a superseded artefact and it is safe.** Nothing in the tree imports it, by a search for `docs.specs.categorisation_engine`, `from categorisation_engine` and `import categorisation_engine` across every `.py` and `.md`. Neither `docs\` nor `docs\specs\` holds an `__init__.py`, so it is not importable from where it sits, and the only `sys.path.insert` in the tree is `setup_auth.py:18` inserting `"."`. **And it could not be substituted by accident:** its `CategorisationEngine.categorise()` takes a bank-feed-shaped `Transaction` and its constructor takes `data_dir`, where the live one at `worker\categorisation\engine.py` takes `receipt_id`, `extraction_id`, `supplier_name`, `client_id`, `business_type` and a `repo`, so a wrong import raises `TypeError` on the first call rather than producing plausible output. **Its GL codes are four-digit, not three**, which a careless pass would misread. Item 54 still stands: it is where the only outside references to `learn_from_correction()` live |
| 129 | ~~**`docs\console-design` was twice called a safety net that "must not be deleted", and it is gone.**~~ **Withdrawn 2026-08-21, and it was my error. `docs/console-design` is a git branch, not a folder, and it exists.** Read off the device: `.git\refs\heads` holds `main`, `docs/console-design`, `feat/console-phase0`, `fix/date-disambiguation-vat-swap` and `fix/imap-message-id-dedup`, with `HEAD` on `feat/console-phase0` and no tags. **I searched for a folder under `docs\`, found only `specs\`, and reported an absence** |
| 132 | **Closed 2026-09-02 with item 65, change log item 51.** It recorded where the instances were, which is what it existed to do. **There were five, not three**, enumerated from the saved file on 2026-09-02: the legacy books migration toast, the handover pack confirm and its toast, the filed receipt toast and the HMRC summary saved toast. All five now build their path with `clientFolderPath()`. **The line numbers this item carried, 1431 and 1475, were already stale and are kept as written, per amendment 82** |
| 136 | **Closed. The `Clients\` fossils were deleted.** `HANDOVER-Prompt-for-Teams.md` instructed "delete the two now-empty `Receipt Inbox\{CODE}\Review` folders and the legacy books.json fossils at `Clients\Test\IntelliBooks\` and `Clients\Test 2\IntelliBooks\`". Listing `Clients\` recursively on 2026-08-21 returns six client folders and **no `IntelliBooks` sub-folder under any of them** |
| 137 | **Closed, and it explains part of item 103.** The 2026-07-17 snapshot of `HANDOFF-Status.md` asks "Paul to add Test client row to clients.csv (`Client_004,Test,,FIRM001,UNSPECIFIED,TEST,`)". `clients.csv` today has no `Client_004` and no `TEST` code. Amendment 49 records that `Client_004` had been given to both `Test` and `She Run's It! Ldn Ltd`, and that `SHERUNSIT` was renumbered to `Client_005` on 2026-07-28. **So `Client_004` was `TEST`, was a duplicate, and was removed**, which accounts for one of the four gaps in the `Client_NNN` sequence. `Client_001` to `Client_003` are still unaccounted for, and `Client_003` is the lost vendor mapping of item 76 |
| 139 | **Closed 2026-08-21. Paul ruled: the copy into `Clients\` is Intellibills' function and `get_client_directory()` stays.** Recorded as amendment 122 of `2026-07-25_CONSOLE_DESIGN.md`, which strikes 18.2b's opening sentence and amendment 73's, unblocks sub-step 10d.14, and schedules the removal of the on-arrival write at step 10f. **What this item got wrong is worth keeping.** It reported that amendment 113 governed "on the record's own logic" but that "nothing says so". Two things did, and neither was cited: **amendment 106 of 2026-08-18**, which already amends 18.9 to read "the copy is Intellibills' own function, with two triggers set per firm", and **section 6 of `2026-08-18_BOUNDARY_two_products.md`**, which works the case through and whose section 5 says that document is above the design document. So this was never a live contradiction needing a ruling; it was a stale sentence in 18.2b that no amendment had struck. **A contradiction between two documents is not established until the set of documents that speak to it has been enumerated, and the higher authority was left out of the set** |
| 140 | **Closed 2026-08-21. Paul decided: reject a future two-digit year.** Amendment 149, sub-step 10d.41. `2000 + c` stays, and where that year is later than the current year the date is unreadable and `_parse_numeric_date()` returns `None`. **No century pivot**, because a cutoff tight enough to turn 99 into 1999 turns 28 into 1928. `elif c < 1000` is deleted: identical body to the branch above it, and a three-digit year is a misread. **The missing note had a mechanical cause:** `_parse_numeric_date()` returns a date or `None` and has no note channel, so `resolve_invoice_date()` records it. **Findings 3 and 4 of 10.2, left by step 6b on 2026-07-27** |
| 141 | **Closed 2026-08-21. Paul decided: assume the figure is the gross, verify the implied rate, flag if it does not verify.** Amendment 150, sub-step 10d.42. **Renamed so nothing in it reads as a VAT feature**, because `apply_vat_inclusive_swap()` and `rate_tol` are both about establishing which figure is the gross and the VAT figure is only the evidence. **Not gated on the client's VAT registration:** a non-registered client's expense is the gross, so a figure taken as net overstates their profit and loss by the VAT. **Three framings were corrected by Paul on the way, and the last removed most of the design:** two numbers on a receipt are always gross and VAT, so there was never a choice between two stories, only one story and a check on it. **Finding 5 of 10.2, the last of the three step 6b left** |
| 142 | **Closed 2026-08-21. Paul decided: the pipeline re-reads the registry on modification time, and 8.6's marker file is struck and not built.** Amendment 132, scheduled as sub-step 10d.35. **The mechanism kept resurfacing because 8.6's version could not be built**: it needs the console to write the marker and the console is steps 11 to 22 away. **And it would have been wrong as well as unbuildable**, because after 10d the registry has more than one writer and a marker only works if every writer remembers to touch it. **Only the pipeline caches the registry**; IntelliBooks reads it when a client is opened and the capture app reads it over Graph per upload, so this was never an architecture question |
| 143 | **Closed 2026-08-21. Paul decided: the hash lookup takes the client, and `app.py:724` gets the guard the other two call sites have.** Amendment 136, at step 10f. **Scoping removes the case rather than deprioritising it**: two clients who send the same file stop being a duplicate of each other, so there is nothing to detect and nothing to report. **The risk was concentrated in PDFs**, because a PDF is produced by software and the same PDF is the same bytes, whereas two photographs of one receipt never match. Live route: a shared insurance schedule, MOT or service invoice between two drivers on one car. **Two things I had wrong and Paul corrected**: I described it as cross-firm, which is theoretical, when the live case is cross-client; and I called the shared-PDF outcome a bookkeeping error when the operator sees both transactions at posting and decides |
| 144 | **Closed 2026-08-21. Paul decided: nothing gets deleted.** Amendment 137, sub-step 10f.21. `_remove_inbox_pair()`'s two callers at `app.py:862` and `app.py:909` become moves to `Processed\`, matching every other outcome. **Step 9c had changed the `ok` path from delete to move on the no-data-loss rule and left the two duplicate paths deleting**, so the rule held everywhere except where a duplicate arrived. Raised and closed the same day, on the practical ground that Paul is about to send a lot of test receipts and an identical resend through the phone or **Add Receipts** was removing the file from disk with one log line and no receipt row |
| 147 | **Closed 2026-08-31. Built the same day in `IntelliBooks-Desktop-v3.html`.** ~~Nothing prevents the SA103F export running while a control account carries a balance.~~ **`exportHMRC()` now refuses to run, and writes no file, while `9996 Ask the Accountant`, `9997 Ask the Client`, `9998 Reclassifications` or `9999 Suspense account` carries a balance**, and the toast names each offending account and its figure. **The balance is taken up to the period end and not within the period**, because the requirement is that the account is nil at the period end, not that it moved by nil. The codes are in a named constant `CONTROL_ACCOUNTS`. **Its last sentence was out of date and is corrected here:** `COA_MASTER_v1.csv` no longer exists, and all four control accounts are in `COA_MASTER_v2.xlsx`. **This was also step 6 of section 9 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md`**, which carried a second copy of the same requirement. Verified by running `exportHMRC()` in node against books with a balance on `9999`: refused, no file written |
| 146 | **Closed 2026-08-31. Built the same day in `IntelliBooks-Desktop-v3.html`.** ~~Nothing reconciles the exported SA103F box totals to the books' profit, so a figure that maps to no box is dropped silently.~~ **The export now carries a Reconciliation block: net profit from the boxes, net profit from the books, and the difference, marked Agrees or DOES NOT AGREE**, with a toast when it does not. **Why the comparison is exact rather than approximate:** the direction of a box comes from the box and not from the account type, so the net profit built from the boxes is the plain total of every categorised profit and loss transaction that reached a box. Comparing it with the books' own total of the same transactions makes an unmapped, mis-boxed or filtered-out figure appear as a difference. **General, not specific to the four control accounts, which is why it is not a substitute for item 147 and item 147 is not a substitute for it.** **A correction to this item's own text:** its description of `exportHMRC()` came from section 4 of the IntelliCharts note rather than from the file, and the file has now been read. **This was also step 7 of section 9 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md`.** Verified by running `exportHMRC()` in node: an expense of 40.00 with no box gave a difference of 40.00 and DOES NOT AGREE; giving that account box 30 gave a difference of 0.00 and Agrees. **One thing this does not do, flagged and not built: nothing compares box 94 Net business assets with box 99 Balance at end of period**, which is the same class of check on the balance sheet half |
| 148 | **Closed 2026-09-01. Built the same day in `IntelliBooks-Desktop-v3.html`, and widened on Paul's instruction from one comparison into four checks.** ~~Nothing compares SA103F box 94 Net business assets with box 99 Balance at end of period.~~ **The balance sheet block is now produced only if four checks pass, and a failure suppresses the whole block and prints why**, because a wrong balance sheet is worse than none: the reader cannot tell which of the seventeen figures is wrong. **One, the chart carries no account with a box in 83 to 99**, so there is nothing to total. **Two, no opening position:** box 95 is nil and no balance sheet transaction is dated before the period start, so every box is a part-year movement rather than a balance. **This is Paul's condition, that a balance sheet cannot be produced where opening balances have not been entered.** **Three, a balance sheet account carrying a figure has no box**, so the totals are short by it. **Four, box 94 does not equal box 99**, which is the original item. When all four pass the block ends with a line reading `Check, Box 94 equals box 99, Agrees`, so a reader can see the check ran. **The tickbox says the client is supposed to have a balance sheet; these say whether one can honestly be produced from what is in the books.** Verified by running `exportHMRC()` in node against five sets of books, one per failure and one clean |
