# Build status — Receipt Capture

Split out of `2026-07-25_CONSOLE_DESIGN.md`'s sections 16 and 17, 2026-09-14, so the file read day to
day is not the same file as the full specification and its amendment record. Nothing is renumbered:
step names, section numbers and amendment references are exactly as they were in the parent document.
Where a row below cites an amendment number, the amendment itself is recorded in
`2026-07-25_CONSOLE_DESIGN.md`'s amendment record, not here.

`2026-07-25_CONSOLE_DESIGN.md` remains the build authority for everything else: the specification
body, sections 1 to 15, 18 and 19, and the full amendment record.

---

## 16. Implementation order

Commit after each step.

**Where we are, as at 2026-09-14, later once more. 49 built, 27 outstanding, 3 cancelled, 1 moved out of this order, 80 steps. Corrected by amendment 457, which built step 10aq. Counted from the table's own 80 rows, not carried from the line above. The twenty-seven outstanding are 10r, 10t, 10v, 10w, 10x, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10at, 10au, 10aw, 10az, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.** Superseded corrections to this line, and the sub-step decomposition notes that used to sit beside them, moved to Appendix 16A at the end of this section, 2026-09-14, so this line stops growing without bound. Nothing deleted, only relocated, in the original order.
**Kept up to date in the same edit as the step below it.** Every step and every sub-step carries one of those four words at its head. **Nothing is inferred from strikethrough**, which in this section means built, superseded wording, suspended or cancelled depending on where it appears.

| Step | What it is | Status |
|---|---|---|
| 0 | Discards, drafts, branch. The merge is cancelled | **BUILT** |
| 1 | Auto-retry loop fix | **BUILT** |
| 2 | `save_extraction(update_status=)`, `BaseExtractor.name` | **BUILT** |
| 3 | `parse_corrections`, zero and coercion fixes | **BUILT** |
| 4 | Review-pair cleanup, counts from the database | **BUILT** |
| 5 | Sidecar `category_code` and `category_name` | **BUILT** |
| 6 | Move post-processing to `postprocess.py` | **BUILT** |
| 6b | Findings 1, 2, 6 and 7 of 10.2's seven | **BUILT** |
| 6c | Write `extractions.details` | **BUILT** |
| 7 | Extraction factory and `extractor.name` | **BUILT** |
| 7b | `pipeline_version` at every write | **BUILT** |
| 8 | `worker/resolution/service.py` | **BUILT** |
| 8b | Three service corrections | **BUILT** |
| 9 | Thin CLIs, one log file per entry point | **BUILT** |
| 9b | `confirm_duplicated_action()`, `receipts.filed_at` | **BUILT** |
| 9c | Folder-intake original out of the inbox | **BUILT** |
| 10 | Back-feed consumer, `apply_resolution_note()` | **BUILT** |
| 10a | Config constants for the folder names, then the `IntelliBooks` parent folder, 3 sub-steps | **BUILT** |
| 10b | The reconciliation check, 13A | **MOVED** |
| 10c | The clean-slate reset, through 17.5a | **BUILT** |
| 10d | One client registry, the phone app credential and its settings model, 68 sub-steps, 67 built, 10d.47 cancelled | **BUILT** |
| 10e | The two settings pages and the two folders, 15 sub-steps, 14 built and 10e.6 cancelled | **BUILT** ~~OUTSTANDING~~ |
| 10f | Publish step, client folder copy, duplicates, ~~30~~ ~~31~~ ~~33~~ ~~34~~ ~~35~~ **36** sub-steps, ~~six of them split into a brief of their own by amendment 267~~ **seven built 2026-09-07 by amendment 268; 10f.32 and 10f.33 added 2026-09-08 by amendment 269 and 10f.34 by amendment 271; 10f.32 and 10f.33 built 2026-09-08 by amendments 272 and 273; 10f.35 added by amendment 274 and built by amendment 275, both 2026-09-08; 10f.36 added 2026-09-08 by amendment 278. ~~Ten built, 26 outstanding~~ ~~Fifteen built, 21 outstanding, corrected 2026-09-09 by amendment 287 which built 10f.4 to 10f.7 and 10f.36.~~ ~~Eighteen built, 18 outstanding, corrected 2026-09-09 by amendment 289 which built 10f.8, 10f.9 and 10f.10.~~ ~~Nineteen built, 17 outstanding, corrected 2026-09-09 by amendment 291 which built 10f.29.~~ ~~Twenty-three built, 13 outstanding, corrected 2026-09-09 by amendment 297 which built 10f.11, 10f.12, 10f.13 and 10f.16.~~ ~~Twenty-five built, 11 outstanding, corrected 2026-09-09 by amendment 299 which built 10f.14 and 10f.15.~~ ~~Twenty-six built, 10 outstanding, corrected 2026-09-09 by amendment 301 which closed 10f.17.~~ ~~Twenty-eight built, 8 outstanding, corrected 2026-09-09 by amendment 302 which built 10f.28 and 10f.34.~~ ~~Twenty-nine built, 7 outstanding, corrected 2026-09-09 by amendment 303 which closed 10f.26.~~ ~~Thirty-one built, 5 outstanding, corrected 2026-09-09 by amendment 305 which built 10f.24 and 10f.25. Enumerated from this section's own sub-step lines and not carried: 31 plus 5 equals 36, contiguous 1 to 36, no duplicates.~~ ~~Thirty-three built, 3 outstanding, corrected 2026-09-10 by amendment 313, which built 10f.30 and caught this count still reading thirty-one after amendment 311 built 10f.2. Enumerated from this section's own sub-step lines and not carried: 33 plus 3 equals 36, contiguous 1 to 36, no duplicates. The three are 10f.1, 10f.3 and 10f.27.~~ ~~Thirty-three built, 1 outstanding, 2 moved, corrected 2026-09-10 by amendment 314, which moved 10f.1 and 10f.3 out to items 177 and 178. Enumerated from this section's own sub-step lines and not carried: 33 plus 1 plus 2 equals 36, contiguous 1 to 36, no duplicates. The one outstanding is 10f.27 and it is in progress.~~ ~~Thirty-three built, 3 outstanding, 2 moved, 38 sub-steps, corrected 2026-09-10 by amendment 315, which added 10f.37 and 10f.38. Enumerated from this section's own sub-step lines and not carried: 33 plus 3 plus 2 equals 38, contiguous 1 to 38, no duplicates. The three outstanding are 10f.27, in progress, and 10f.37 and 10f.38, new.~~ ~~Thirty-four built, 2 outstanding, 2 moved, 38 sub-steps, corrected 2026-09-11 by amendment 317, which built 10f.37. Enumerated from this section's own sub-step lines and not carried: 34 plus 2 plus 2 equals 38, contiguous 1 to 38, no duplicates. The two outstanding are 10f.27, in progress, and 10f.38.~~ ~~Thirty-five built, 1 outstanding, 2 moved, 38 sub-steps, corrected 2026-09-11 by amendment 318, which closed 10f.27 on a live test. Enumerated from this section's own sub-step lines and not carried: 35 plus 1 plus 2 equals 38, contiguous 1 to 38, no duplicates. The one outstanding is 10f.38.~~ **THIRTY-SIX BUILT, NONE OUTSTANDING, 2 MOVED, 38 SUB-STEPS. STEP 10f IS COMPLETE, 2026-09-11, amendment 322, which closed 10f.38 on a live test. Enumerated from this section's own sub-step lines and not carried: 36 plus 0 plus 2 equals 38, contiguous 1 to 38, no duplicates.** | **BUILT** |
| 10g | The VAT work in 18.4 and 18.5, plus the two category controls, 10 sub-steps. ~~Six built 2026-09-04; 10g.8 cancelled 2026-09-11 by amendment 324 with 18.5b. Enumerated from this section's own sub-step lines and not carried: 6 built plus 3 outstanding plus 1 cancelled equals 10, contiguous 1 to 10, no duplicates. The three outstanding are 10g.3, 10g.4 and 10g.7.~~ ~~Eight built, 1 outstanding, 1 cancelled, corrected 2026-09-11 by amendment 325, which built 10g.3 and 10g.7. Enumerated from this section's own sub-step lines and not carried: 8 plus 1 plus 1 equals 10, contiguous 1 to 10, no duplicates. The one outstanding is 10g.4, the split transaction.~~ **NINE BUILT, NONE OUTSTANDING, 1 CANCELLED. STEP 10g IS COMPLETE, 2026-09-11, amendment 326, which built 10g.4. Enumerated from this section's own sub-step lines and not carried: 9 plus 0 plus 1 equals 10, contiguous 1 to 10, no duplicates.** | **BUILT** |
| 10h | Eight documents reconciled with the code | **BUILT** |
| 10j | The receipt accounts list, layer 5's context, and the fallback column, 11 sub-steps, all built | **BUILT** ~~OUTSTANDING~~ ~~10 built, 10j.11 outstanding~~ |
| 10k | An edit to an already-filed receipt writes a `corrected` note | **BUILT 2026-09-11, both halves. Amendment 328.** |
| 10l | A layer 5 categorisation holds the receipt instead of filing it | **BUILT AND CLOSED 2026-09-12, BOTH HALVES, amendments 336 and 342. The live check on screen PASSED.** ~~The live check on screen is outstanding.~~ ~~OUTSTANDING.~~ **Its open design question decided 2026-09-11, amendment 330, and the trigger amended 2026-09-12 by amendment 333.** **Split by amendment 331. The pipeline half is BUILT, ~~commit `b439b92`, and needs the one-line change amendment 333 decides~~ **commits `b439b92` and `e27aecc`, the second being amendment 333's one-line change, which is IN `worker\publish.py` and was read there on 2026-09-12. Struck the same day.** The Desktop half is written from the 10l report and is not started.** **The confirmation amendment 330 required is settled 2026-09-12 by amendments 334 and 335: the act is pressing `File Receipt` in the Complete Review Receipt window, and NOTHING IS STORED, because the pill is drawn on review rows only and a confirmed receipt is not one.** |
| 10m | The firm vendor table, written only where a correction confirms the classifier | **BUILT 2026-09-12, amendment 344.** Claude Code, commits `ecc84dc` and `5d44937`. ~~OUTSTANDING~~ **No row can appear until the classifier is on, which is step 10p.** |
| 10n | **A capture report, on demand, for one client, by tax year or by a range of dates.** Paul's decisions of 2026-09-11, amendments 319 and 327. **Everything captured for that client and what became of each one**, not only what reached the books. **The pipeline writes it**, because only the pipeline's own database holds the failures, the duplicates and the discards. ~~IntelliBooks Desktop writes it and Paul sends it.~~ **Struck by amendment 327.** Nothing new writes into `Clients\`. | **BUILT 2026-09-11, amendment 332.** |
| 10p | The classifier's inputs and its switch: item lines stored on the extraction with description and amount separate, a deterministic call, and a per-firm on and off | ~~**OUTSTANDING. Added 2026-09-12 by amendment 340.** The pipeline half is Claude Code's; the Firm Settings half is the consultant session's.~~ ~~**OUTSTANDING, NARROWED. Pipeline half BUILT 2026-09-12, verified against the code, amendment 352.** Claude Code, commits `a45a8ce` and `4c90130`. **What is left is the Firm Settings screen, the consultant session's.** The setting it must write is `classifier_enabled` on the firm record, JSON boolean, absent means off; until the screen exists, setting it is a hand edit of `Intellibills\firms.json`, the same as other settings before their screen.~~ **BUILT AND CLOSED, BOTH HALVES, 2026-09-12, amendment 353.** The Firm Settings half is a two-value select, Off or On, on the Intellibills Settings card. **10o is skipped deliberately: it reads as 10 followed by a nought.** |
| 10q | The capitalisation threshold: a firm default, £50, with a per-client override, and a client Accounting method dropdown that greys the override out under Cash Basis | ~~**OUTSTANDING. Added 2026-09-12 by amendment 349, Paul's decision on outstanding item 33.** `2026-08-20_LIST_settings_firm_and_client.md` F19, C21 and C22. A Desktop settings feature, not started.~~ ~~**OUTSTANDING, NARROWED. Consultant session, 2026-09-12, amendment 354.** C21 and C22 are BUILT, on the Client Settings tab. **F19 is not built and is not started: it needs a firm-level IntelliBooks storage decision that has not been made, put to Paul rather than guessed at.** Until then a blank client override reads as a fixed £50.~~ **BUILT AND CLOSED, ALL THREE SETTINGS, 2026-09-12, amendment 355.** F19 stores on `Intellibills\firms.json`, Paul's decision, entered on the Firm Settings tab's IntelliBooks Settings card. |
| 10r | Exports leave the repository for `Intellibills\Exports\`, one location instead of two | **OUTSTANDING. Added 2026-09-12 by amendment 359, Paul's decision closing outstanding item 37.** |
| 10s | 18.5's Attach rule: `attachReceipt()` carries nothing from the receipt to the transaction | **BUILT 2026-09-12, amendment 360.** Consultant session. **Its scope sat inside step 10g, which closed on 2026-09-11 without it.** |
| 10t | The reports set: what IntelliBooks reports, in what form, and which are kept as issued | **OUTSTANDING. Added 2026-09-13 by amendment 369, Paul's direction.** |
| 10u | `chartFor()` refuses a duplicate partner code; partnerships capped at ten | **BUILT, 2026-09-14, in `chartFor()` and `saveClientModal()`, sharing a new `chartDuplicateCodes()` with the Import chart control. See amendment 455.** |
| 10v | `COA_MASTER_v2.xlsx` Master sheet gains six dropdowns and extends one | **OUTSTANDING. Added 2026-09-13 by amendment 372, Paul's decision closing outstanding item 162. Paul's own hand-edit.** |
| 10w | `engine.py:527` renames `vendor_key` to `vendor_code` at three call sites | **OUTSTANDING. Added 2026-09-13 by amendment 373, Paul's decision closing outstanding item 54.** |
| 10x | `t.category` no longer carries the literal `"(Transfer)"` | **OUTSTANDING. Added 2026-09-13 by amendment 374, Paul's decision closing outstanding item 56. Mechanism not yet settled.** |
| 10y | A malformed sidecar keeps its image; a loose PDF with no sidecar is not silently dropped | **BUILT, 2026-09-14, in `ingestReceiptFiles()`, matching amendment 375 exactly. NOT AN OUTSTANDING ITEM AND NOT TO BE RAISED AS ONE: the function has no caller anywhere in the file today (its own comment says so, confirmed by search), so the fix changes nothing on screen. Paul's decision, 2026-09-14: leave it built, do not carry it forward as open work, do not raise it again unless something starts calling the function. See amendment 451.** |
| 10z | The blank-category refusal at posting widens to cover an unknown category | **BUILT, 2026-09-14, in `postReceiptToCashbook()`. See amendment 452.** |
| 10aa | The To Cashbook button is disabled on a row posting will refuse | **BUILT, 2026-09-14, in `renderReceipts()`. See amendment 452.** |
| 10ab | The rule duplicate check and the three-character minimum each extend to the door missing them | **BUILT, 2026-09-14, in `addRule()` and `updRule()`. See amendment 452.** |
| 10ac | Deleting a category checks the Review queue too | **BUILT, 2026-09-14, in `categoryReferences()`. See amendment 452.** |
| 10ad | A practice backup includes an orphaned books file rather than excluding it | **BUILT, 2026-09-14, in `exportPracticeBackup()`. See amendment 452.** |
| 10ae | The Bank Transactions account filter keeps its selection across a redraw | **BUILT, 2026-09-14, in `renderAccounts()`. See amendment 452.** |
| 10af | A failed file read reports itself rather than staying silent | **BUILT, 2026-09-14, in `readJSON()` and `exportPracticeBackup()`. See amendment 455.** |
| 10ag | The `RESOLUTIONS_DIR` environment override is removed from the pipeline | **BUILT, 2026-09-13/14, commits `476df39` and `af60be2`. See amendment 454.** |
| 10ah | The stale three-digit test fixture becomes a real four-digit example | **BUILT, 2026-09-13, commit `98effd8`. See amendment 454.** |
| 10ai | The per-client capture report moves into the client's own folder | **OUTSTANDING. Added 2026-09-13 by amendment 388, Paul's decision closing outstanding item 73. Claude Code's to build.** |
| 10aj | An end-to-end test proves a statement can arrive and be filed | **OUTSTANDING. Added 2026-09-13 by amendment 389, Paul's decision closing outstanding item 74. Claude Code's to build.** |
| 10ak | The phone capture app's Statements screen, alongside step 10aj | **OUTSTANDING. Added 2026-09-13 by amendment 393, Paul's decision closing outstanding item 81, narrowed. Consultant session's to build.** |
| 10al | The four remaining Desktop checks, run on screen | **OUTSTANDING. Added 2026-09-13 by amendment 398, Paul's decision closing outstanding item 86; item 83 merged in the same day by amendment 402, closing outstanding item 83. Consultant session's to build.** |
| 10am | A synthetic test forces a receipt through the Review seam | **OUTSTANDING. Added 2026-09-13 by amendment 399, Paul's decision closing outstanding item 88. Consultant session's to build.** |
| 10an | `handoverPack()`'s six files, run against a real client's books | **OUTSTANDING. Added 2026-09-13 by amendment 400, Paul's decision closing outstanding item 89. Consultant session's to build.** |
| 10ao | The remaining untested Receipts-tab changes, run on screen | **OUTSTANDING. Added 2026-09-13 by amendment 401, Paul's decision closing outstanding item 90. Consultant session's to build.** |
| 10ap | `setup_auth.py` deleted, `RECEIPT_CAPTURE_GUIDE.md`'s two references corrected | **BUILT, 2026-09-13, commit `f185916`. See amendment 454.** |
| 10aq | The recovery sweep routes a fallback to Review | **BUILT, 2026-09-14, in `_publish_unpublished_receipts()` (`app.py`), commit `9869a63`. Publishes carrying `validation_status: needs_review` rather than withholding, since Desktop's Review queue has no other reader since 10f.15. See amendment 457.** |
| 10ar | `claimed_client_id` removed, dead since `capture_token` replaced a phone-supplied claim | **BUILT, 2026-09-13, commit `cf94b62`. See amendment 454.** |
| 10as | The toothless duplicate test in `test_extractor_name.py` is deleted | **BUILT, 2026-09-13, commit `714e3c8`. See amendment 454.** |
| 10at | `export_bookkeeping.py`'s latest-attempt columns read from one row, picked by timestamp | **OUTSTANDING. Added 2026-09-13 by amendment 413, Paul's decision closing outstanding item 126. Claude Code's to build.** |
| 10au | The delivery-log reconciliation check, `IntelliBooks\Delivery\{CODE}.log` against each client's folder, specified and built | **OUTSTANDING. Added 2026-09-13 by amendment 417, Paul's decision closing outstanding item 131. Consultant session's to build. NARROWED 2026-09-14, amendment 456: nothing writes the log yet on either side, checked directly against `worker/client_copy.py`. This step is the Desktop-side reconciliation check only, and cannot be built until step 10az writes the log it reads. Blocked on 10az.** |
| 10av | `write_review_file()` deleted from `worker/filing.py` | ~~**OUTSTANDING. Added 2026-09-13 by amendment 443, Paul's decision. Confirmed dead code, never called. Claude Code's to build.**~~ **CANCELLED 2026-09-13, later the same day. Duplicate of item 114: already built 2026-09-12, commit `8a30daa`. Amendment 443 was wrong to schedule this as new work. Nothing to build.** |
| 10aw | The null-id filename fallback fixed, 12.3 step 2 | **OUTSTANDING. Added 2026-09-13 by amendment 444, Paul's decision. Recorded at 12.3 step 2 and amendment 48. Claude Code's to build.** |
| 10ax | `mark_receipt_filed()` error handling in `copy_for_published_receipt()` | **BUILT, 2026-09-13, commits `247442c` and `01a8dbc`. See amendment 453.** |
| 10ay | `_retry_failed_receipts()`'s failure handler no longer assumes no extraction row was written this attempt | **BUILT, 2026-09-13, commit `e9aab22`. See amendment 454.** |
| 10az | The delivery-log writer: one entry per document `copy_for_published_receipt()` copies into a client's folder, written to `IntelliBooks\Delivery\{CODE}.log` | **OUTSTANDING. Added 2026-09-14 by amendment 456, Paul's decision. Format specified in the same amendment. Briefed in `PROMPT_claude_code_2026-09-14_delivery_log_writer.md`, held for when Claude Code reaches it. Claude Code's to build.** |
| 10i | The pilot | **OUTSTANDING. Immediately before this step, and not before, the test estate is cleared: 17 receipt pairs and 34 files across four of the five test clients in `Clients\*\IntelliBooks\Receipts\`; five `Client_00X-books.json` files in `IntelliBooks\Books\`; 14 receipts and 11 categorisations in `receipts.db`; and the processed inbox copies under `Intellibills\Receipt Inbox\*\Processed\`. `Clients\Paul Keating\` and `Clients\She Run's It! Ldn Ltd\` are NOT test data and are not in scope. The three have to go together and in this order: clearing the receipt files without clearing the books leaves IntelliBooks showing receipts whose images are gone; clearing the books without the database leaves the pipeline's duplicate guard holding receipts that no longer exist anywhere. The consultant session tells Paul when this step is scheduled. Closed here 2026-09-13 as outstanding item 168, amendment 429.** |
| 11 | Console schema additions | **OUTSTANDING** |
| 12 | Load the app default chart into `coa_accounts` | **CANCELLED** |
| 13 | Token usage capture, `extraction_usage` | **OUTSTANDING** |
| 14 | Auth, `console_users`, login | **OUTSTANDING. When this step is built: the guard in `tests/test_logging_setup.py` hardcodes three log-handler entry points where `worker/logging_setup.py`'s own `ENTRY_POINT_LOGS` names four, `run`, `resolve`, `discard` and `console`. The fourth cannot be missed today because it does not exist yet, but the guard should read the list from `logging_setup.py` rather than restating it, so a console entry point does not arrive to a guard that keeps passing in silence. Closed here 2026-09-13 as outstanding item 176, amendment 434.** |
| 15 | Read queries, status page, queue page | **OUTSTANDING** |
| 16 | Receipt detail page, correction form, GL control | **OUTSTANDING** |
| 17 | `tax_year_bounds()`, then the browse page | **OUTSTANDING. Carries a decision from 17.4, amendment 448: the browse page exports CSV, reusing `export_bookkeeping.py`'s logic after section 16 step 10at's fix, not today's version.** |
| 18 | Intake panel and the registry reload | **OUTSTANDING. Carries a decision from 17.4, amendment 449: the panel filters out `receipt_id` values `recent-receipt` and `56b29977`, three test-written rows in the live event logs, rather than the logs being edited to remove them.** |
| 19 | Costs API client and the spend panel | ~~**OUTSTANDING**~~ **CANCELLED 2026-09-13 by amendment 415, Paul's decision closing outstanding item 128: he will monitor OpenAI spend directly at openai.com rather than build API-based cost tracking.** |
| 20 | Billing-error classification | **OUTSTANDING** |
| 21 | `RECEIPT_CAPTURE_GUIDE.md` for the console | **OUTSTANDING** |
| 22 | `CLAUDE.md` for the console | **OUTSTANDING** |

**Before any code, Paul:**

0. **BUILT.** Discard the two disposable failed test receipts. Delete the two untracked draft files. Merge `fix/imap-message-id-dedup` into `main`. Start a fresh branch. Ordering matters: every commit bumps `pipeline_version` and triggers an auto-retry pass, so clear the discards first.

> **Amended 2026-07-26. Step 0 was done differently and the merge is cancelled.** What actually happened, and why it matters to anyone reading this later.
>
> The discards were done. The merge was not, and should not be attempted on the basis written above. `main` is **42 commits behind** `fix/imap-message-id-dedup` and diverged by one, not six behind as the 2026-07-25 handover recorded. The single commit `main` holds is `965cb24`, a merge commit whose two parents are both ancestors of the working branch and whose tree is byte-identical to a recomputed clean merge of them, so it contributes no content. Nothing on `main` is needed. The only tracked files there and not on the working branch are the three `logs\*.ndjson`, deliberately untracked by `0859817`.
>
> Before this was noticed, a branch `docs/console-design` had been cut from `main` and this document committed on it, leaving the working tree missing 13 files of the built system, including `resolve_receipt.py`, `worker/extraction_pipeline.py` and three regression tests. Recovered on 2026-07-26: the two doc commits were cherry-picked onto `fix/imap-message-id-dedup`, and `feat/console-phase0` was cut from that. `docs/console-design` is kept as a safety net. One casualty: `logs\runs.ndjson` lost its records between 24 July 13:20 and 25 July, restored as far as `0859817^` allows. Log history only, no database or client file affected.
>
> **Phase 0 and everything after it happens on `feat/console-phase0`.** Bringing `main` up to date is its own session, with its own care, and is not a prerequisite for anything here.

**Phase 0:**

1. **BUILT.** ~~The auto-retry loop fix, 3.1, with tests 1 to 3.~~ **Built 2026-07-26 in `787493f`**, with `tests/test_auto_retry_no_loop.py`. See the note in 3.1. Ordering deviation, deliberate: step 2 was built first, in `22130d7`, because the fix cannot be written without it.
2. **BUILT.** ~~`save_extraction(update_status=False)`.~~ **Built 2026-07-26 in `22130d7`**, as `update_status=True` by default with `False` at the two new call sites. Plus `BaseExtractor.name` in `117fb1b`, pulled forward from step 7, see 10.1. Plus two unplanned commits the work exposed: `2d19521`, test log isolation, see section 15; and `285ed63`, actually writing `data/run.log`, see 4.3 and 6.5.
3. **BUILT.** ~~`parse_corrections` plus the zero-value and coercion fixes, tests 4 to 6, 10, 11.~~ **Built 2026-07-27** in `c0ac145`, `worker/resolution/service.py` with `tests/test_parse_corrections.py`, and `0cae398`, the CLI rewired with `tests/test_resolve_receipt_zero_and_types.py`. See the note in 4.2. Suite 48 of 48.
4. **BUILT.** ~~Review-pair cleanup and `review_count` from the DB, tests 7, 8. Also `processed_today`, per 3.10.~~ **Built 2026-07-27** in `dce1fdc`, `remove_review_pair()` in `worker/filing.py` with `tests/test_review_pair_cleanup.py`, and `25c6665`, `review_count` and `processed_today` from the database with `tests/test_status_counts_from_db.py`. See the notes in 3.5, 3.6 and 3.10. Suite 64 of 64.
5. **BUILT.** ~~Sidecar `category_code` and `category_name`, test 9.~~ **Built 2026-07-27** in `9f5cdad`, all four call sites plus the removal of the post-hoc mutation, with `tests/test_sidecar_category_keys.py`, and `96a5c5a`, `retroactive_categorise.py` with `tests/test_retroactive_categorise_sidecar.py`. See the notes in 3.7. Suite 73 of 73.
6. **BUILT.** ~~Move post-processing to `worker/extraction/postprocess.py`, test 12. Pure move.~~ **Built 2026-07-27** in `bf1976d`, with `tests/test_postprocess.py`. Suite 94 of 94. See the note in 10.2, which records the seven defects the move exposed and did not fix.
6b. **BUILT.** ~~Fix findings 1, 2, 6 and 7 from the 10.2 note.~~ Inserted 2026-07-27 by decision, see 17.4. **Built 2026-07-27** in `725545b`, `843a6b1`, `f0e0613` and `dc2e2ae`, with `tests/test_prefer_dayfirst_isolation.py` added. Suite 116 of 116.
6c. **BUILT.** ~~Write `extractions.details`, per 3.11.~~ **Built 2026-07-27** in `4aeadcd`. The write was dropped by `799cead` on 21 July, confirmed from the diff: it removed the parameter, the column and the value in a commit that rewrote 457 lines of `app.py`.
7. **BUILT.** ~~Extraction factory and `extractor.name`, replacing the hardcoded strings.~~ **Built 2026-07-27** in `71fe757`, the factory plus `config.EXTRACTION_ENGINE`, and `d931045`, the three failure paths. Suite 134 of 134. `app.py` no longer imports `OpenAIVisionExtractor` at all, so the concrete class is reachable only through the registry.
7b. **BUILT.** ~~Pass `pipeline_version` at the two embedded-image writes, per 3.12.~~ **Built 2026-07-27** in `684216f`. All six writes in `app.py` now pass it, checked programmatically.

**Resolution service:**

8. **BUILT.** ~~`worker/resolution/service.py`, tests 13 to 21.~~ **Built 2026-07-27** in `4c7c733` the `resolution_events` table, `f700706` the read side, and `60df040` `resolve_receipt()` and `discard_receipt()` with 23 tests. Suite 181 of 181. See the 2026-07-27 amendments to 4.2, 4.3 and 5.1 for the five points the control flow left open.
8b. **BUILT.** ~~The three service corrections: the `already_filed` guard, preserving `possible_duplicate` on `still_invalid`, and the `reason` column.~~ **Built 2026-07-27** in `d3bf4e1`, `4fddcb0` and `39d28b5`.
9. **BUILT.** ~~`resolve_receipt.py` as a thin CLI, plus `discard_receipt.py`. Verify test 40 by hand.~~ **Built 2026-07-27** in `e44be94` and `d4a5a97`. Suite 221 of 221. Test 40 run by hand against a temp database in all three modes, with the audit trail in `resolution_events` quoted in the report. `add_validation_note()` removed from `Repository`. One file per entry point for logging, `run.log`, `resolve.log`, `discard.log`, `console.log` reserved: the `QueueHandler` alternative was rejected because its listener has to be a process that owns the file, which means either the pipeline must be running before the CLI can log or there is a log daemon, and logging then stops silently if the listener is down.
9b. **BUILT.** ~~Two small corrections from the 4.4 amendment: call `confirm_duplicated_action()`, and add `receipts.filed_at` per 5.1a.~~ **Built 2026-07-28** in `4b50910`, the duplicate confirmation, and `d7c3d63`, `filed_at` plus the date in the `already_filed` message.
9c. **BUILT.** ~~Move the folder-intake original out of the inbox on every outcome, per 3.13.~~ **Built 2026-07-28** in `5ceb38b`. One commit as planned, done in the same session as 9b because it was live and costing an OpenAI call per poll for any receipt sitting in review.
   Original step 9 text, for the record: **Also `worker/logging_setup.py`**, per 6.5: move `attach_run_log_handler()` out of `app.py`, call it from every entry point, and settle whether it is one log file per process or a single writer. 4.3's accepted trade-off is unsound until this is done.
10. **BUILT.** ~~Back-feed consumer and `apply_resolution_note()`, tests 22 to 28.~~ **Built 2026-07-28** in `6d86894`, `apply_resolution_note()` as the entry point, `af5ab0a`, the consumer at the start of every poll, and `f453a9c`, tests 22 to 28. Suite 263 of 263. **Verified end to end on 2026-07-29 by test 41**, against the IntelliBooks half built by a session that could not see this one: a receipt filed in Desktop came back `ok` with its corrected figures and one copy on disk, a receipt deleted came back `discarded`, both notes reached `processed\`, and `failed\` was never created. See the 12.2, 12.3 and 12.4 amendments for the five points the two halves disagreed on and how each was resolved. Recorded as built on 2026-07-29: the commits landed on the 28th and section 16 was not updated at the time, which understated progress by three steps.

> **Steps 10a, 10b and 10c were suspended on 2026-07-30 by amendment 70, and resolved later the same day by amendments 72 and 73.** `PROMPT_claude_code_step10a_and_10b.md` still **must not be sent**: it was written against the abandoned folder scheme.
>
> **Where each now stands.** **10a** ~~shrinks a long way, because Intellibills no longer writes into `Clients\` at all, per amendment 73~~ **does not shrink at all. Corrected 2026-08-21 by amendment 122, fourth of four places, and the last one found.** Intellibills is the writer, so the folder names it uses still need config constants and step 10a's deliverable is untouched. What it becomes is the move of Intellibills' own paths to 18.2a: its document store to `Intellibills\Documents\` ~~`Intellibills\data\files\`~~, the database to `C:\Intellibills\db\` and out of OneDrive, its backups to `Intellibills\Backups\`, and `Receipt Inbox`, `Review` and `Resolutions` under `Intellibills\`. Paths corrected by amendment 76. The principle of holding the layout in config constants rather than string literals survives and is still the right deliverable. **10b is removed from the pipeline's build order entirely**, amendment 73: section 13A becomes IntelliBooks' work. **10c is superseded by 17.5a**, which combines it with the restructure into one six-stage operation, reset first. Rewrite 10a and 10c against 18.2a and 17.5a.

10a. **BUILT 2026-09-02.** ~~OUTSTANDING.~~ **All three sub-steps are done and the four checks that could be run passed. Amendment 172, which records what was checked and the two checks that could not be run.** ~~Move the client folder layout to amendment 55's namespaced form, **as corrected by amendment 65: five subfolders, including `_Statements`.**~~ Suspended, see the note above. ~~On the pipeline side this is small: `get_client_directory()` at `worker/filing.py:64` is the single choke point, and the only other site is the `*/Review` glob at `filing.py:297`.~~ **Struck 2026-09-02 by amendment 168. Both sites were wrong.** `get_client_directory()` builds the **client** folder from the client name and holds no subfolder name at all. **The two subfolder literals are `"Receipts"` at `worker/filing.py:78` and `"Statements"` at `worker/filing.py:103`.** There is no `*/Review` glob: `worker/filing.py:297` is `removed = _delete_review_pair(sidecar)`, and the Review scan is `_scan_other_clients_for_receipt()` at `worker/filing.py:303`, which already iterates `config.REVIEW_ROOT`. **Introduce config constants for the folder names rather than editing string literals**, so 13A and everything after it derive the layout from one place. ~~config constants for the four folder names~~ ~~The pipeline writes to three of the five, so it needs three constants plus the managed level: `_Receipts`, `_Statements` and `_Review`. `_Handover Pack` and `_HMRC Summaries` are Desktop's alone and get no constant here.~~ **Struck 2026-09-02 by amendment 168, on three counts.** **Two constants are needed, not three:** Review left the client folder and already has `config.REVIEW_ROOT` at ~~`config.py:42`~~ `config.py:60`, corrected 2026-09-02 by amendment 174 because 10a.1 and 10a.2 inserted three constants above it, put there by 18.2a and explained in the docstring at `worker/filing.py:156`. **The names carry no underscore.** The literals are `"Receipts"` and `"Statements"`, so landing the constants at their current values means those two strings; writing them with an underscore would send the pipeline to folders `IntelliBooks-Desktop-v3.html` does not read. **And the set of five is wrong.** What is written under `Clients\<name>\` today is `Receipts` and `Statements` by the pipeline, and `IntelliBooks`, `HMRC Summaries` and `Handover Pack` by `IntelliBooks-Desktop-v3.html` at lines 703 and 3105, 1165 and 1181, and 1978. **`IntelliBooks` was in no list until now.** The `getDir` call at `IntelliBooks-Desktop-v3.html:2847` takes `...subParts` and has not been traced. Land the constants at their **current** values, so this commit changes no behaviour. ~~and flip them in 10c when there is nothing on disk to migrate.~~ **Struck 2026-09-02 by amendment 168: 10c is BUILT, so the window this pointed at has closed, and the namespaced form above is struck as suspended, so nothing schedules the values ever changing.** ~~Whether that form is still wanted, and where the flip lives if it is, are Paul's and are open.~~ **Answered 2026-09-02 by amendment 170. The parent folder is kept and the underscores are dropped, and the flip lives at sub-step 10a.2.** Desktop's ~~six~~ **nine** `getDir(["Clients", ...])` sites change in the same window, **at `IntelliBooks-Desktop-v3.html` lines 703, 1165, 1181, 1793, 1819, 1978, 2475, 2847 and 3105, plus a tenth site at 2519 that builds the path as a string instead of calling `getDir`. Corrected 2026-09-02 by amendment 168; step 10f already carried nine.**, and the tax year folder keeps its bare `2026-27` form for the reason in amendment 55.

   **Decomposed into three sub-steps 2026-09-02 by amendment 170**, because the constants and the parent folder are two changes and only the first of them changes nothing. **3 sub-steps.**

   10a.1 **BUILT 2026-09-02 in `7ea2dc4`.** ~~OUTSTANDING.~~ **The two subfolder names become config constants, at their current values.** `"Receipts"` at `worker/filing.py:78` and `"Statements"` at `worker/filing.py:103`, the only two literals, and nothing else moves. **This changes no behaviour and nothing on disk**, which is the whole point of doing it separately.

   10a.2 **BUILT 2026-09-02.** Pipeline `2ac70ab`; Desktop items 50 and 51 of `IntelliBooks\App\Docs\IntelliBooks-Change-Log.md`; the five folders moved by `_step10a_move.py --apply`, run by Paul. ~~OUTSTANDING.~~ **The client folder gains one parent folder, `IntelliBooks`, and every folder either product owns sits inside it. No underscore on the parent or on any child.** Paul's decision, 2026-09-02, amendment 170. The shape is `Clients\{client_folder_name}\IntelliBooks\` holding `Receipts\{tax year}\`, `Statements\{tax year}\{platform}\`, `HMRC Summaries\` and `Handover Pack\`. **Four children, and `Review` is not one of them**: it left the client folder by 18.2a and lives at `Intellibills\Review\{client_id}\`. **This is the step that moves files, in both products and in every client folder that already has one, and it is one coordinated change or it is a repeat of 2026-09-01**, when the pipeline wrote a path IntelliBooks did not read. **It is also where the flip of the constants' values lives**, which is the question amendment 168 left open when it struck "flip them in 10c" and found 10c already BUILT. **`Clients\{name}\IntelliBooks\` already exists as a name in the code and conflicts with nothing**: it is the legacy books location, read at `IntelliBooks-Desktop-v3.html:703` and `:3105`, both passing `create` as false so neither can create it, and no client folder on disk has one. **One toast changes with it, at `IntelliBooks-Desktop-v3.html:710`**, which today reads "Books migrated to IntelliBooks\Books. The old copy under Clients\{name} can be deleted" and would be telling the operator to delete the parent of everything.

   10a.3 **BUILT 2026-09-02, and it is a document sweep rather than code.** **Every live path statement in this document that names a folder directly under the client folder gains the parent level, and the sweep was enumerated when it was run rather than from the list written an hour earlier.** **Done by amendment 171, which records what was changed and what was deliberately left.** Amendment 170 changed two of them, at 13A.1 and 13A.3, being the two Claude Code identified on 2026-09-02 as protected only by a warning four lines above rather than by their own text. **A count taken today is 46 live occurrences of a `Clients\` path across 35 lines in sections 3.5, 3.6, 3.7, 12.2, 12.4, 13A, 13A.3, 16, 17.2, 17.5, 17.5a, 18.2, 18.2a, 18.2b, 18.6, 18.9 and 18.11, of which roughly a dozen name a subfolder and gain a level. That figure is context and not the work**: several are already stale for a different reason, being `Review` paths superseded by 18.2a, and this project's own history with counted sets says the enumeration belongs to whoever runs the sweep.
10b. **MOVED.** ~~The file reconciliation check, section 13A.~~ **Removed from the pipeline's build order 2026-07-30, amendment 73.** Section 13A becomes IntelliBooks' work, because the question it now answers is whether the client folder matches IntelliBooks' delivery log, and IntelliBooks is what writes there. Intellibills may still want a smaller check of its own archive against its own database; that is not specified and is not this step. Original text follows for the record. Run it once against the current state before anything is cleared, and confirm it finds `PKPH-books.json` and whatever else is there. A check that reports nothing today is broken.

**The clean-slate reset:**

10c. **BUILT.** The staged clear-down in 17.5. ~~**After the live round trip, test 41, and before any console work.**~~ ~~**Rescheduled 2026-07-29: after the four IntelliBooks Desktop changes A to D and their manual checks, and still before any console work.** Test 41 has passed, so the original precondition is met; the new one is that change D's test needs the books populated and the reset empties them. See amendment 54.~~ **Superseded 2026-07-30 by 17.5a**, which combines the reset with the practice root restructure into one six-stage operation, reset first, code change last. The change D precondition is gone with change D. Placed here on 2026-07-28 by decision. Not before step 10, because resetting and then changing the pipeline is two variables at once; not after step 11, because the console should be built on a clean slate rather than on 27 test receipts. The consultant session supervises it stage by stage, verifying state before and after each stage rather than at the end.

**The one client registry:**

10d. **BUILT.** **The one client registry and the capture credential, all three codebases in one window.** **Built 2026-09-03: 68 sub-steps, 67 built and 10d.47 cancelled. Amendments 182 to 185.** ~~The one client registry, both products in the same window.~~ **Widened 2026-08-20 by amendment 113, which added the capture app and `capture_token`.** Added 2026-08-20 by amendment 110, which carries the reasoning for the position. **It comes here, after the reset and before every console step**, because steps 11 to 22 are all unstarted and this is not one of them.

   **Decomposed into sub-steps 2026-08-21 on Paul's instruction, because a paragraph cannot carry a status and a numbered line can.** **67 sub-steps.** ~~58 sub-steps.~~ **Corrected 2026-09-02 by amendment 176, which added 10d.59 to 10d.67.** ~~52 sub-steps.~~ **Corrected 2026-09-02 by amendment 169, which added 10d.53 to 10d.58.** ~~50 sub-steps.~~ **Corrected 2026-09-01 by amendment 164, which added 10d.51 and 10d.52.** ~~35 sub-steps: `10d.35` was added later the same day by amendment 132.~~ **Corrected 2026-08-22 by amendment 157: sub-steps `10d.36` to `10d.50` were added on 2026-08-21 by amendments 141, 144, 146, 147, 149, 150 and 152, and this sentence was not moved with them. 50 enumerated, contiguous 1 to 50, no duplicates, and the table at the head of this section already said 50.** The reasoning is not repeated below: **amendments 110 to 117 and 120 carry the decisions, 18.2a the layout, 17.5a the order, and 18.2b the product boundary.** Read those before starting, not the summary.

   **The registry.**

   10d.1 **BUILT.** Create `C:\Users\PDK7\OneDrive - Intellitax Accounting Limited\Intellibills\clients.json` with amendment 111's field list: `client_id`, `client_name`, `client_folder_name`, `capture_token`, `emails`, plus the book attributes. **No `client_code`.** snake_case throughout. Rename `clients.csv` to `clients.csv.superseded-2026-08-20` rather than deleting it.
   10d.2 **BUILT.** ~~Carry the clients across and rename each books file from `{CODE}-books.json` to `{client_id}-books.json`. **Blocked by outstanding item 98:** amendment 111 names `PKPH`'s books file, which no longer exists, and does not name `PSHIPN-books.json`, which does.~~ **Nothing is carried across. Paul's decision, 2026-08-21, amendment 139.** The test clients are created fresh in the new shape and `chartFor()` builds their books. **All seven books files in `IntelliBooks\Books\` are retired**, including the orphan `PSHIPN-books.json`. **This unblocks the sub-step by removing the migration rather than by settling item 98**, which closes with it.
   10d.3 **BUILT.** IntelliBooks stops holding its own client list and reads `clients.json`. It never reads `clients.csv` today, zero occurrences, counted.
   10d.4 **BUILT.** Strike through `CLAUDE.md`'s "Two rules about `clients.csv`" section and 18.2c's paragraph on the same subject. The `emails` array retires both rules.

   **The capture credential. Amendment 113.**

   10d.5 **BUILT.** `capture_token` replaces `code` in the capture app's payload entirely. The payload carries no client claim of any kind.
   10d.6 **BUILT.** `upload.js` reads `clients.json` over Graph, validates the token, resolves it to `client_id` and `client_folder_name` itself, writes `client_id` into the sidecar, and names the inbox folder from `client_folder_name`.
   10d.7 **BUILT.** Remove the shared `UPLOAD_KEY` in the same change. No fallback is left working.
   10d.8 **BUILT.** Delete `s-code` at `index.html:221`, `set-code` at `:241` and `set-name` at `:155`, so nothing on any screen can alter which client a receipt belongs to.
   10d.9 **BUILT.** Raise `upload.js:34`'s eight-character truncation.
   10d.10 **BUILT.** `dispatch()` stops calling `addHistory()` after a failed send, which today lists a receipt in **Recently Sent** while the card above it says the send is still waiting.

   **The inbox. Amendment 112.**

   10d.11 **BUILT.** `scan_inbox()` reads `client_id` out of the item's sidecar instead of deriving the client from the folder name. The folder name becomes decoration. **A file with no sidecar therefore has no client. Added 2026-08-21 by amendment 147: it gets `source = other` and goes to Review**, per 10d.16 and 10d.18, which needs no exception because an unresolved client already routes that way. `worker/intake/folder_reader.py:102` does not require a sidecar today and must not start requiring one: **the file is kept and reported, not refused.**
   10d.12 **BUILT.** `importToInbox()` in `IntelliBooks-Desktop-v3.html`, which is **Add Receipts**, starts writing a sidecar carrying `client_id`. Today it copies the file alone and that route has no client in the item.

   **The client folder. Amendment 113.**

   10d.13 **BUILT.** Delete the fallback at `app.py:143`, which reads `config.CLIENTS_BY_CODE.get(client_code, {}).get("client_name", client_code)` and silently substitutes the code for the name whenever the lookup misses.
   10d.14 **BUILT.** `get_client_directory()` at `worker/filing.py:64` takes `client_folder_name` off the client record. ~~**Blocked by outstanding item 139:** amendment 73 and 18.2b say the pipeline loses this function entirely and never writes into `Clients\`, and this sub-step keeps it. One of the two is wrong and it is Paul's ruling.~~ **Unblocked 2026-08-21 by amendment 122. Item 139 is closed and this sub-step is the correct side: the function stays and is repointed.** The freeze narrowed for step 10d at 18.2b still governs what else may change in that function, which is nothing.
   10d.15 **BUILT.** IntelliBooks' nine `getDir(["Clients", safeName(c.name), ...])` sites take `client_folder_name` instead of `safeName(c.name)`, and so does the tenth site, which builds the path as a string rather than calling `getDir`. ~~four `getDir` sites~~ Corrected 2026-09-02 by amendment 174: amendment 168 enumerated nine plus the string site, and all ten were changed that afternoon by change log item 50 of `IntelliBooks\App\Docs\IntelliBooks-Change-Log.md`.

   **`UNKNOWN` and the firm. Amendments 115, 117 and 120.**

   10d.16 **BUILT.** A receipt written with `client_id = UNKNOWN` is a review item and reports, and is never `status = ok`. The value stays; what goes is its arrival as a fallback rather than as a recorded conclusion.
   10d.17 **BUILT.** Remove `save_receipt()`'s **four** defaults at `worker/database/repository.py:209`, being `firm_id`, `client_id`, `client_code` and `source`. ~~three defaults~~ **Corrected to four on 2026-08-21 by amendment 120:** the rebuild removes the column default for `source`, and Python supplies its own before the SQL is reached, so leaving it defeats that change.
   10d.18 **BUILT.** An unresolved client files nothing into `Clients\` and the item goes to `Intellibills\Review\`. **This is the only part of step 10d that reports.**
   10d.19 **BUILT.** `DEFAULT_FIRM_ID` stops being a fallback. `worker/intake/folder_reader.py:88` goes with the unresolved-client case, the client loader refuses a record with no firm, `app.py:1045` and `:1061` take the firm from the receipt they already name, and the client is resolved once before the per-attachment loop rather than at `app.py:1071` inside it. ~~**Blocked in part by outstanding item 1:** where an unattributable intake event is logged.~~ **Unblocked 2026-08-21 by amendment 128, Paul's decision: an unattributable event goes to a reserved firm id, `receipt_events_UNATTRIBUTED.ndjson`.** The name is built at `app.py:102` and again at `worker/extraction_pipeline.py:96`, the same line twice, and both change. **`config.RECEIPTS_LOG` at `config.py:52` is deleted in the same sub-step rather than revived**, which closes outstanding item 72; `tests/test_path_layout.py:83` asserts its value and goes with it.
   10d.20 **BUILT.** `repository.py:60` and `:69` return `config.DEFAULT_FIRM_ID` instead of the literal `"INTELLITAX"`.
   10d.21 **BUILT.** Rename `ONEDRIVE_ROOT` and `LOCAL_ROOT`. Each names a thing that is not the property that matters, and nothing in the pipeline calls a Microsoft API.

   **The database, rebuilt and not migrated. Amendment 116.** There is no data worth preserving: three test receipts, three extractions, three categorisations, one `processed_attachments` row and seven empty tables.

   10d.22 **BUILT.** Edit `schema.py` to the shape wanted, drop the affected tables, and let `init_db()` recreate them, so one definition is the only definition.
   10d.23 **BUILT.** `receipts.client_code` column removed. 18.9 records it as cancelled.
   10d.24 **BUILT.** `receipts.firm_id`: `DEFAULT 'INTELLITAX'` removed, `NOT NULL` kept.
   10d.25 **BUILT.** `receipts.client_id`: `DEFAULT 'UNKNOWN'` removed and `NOT NULL` added.
   10d.26 **BUILT.** `receipts.status`: `DEFAULT 'pending'` removed. `save_receipt()` writes the literal at `repository.py:216`, so the default is never reached.
   10d.27 **BUILT.** `receipts.email_received_at`: one format, ISO 8601 UTC. **`app.py:926` changes with it**, which today passes `int(intake.source_path.stat().st_mtime)`.
   10d.28 **BUILT.** `receipts.locked_at`: `TIMESTAMP` becomes `TEXT`. **`acquire_receipt_lock()` at `repository.py:675` changes with it**, passing `.isoformat()` rather than a `datetime` object, and the comparison at `:674` with it.
   10d.29 **BUILT.** `statements.client_code` column removed. One writer, `app.py:890`. The table has 0 rows.
   10d.30 **BUILT.** `categorisations.business_type` renamed `trade`, per amendment 105.
   10d.31 **BUILT.** `extractions.currency`: `DEFAULT 'GBP'` removed, and **the twelve `"GBP"` literals become one constant**, six in `app.py`, four in `worker/resolution/service.py` and two in `worker/extraction/openai_vision.py`.
   10d.32 **BUILT.** `processed_attachments` gains `firm_id`, informational, key unchanged at `(message_id, attachment_id)`. One writer, `mark_processed()` at `repository.py:263`. ~~**See outstanding item 6**, which questions the key.~~ **Item 6 closed 2026-08-21 by amendment 129, Paul's decision: the key stays.** A `message_id` is generated by the sender's mail client and is unique by design, so the key is already unique across firms, and adding `firm_id` would loosen it rather than tighten it. **The one case that would want it is recorded in section 6 of the outstanding items list as a cloud constraint**, being a firm with its own capture address, where the same message reaching two firms would be rejected as a duplicate by this key.
   10d.33 **BUILT.** `resolution_events`: drop `receipt_id`'s foreign key at `schema.py:172`, leave `extraction_id` unconstrained, and **rewrite the comment at `:155` to `:159`, which gives a false reason**: a NULL foreign key value satisfies the constraint, so the case it claims to protect was never at risk.
   10d.34 **BUILT.** Remove the nine `ALTER TABLE ADD COLUMN` migrations at `schema.py:180` to `:232`. **Delete two tests with them**, per amendment 120: `tests/test_discard_reason.py`'s `test_the_column_exists_on_an_older_database` and `tests/test_filed_at_column.py`'s `test_existing_rows_are_not_back_filled`, which exist only to prove those guards work.

   **The registry is re-read while the pipeline runs. Amendment 132.** Numbered last because it was added on 2026-08-21, after 10d.1 to 10d.34 were fixed; its subject belongs with the registry at the top of this step.

   10d.35 **BUILT.** `app.py` stats the registry file at the top of each `process_once()` and calls `config.load_clients()` again if the modification time has moved. **`config.py:149` loads it once at import**, and `main()` at `app.py:1203` polls in a loop until the process ends, so today a client registered while the pipeline runs is invisible to it until a restart. **Two conditions:** a failed parse keeps the registry already in memory and logs an error, never emptying it and never ending the poll; and the writer writes temp-and-rename, as amendment 104 already requires for the inbox JSON. **Section 8.6's marker file is struck and not built.** Closes outstanding item 142.

   **Two small firm-identity fixes, added 2026-08-21 by amendment 141. Both are here because this step is where firm and client identity is settled.**

   10d.36 **BUILT.** **`send_unknown_sender_alert()` at `worker/email/alerts.py:54` takes a `firm_name`, the way `send_no_attachment_alert()` at `:11` already does.** Three literals go with it: `support@lastingimpact.co.uk` at `:69`, and `Lasting Impact` at `:75` and `:80`. **This is the only automatic email that reaches somebody who is not a known client**, so it is the first thing an unregistered sender sees, and it currently names the wrong company. Row F7 of `2026-08-20_LIST_settings_firm_and_client.md`. Closes outstanding item 16.
   10d.37 **BUILT.** **Delete `EXPORTS_DIR` at `config.py:38` and its `mkdir` at `:95`.** No reader anywhere outside `config.py` and `tests/test_path_layout.py:40` and `:112`, which go with it. **The folder itself stays**: `Intellibills\Exports\` holds `2026-08-18_EXPORT_categorisations_client_vendors.csv`, the only thing of value the database ever held. Same class as `RECEIPTS_LOG` at 10d.19. Closes outstanding item 25.

   **Three things are decided and need no sub-step, recorded so they do not look dropped.** `categorisations.needs_review INTEGER DEFAULT 1` **stays**, being the only default in the schema pointing the cautious way. **One email address serves one client**, confirmed rather than changed by amendment 117: the `emails` array holds several addresses for one client and not one address for several clients. And **collapsing the inbox to one folder stays available as a later decision**, cheaper after this step because by then nothing reads the folder name.

   **The order on the day, and it mirrors 17.5a because this is a reset.** Five stages rather than sub-steps: changing code against a populated database of the old shape is two variables at once.

   1. **Stop the pipeline.** Nothing mid-write, and confirm no `Intellibills\pipeline.lock` is left behind. ~~**See outstanding item 104**, a lock present on 2026-08-21.~~ **Corrected 2026-08-21: a lock left behind is normal and not a fault.** Paul starts the pipeline on demand and closes it, so the lock outlives every session and `acquire_lock()` clears it at the next start. Items 26 and 104 were both raised on it and both closed. **What this stage needs is that the pipeline is not running, not that the file is absent.**
   2. **Back up.** `backup_db()`, plus a full listing of the practice root and every client folder.
   3. **Empty and rebuild.** Drop the affected tables and run `init_db()` from the edited `schema.py`. **`processed_attachments` is free to clear**: Paul confirmed on 2026-08-20 that every folder in the capture mailbox is empty.
   4. **Change the code, all three codebases together.**
   5. **Start, run one clean cycle, confirm.** One receipt from the phone end to end, then a Review item, then a post.

   **One more sub-step, because stage 5 needs it. Added 2026-08-21 by amendment 144, closing outstanding item 21.**

   10d.38 **BUILT.** **`loadSampleData()` writes category names into a field that holds codes.** At `IntelliBooks-Desktop-v3.html:2606` onward it builds five sample receipts carrying `Motor expenses`, `Sundry expenses`, `Repairs and maintenance`, `Parking and tolls` and `Fuel`, so **every sample receipt lands with a category the chart does not contain**, and the sample transactions land uncategorised. It also sets `category="(Transfer)"` on the linked pair, which is outstanding item 56 and is not this. **It is here because this step is where it starts to matter:** 10d.2 recreates the test clients with empty books, and this function is the only way to get transactions into a fresh one, so it is the first thing stage 5 reaches for. It is also the source of the demo version's data, per outstanding item 21.

   **Two more on the database and on what a source word means. Amendments 146 and 147.**

   10d.39 **BUILT.** **`categorisations_firm_vendors` gains a nullable `firm_id`, written and not read. Added 2026-08-21 by amendment 146, closing outstanding items 17 and 47.** **The unique key does not change**, so behaviour does not change: `get_firm_vendor()`, `list_firm_vendors()` and the fuzzy layer all carry on ignoring the firm and the pool stays shared. **The column exists so the provenance of a learned mapping is captured while it is still capturable.** **`upsert_firm_vendor()` at `repository.py:389` takes the firm and writes it**, which is one production caller at `engine.py:420` and one test caller at `tests/test_resolution_view.py:288`. **The engine resolves the firm from the `client_id` it already receives rather than gaining a parameter**, which avoids touching `categorise()`'s five production call sites, being `app.py:376`, `worker/extraction_pipeline.py:191`, `worker/resolution/service.py:643` and `:1034`, and `retroactive_categorise.py:133`. **That route only works after 10d.19**, which makes the client loader refuse a record with no firm, so this sub-step comes after it.
   10d.40 **BUILT.** **`receipts.source` has four values and no others: `email`, `phone`, `desktop`, `other`. Added 2026-08-21 by amendment 147, closing outstanding item 18.** **Each writer declares it and the reader reads it.** The capture app writes `phone` into its sidecar, **Add Receipts** writes `desktop` into the sidecar it starts writing at 10d.12, the email path writes `email`, and **a file with no sidecar gets `other`** and goes to Review per 10d.11. **`scan_inbox()` stops hardcoding `source="capture"` at `worker/intake/folder_reader.py:114`.** **And the sidecar stops using its own vocabulary:** `worker/extraction_pipeline.py:220` and `:261` write `"email" if message_id else "folder"`, so today one receipt is `capture` in the database and `folder` in the sidecar. Both use the four words. **No migration:** 10d.22 rebuilds `receipts`, and the three rows on it today are two `capture` and one `email`.
   10d.41 **BUILT.** **A two-digit year that resolves into the future is rejected, and the three-digit branch is deleted. Added 2026-08-21 by amendment 149, closing outstanding item 140. Findings 3 and 4 of 10.2, which step 6b did not take.** `_parse_numeric_date()` at `worker/extraction/postprocess.py:60` reads `if c < 100: year = 2000 + c`, so `01/01/99` becomes **2099**. **Keep `2000 + c`, and where that year is later than the current year treat the date as unreadable and return `None`.** **No century pivot:** a cutoff tight enough to turn 99 into 1999 turns 28 into 1928, so the system would be choosing between 1928 and 2028 on its own. `CLAUDE.md`'s closing rule governs, if something is uncertain mark it for review and do not guess. **`:62` to `:64`, `elif c < 1000`, is deleted**: identical body to the branch above it, a comment claiming "treat as 2000s" while `2000 + 999` is 2999, and a three-digit year is a misread rather than a year. **The note comes from `resolve_invoice_date()`**, because `_parse_numeric_date()` returns a date or `None` and has no note channel, which is why there is no note today. It records that a year was inferred from two digits, and records the rejection.

   10d.42 **BUILT.** **Identifying the gross figure. Assume, verify, and flag rather than guess. Added 2026-08-21 by amendment 150, closing outstanding item 141. Finding 5 of 10.2, the last of the three step 6b left.** **Nothing in this is a VAT question and the naming must stop saying it is.** `apply_vat_inclusive_swap()` at `worker/extraction/postprocess.py`, its `rate_tol = 0.03` at `:113` and the wording of its note are all renamed to say what they do, which is **establish which figure is the gross**. The VAT figure is the evidence, not the subject.

   **The rule.** Where a receipt yields a money figure and a VAT figure and no gross, **assume the figure is the gross**, because Paul's observation is that a receipt showing two numbers always shows gross and VAT. **Verify it:** the implied rate is the VAT divided by the figure less the VAT, and it must come out at a recognised rate within a **rounding allowance only**, being a fraction of a percentage point. **If it verifies, accept:** gross is the figure, net is the figure less the VAT. **If it does not verify, change nothing and route to Review**, with the implied percentage in the note.

   **Three things this must not become.** It must not be **gated on the client's VAT registration**: a non-registered client's expense is the gross, so getting this wrong overstates their profit and loss by the VAT, which makes it matter more for them and not less. It must not carry a **per-rate window or a minimum receipt size**, both of which were designed and then made unnecessary by the assume-and-verify shape. And the recognised rates must come from **18.4's vocabulary rather than a literal list** in the function, which today reads `common_rates = [0.2, 0.05]`.

   **The phone app's settings model. Amendment 152. Who owns each setting, how it reaches the phone, and what the phone is told.**

   10d.43 **BUILT.** **"Capture" stops meaning the phone app. It becomes the phone app throughout**, F10 the phone app address, C18 the phone app token, and the link the **Phone App Setup Link**. **"Capture" is left meaning the mailbox and nothing else**, which is `capture@lastingimpact.co.uk` and F3. The word currently means five things and one of them, `source='capture'`, was already retired at 10d.40.
   10d.44 **BUILT.** **The setup link carries every firm-owned setting, always, as a complete statement.** Today `copyCaptureLink()` adds a parameter only when the value is truthy, `if(c.vat)`, `if(c.mode==="confirm")`, `if(c.phv&&c.phv.length)`, and `index.html:200` to `:201` only takes a value when the parameter is present. **So the two halves conspire and a link can turn a setting on and never off.** `&vat=0` and an empty `&phv=` become meaningful.
   10d.45 **BUILT.** **Client-owned settings never appear in the link at all**, so the firm cannot overwrite a choice that is not theirs.
   10d.46 **BUILT.** **The upload response carries the current firm-owned settings and the phone applies them.** After 10d.6 `upload.js` already reads `clients.json` over Graph to validate the token, so the server holds the client record at every upload. **This is what makes a firm-owned setting reliable**, because the link is a one-time push and only reaches the phone when somebody opens a fresh one.
   10d.47 **CANCELLED.** **Paul's decision, 2026-09-03. See amendment 185: 10d.46 removed the problem this was written for.** ~~A change to a firm-owned setting notifies the client, probably by email.~~ A third outbound message beside the no-attachment and unknown-sender alerts, so it lands with 10d.36, which is already giving those two the firm's name.
   10d.48 **BUILT.** **Confirm mode is the client's alone and is off by default.** It comes off the firm's side entirely: out of the client **Edit** window, out of the link, and off Client Settings. The client turns it on in the phone's own settings. C4.
   10d.49 **BUILT.** **The PHV platforms and the statement week ending day are the firm's alone and the client cannot change them.** Shown **read-only** on the phone's settings screen so the client can see what applies. C5 and C6. **C6 exists only on the phone today**, at `index.html:244`, where the firm cannot read it, restore it or know it changed.
   10d.50 **BUILT.** **PHV settings appear only on a PHV driver's phone app.** They are currently offered to every client.

   **What the phone asks for when confirm mode is on, and it is not everything.** The VAT box at `index.html:114` earns its place least: extraction reads the VAT off the image anyway and nothing treats a client's typing as better. **The three worth asking are the gross where a receipt shows a figure and a VAT figure without saying which, the supplier where the receipt is unclear, and the category.**

   **And the reason confirm mode is off by default is a design observation rather than a preference, recorded so it is not lost.** Whether a given supplier's receipt states the gross or the net is **learnable once per supplier and true for every client**, which is the same shape as the vendor mapping tables. **So it is a fact about a layout, not a question for a person**, and asking every time would be the wrong answer even if nobody minded. **Two caveats:** nothing today learns anything about layout, only GL codes, so it would be a new kind of stored fact; and it is true most of the time rather than always, since a supplier can change format and one supplier can issue both a till receipt and an invoice, so whatever holds it must be revisable exactly as a vendor mapping is. **Not scheduled. Recorded as the reasoning.**

   **Two things are not touched, and the first has already been got wrong once.** `Clients\Paul Keating\` holds four engagement letters, four proposals, `Document Requests\` and `Misc\`, **real firm records**, and 17.5a records that a session once described that whole folder as disposable. And `Intellibills\Exports\2026-08-18_EXPORT_categorisations_client_vendors.csv`, the only thing of value the database ever held, 100 rows, all `Client_006`.

   **It needs three briefs and none of them is written.** The pipeline, `IntelliBooks-Desktop-v3.html`, and the capture app, which is a separate Netlify deployment only Paul can release. **All three written against the same field list, and the flip happens in one sitting or receipts stop arriving.** The capture app's brief is the one with a deploy in it and no test suite behind it.

   **The firm file, and the third file that is not created. Amendment 164.**

   10d.51 **BUILT.** **`Intellibills\firms.csv` becomes `Intellibills\firms.json`, and it takes the phone app address.** `load_firms()` at `config.py:132` reads JSON. **One reader in the whole system**, `config.FIRMS` at `config.py:150`, read at `app.py:839` and nowhere else, and IntelliBooks does not read it at all, so the conversion is that one function. **snake_case throughout**, matching `clients.json`. **The `email` column comes across unchanged rather than being quietly dropped**, which is outstanding item 24: it is read by nothing and it is one of the three fields a firm currently is. `firms.csv` is renamed `firms.csv.superseded-2026-09-01` rather than deleted, the same treatment `clients.csv` gets at 10d.1.

   10d.52 **BUILT.** **`IntelliBooks-Practice.json` is retired with nothing left in it, and no third firm file is created.** Its client list goes to `clients.json` at 10d.3. Its `settings.uploadKey` goes at 10d.7. **Its `settings.captureUrl` becomes the phone app address on `firms.json`**, which is row F10 and the only firm setting it held. **And its `version`, `savedAt` and `instance` are per-file housekeeping rather than firm data**: `instance` is a per-browser id generated at `IntelliBooks-Desktop-v3.html:593` from `localStorage["ib3_instance"]`, stamped on save at `:671` and `:690`, and compared at `:718` to notice when another browser has written the same file. IntelliBooks stamps all three on whatever it writes, the way it already does on every books file. **The file is renamed `IntelliBooks-Practice.json.superseded-2026-09-01` rather than deleted.** **`firm-settings.json` is not created:** the consultant session proposed it on 2026-09-01 and Paul rejected it, because firm data in two files is worse than a file extension.

   **The two folder layouts keyed on the client code, the statement's own copy, and IntelliBooks' two code-named outputs. Amendment 169.**

   10d.53 **BUILT.** **`Intellibills\Documents\` is keyed on `client_id` rather than the client code.** `worker/storage/store.py` builds `config.FILES_DIR / client_code / year / month` at lines 23 and 37, in `save_file()` and `save_inbox_file()`. Both take `client_code` as their second parameter, and so do their three callers, `app.py:733`, `app.py:918` and `app.py:1097`. **The year and month stay.** They are the arrival date, and the store is keyed on it below the client because at the moment the file is written nothing has read the receipt and there is no invoice date to file by; `app.py:367` takes the invoice date out of the extraction record, which does not exist yet. **The folders on disk today are `PKPH` and `TESTST`, five files between them, and neither client survives 10d.2. Leave them, name them in the report, and Paul removes them.**

   10d.54 **BUILT.** **`Intellibills\Review\` is keyed on `client_id` rather than the client code.** `_review_dir_for_client_code()` at `worker/filing.py:155` becomes `_review_dir_for_client_id()`, and `file_review()`'s `client_code` parameter at `worker/filing.py:118` changes with it. **The docstring's reasoning survives intact**, because a `client_id` cannot drift either and that is the whole point of it. **`_scan_other_clients_for_receipt()` at `worker/filing.py:303` needs no change**: it iterates `config.REVIEW_ROOT` and every subfolder is still one client's.

   10d.55 **BUILT.** **A statement gets a copy in `Intellibills\Documents\` before it is filed.** Paul's decision, 2026-09-02. The statement branch at `app.py:858` calls `save_inbox_file()` first, the way the receipt branch does at `app.py:918`, and passes that path to `file_statement()`. **Today that branch never calls into `worker/storage/store.py` at all**, so `worker/filing.py:111` copies the statement from the inbox straight into the client folder and `Clients\<name>\Statements\` is the only copy. **That is why receipts can be reconstructed and statements cannot.** The `statements` table has 0 rows, so nothing on disk is affected.

   10d.56 **BUILT.** **`statements` gains `filed_path`, and `file_path` means the same thing on both tables.** Paul's decision, 2026-09-02. `receipts` has both: `file_path` at `worker/database/schema.py:87` for the copy in `Intellibills\Documents\` and `filed_path` at `:89` for the copy in the client folder. **`statements` has only `file_path`, at `:102`, and `app.py:898` writes the client folder path into it**, so one column name means the original on one table and the copy on the other. **After this, `file_path` is the document store's copy on both and `filed_path` is the client folder's copy on both.** `save_statement()` in `worker/database/repository.py` changes with it. **`app.py:361` is the line that shows why it matters**: it takes `receipt["file_path"]` as the file to copy from when filing, and the same line written against a statement would copy the filed copy onto itself.

   10d.57 **BUILT.** **The HMRC summary filenames stop carrying the client code.** `IntelliBooks-Desktop-v3.html` writes into `Clients\<name>\HMRC Summaries\` at lines 1165 and 1181. The two files in `Clients\Test Sole Trader\HMRC Summaries\` are named `testst-hmrc-2025-04-06-to-2026-04-05.csv` and `testst-archive-2025-04-06-to-2026-04-05.json`, the client code in lower case. **`client_id` replaces it.** Both filenames read off disk on 2026-09-02.

   10d.58 **BUILT.** **The archive JSON's `code` field goes.** `testst-archive-2025-04-06-to-2026-04-05.json` carries `"client": "Test Sole Trader"` and `"code": "TESTST"`. **`client_id` replaces the code and `client` stays as the display name.** **Its `chartSource` and `chartPublished` keys are untouched**, being `"SALE_OF_SERVICES.csv"` and `"2026-09-01 11:11 BST"` in that file: they are the provenance stamp that makes the archive a point-in-time record rather than a report, and they are why re-running a summary under a republished chart does not reproduce it.

   **The client code's remaining carriers on the Desktop side. Amendment 176.** Found 2026-09-02 by enumerating every `.code` occurrence in `IntelliBooks-Desktop-v3.html` and tagging each with its enclosing function. **100 occurrences: 65 are four-digit chart account codes and are not touched, 35 are client codes.** None of these nine was named by any earlier sub-step or by `PROMPT_intellibooks_2026-09-01_step10d_desktop.md`.

   10d.59 **BUILT.** **`scanReview()` at `IntelliBooks-Desktop-v3.html:2404` reads `Intellibills\Review\` by client code and takes `client_id`.** The line is `try{dir=await getDir([PIPE_DIR,"Review",c.code],false);}`. **This is the Desktop half of 10d.54**, which rekeys that folder on the pipeline side, and without it the pipeline writes `Review\Client_004\` while Desktop looks in `Review\TESTST\`. **That is the 2026-09-01 TESTST failure exactly**, and `scanReview()` fails silently, so the Review list would simply be empty. **It is also the path amendment 172 records as the one check step 10a could not run.**

   10d.60 **BUILT.** **The resolution note stops carrying `client_code`, and both writers change.** `fileReviewReceipt()` at `:2519` writes `client_code:c.code,` and `discardReviewReceipt()` at `:2562` writes `client_code:(c&&c.code)||r.clientCode||"",`. **Both become `client_id`.** **This is section 12's contract**, which was built in halves with five points disagreeing, and `worker/resolution/service.py` is the reader. **The Desktop plan named only `filed_path` at `:2532` and recorded that the old `filed_path` had been at `:2519`, so the line read as dealt with when what sits there now is a different field.**

   10d.61 **BUILT.** **The filed receipt's sidecar carries `client:{code,name}` at `:2501` and takes `client_id` plus the display name.** Written beside the filed image in the client folder, and the same shape question as the inbox sidecar at 10d.12.

   10d.62 **BUILT.** **The books file's own `code` field and the cache key derived from it.** `emptyBooks()` at `:590` writes `code:client.code` into every books file; `saveBooks()` at `:706` uses `idbSet("cache:"+books.code,books)`. **Both take `client_id`.** 10d.2 changed the books filename and left the field inside the file.

   10d.63 **BUILT.** **`exportPracticeBackup()` stops keying the backup on the code**, at `:3121`, `:3122` and `:3129`, the last being `const codes=new Set(practice.clients.map(c=>String(c.code).toUpperCase()));`.

   10d.64 **BUILT.** **Three download filenames stop carrying the client code**: the handover pack prefix at `:2003`, the transactions CSV at `:2850`, and the client backup at `:3074`. **Same shape as 10d.57**, which got a sub-step for the HMRC filenames.

   10d.65 **BUILT.** **The two `ib3_lastYear_` localStorage keys at `:1816` and `:1823` take `client_id`.** Per-browser, and the only visible effect is that the remembered tax year is forgotten once.

   10d.66 **BUILT.** **`deleteClient()` at `IntelliBooks-Desktop-v3.html:1062` asks the operator to type the client's business name to confirm a permanent deletion. Paul's ruling, 2026-09-02, and written the same day** as change log item 52. **The status stays OUTSTANDING with every other 10d sub-step until the flip, per amendment 180: the code is written and the acts are not.** It matches on `client_name`, which is what that window shows, and not on `client_id`, whose five values differ by one digit. ~~makes the operator type the client code~~ ~~Paul's ruling needed and nothing is decided here.~~

   10d.67 **BUILT.** **`client_code` goes completely and appears nowhere. Paul's ruling, 2026-09-02**, and written into all three codebases the same day and the next. **The status stays OUTSTANDING with every other 10d sub-step until the flip, per amendment 180.** So the Code field at `IntelliBooks-Desktop-v3.html:959` and its duplicate check at `:975` come off the **Edit Client** window, and **sub-step 10e.4's wording is corrected rather than step 10d being narrowed**, even though 10e.4 is BUILT. **Amendment 111's field list, restated as section A of all three step 10d briefs, already said there is no `client_code` in the file, on any table or in any payload**, so the ruling confirms section A and corrects 10e.4. ~~Paul's ruling needed, and it is a conflict between two sub-steps rather than a defect.~~ ~~Either 10e.4's wording is corrected and the field comes off, or a display-only code survives on the client record and section A is amended to say so.~~

   **The client's email addresses get a writer. Amendment 185.**

   10d.68 **BUILT.** **The Edit Client window gains an Email addresses field, and `emails` gets its first writer in either product.** `emails` has been a client field since 10d.1 and amendment 111's field list, and no screen in either product could put anything in it, so all five records carried an empty array and the pipeline's email route could resolve no client at all. One comma-separated box in `IntelliBooks-Desktop-v3.html`'s Edit Client window, stored lower-cased and trimmed to match `address.strip().lower()` in `config.py:262`. **The uniqueness check is in that window and has to be**: `config.load_clients()` builds its index with `clients_by_email[address] = client_data`, a plain assignment, so two clients carrying one address means the last record read silently wins and that sender's receipts go to whichever client that is. **The pipeline cannot warn about it**, because it sees a registry rather than an edit. Step 10d's own prose settles the rule it enforces: one email address serves one client, and the array holds several addresses for one client rather than one address for several clients.

   **Why the sitting is now, measured rather than asserted.** On 2026-08-20 the whole of what stands behind this change is test data. **After the pilot it is a real client, a real capture link already on a phone, and a migration.** And the error the credential prevents is a receipt in the wrong client's records, which matters most once the clients are real.

10e. **BUILT 2026-09-07, all 15 sub-steps: 14 built and 10e.6 cancelled. Amendment 266, which built 10e.15 and with it the whole step.** ~~OUTSTANDING.~~ **The Firm Settings page is filled. Added 2026-08-20 by amendment 117.** Paul's stated priority. **Every firm variable appears on it**, including the ones that cannot be changed from the app: `RECEIPTS_ROOT`, `ONEDRIVE_USER` and the Azure app registration live on Netlify, and they appear **displayed and labelled as external** rather than omitted, so the page is a complete statement of what a firm consists of. The engineering constants stay off it, per amendment 108: the extraction engine, the model, the poll interval and the validation tolerance are not settings and a page of things not to change is worse than no page.

   **Client Settings gets its own tab, in the centre menu group, beside Client Data, named Client Settings. Decided 2026-08-21 by amendment 130, closing outstanding item 2.** Section 7 of `2026-08-20_LIST_settings_firm_and_client.md` carries the reasoning: every client setting is about whichever client is selected, which is what the centre group means, and ten of the seventeen that exist are reachable today only by leaving the client you are working on, through **Clients** then **Edit** on a row. **Two things it does not take.** Creating a client keeps the name ~~, the code~~ and the entity type in the Edit window, because `chartFor()` cannot build the books without the type. **The client code came off that window at 10d.67 and appears nowhere; corrected here 2026-09-04.** And **C16, the period lock date, stays on Client Data** in the **This Client's Data** card, because it is set once a quarter as part of closing a period and moving it would put the lock two clicks from the transactions it locks. ~~**Until the one registry exists**, C1, C2 and C3 live in `clients.csv` and the rest in `IntelliBooks-Practice.json`, so the page shows those three read-only rather than writing across two files.~~ **The one registry exists. Corrected 2026-09-04: step 10d built it, `clients.csv` is gone, `IntelliBooks-Practice.json` is superseded, and every client field including C1, C2 and C3 is on the client's record in `Intellibills\clients.json`. Nothing on the new tab needs to be read-only and nothing writes across two files.** See 10e.6.

   **The Practice Settings card moves here from the Clients tab**, with the capture app address and ~~, until `capture_token` replaces it, the upload key~~. **`capture_token` has replaced it. Corrected 2026-09-04: sub-steps 10d.5 to 10d.7 retired the shared `UPLOAD_KEY`, it was deleted from Netlify on 2026-09-03, and there is no key field to move.** **Two toasts in `copyCaptureLink()` name that card and change with it**: "Set the capture app address in Practice Settings first (Clients tab)" and "Set the upload key in Practice Settings first (must match UPLOAD_KEY on Netlify)". This closes what the Desktop session flagged on 2026-08-20, that Practice Settings and Firm Settings are two names for practice-level settings in adjacent menu items.

   **`vatScheme` is deleted rather than carried onto the new tab. Added 2026-08-21 by amendment 142, on Paul's decision, closing outstanding item 23.** Five places in `IntelliBooks-Desktop-v3.html`: the input at `:348`, the clear at `:850`, the read at `:861`, the write at `:884` and the shape comment at `:572`. The field goes from `IntelliBooks-Practice.json` with it. **No data is lost: all six clients hold an empty string, read from the file.** **It is here rather than anywhere else because this step decides what a client setting is**, and a field nothing reads should not be the first thing carried onto a page built to hold client settings. **The accounting argument, and it was the consultant session's rather than Paul's:** the box looks like the system knows the scheme, and a client on flat rate generally cannot recover input VAT on purchases while their receipts are being given VAT the standard way. **Attribution corrected 2026-08-23. Paul wanted to keep the field even though nothing read it, and was argued out of it by this session on a risk it raised.** **Amendment 148 then established the opposite rule**, that an unused field is not a defect and whether it is a risk is Paul's call, so the argument that removed this field would not be made again. He will ask for it when he wants it, and it will need fixed values rather than free text.

   **Decomposed into 15 sub-steps 2026-08-21 by amendment 151, for the reason amendment 137 gives about step 10f: this step had accumulated six bodies of work as prose and a paragraph cannot carry a status.** The folder decisions are new; everything above is restated as numbered lines without change.

   **The two settings pages.**

   10e.1 **BUILT 2026-09-07 by amendment 259.** The **Firm Settings** page is filled. **Every firm variable appears**, including the ones that cannot be changed from the app: `RECEIPTS_ROOT`, `ONEDRIVE_USER` and the Azure app registration live on Netlify and appear **displayed and labelled as external** rather than omitted.

   **What was built, and the shape follows 10e.3's Client Settings tab, which Paul accepted on 2026-08-31.** Six cards. **Intellibills Settings** carries the three fields IntelliBooks can write, being F5 the firm name, F6 the firm contact email and F10 the phone app address, each with a note saying what it does, plus one `Save`; then the firm id read-only, then the practice root line and the `Change Practice Folder` button. **IntelliBooks Settings** carries its heading and a sentence saying why it is empty. **Set in the pipeline's own configuration** names F2, F3, F4, F8 and F9 with no values, **because IntelliBooks cannot read `.env` and a box that cannot show a value must not look like one that can**. **Held outside both products** names F12, F13 and the Azure app registration. **Decided and not built yet** names F14, F15, F16 and F17, each with the sub-step that builds it. **Chart usage across clients** is unchanged and was carried over verbatim.

   **Three fields became reachable that were not.** F5 and F6 needed `Intellibills\firms.csv` edited by hand until today. `savePracticeSettings()` now writes all three of F5, F6 and F10 to `Intellibills\firms.json`, and `renderFirmSettings()` reads them back. ~~**A blank firm name is saved and reported rather than refused**, because it is the name a client sees on an automatic email and refusing would leave a wrong value uncorrectable; both functions in `worker/email/alerts.py` already handle an empty name deliberately.~~ **Overruled by Paul the same hour and rebuilt. Amendment 260: a blank in any of the three is refused, and the `Save` button is disabled so it cannot be pressed. `firmSettingsGuard()` holds the rule and `savePracticeSettings()` calls it, so there is one copy of it and two layers of enforcement.**

   **The firm id is displayed and is not editable**, which is a judgement rather than an instruction: section 5 of `2026-08-20_LIST_settings_firm_and_client.md` excludes it as identity, and 10e.1 asks for a complete statement of what a firm consists of. The pipeline refuses a client record whose firm id it does not know, so a box that could change it would be a box that breaks the registry.

   **F7 and F11 appear as two rows that stopped being settings**, named so nobody looks for them: F7 is now the alert mailbox at F4 plus the firm name at F5, per 10d.36, and F11 does not exist, per 10d.5 to 10d.7.

   10e.2 **BUILT 2026-09-07 by amendment 259.** The engineering constants stay off it, per amendment 108. They are section 4 of `2026-08-20_LIST_settings_firm_and_client.md`, ~~`S1` to `S11`~~ **`S1` to `S10`, and the range is narrowed here rather than in passing: `S11`, the Azure app registration, is named on the page because 10e.1 names it explicitly among the external ones. Those two sub-steps contradicted each other on that one row and 10e.1 is the more specific instruction.** **A page of things not to change is worse than no page.**

   **What "stay off it" was built as, so it can be checked.** No `S` row is laid out as a row, and no variable or value of `S1` to `S10` is on the page: `EXTRACTION_ENGINE`, `OPENAI_MODEL`, `gpt-4o`, `POLL_INTERVAL_SECONDS`, `_VAT_TOLERANCE`, `rate_tol`, `pipeline_version`, `OPENAI_API_KEY`, `IMAP_PASSWORD` and `SMTP_PASSWORD` are all absent, each checked by name. **What the page does carry is one sentence saying they are deliberately not here**, naming the categories and not the settings, because amendment 156 records Paul's point that an absent field reads as a decision when nothing says why. **A sentence explaining an absence is not a page of things not to change.**

   10e.3 **BUILT 2026-08-31.** A **Client Settings** tab sits in the centre menu group beside **Client Data** in `IntelliBooks-Desktop-v3.html`. It carries VAT registered, Confirm mode, PHV platforms, Year end, MTD client, MTD quarter basis and the Balance sheet tickbox, each saving on change. A second card names what is set elsewhere. ~~OUTSTANDING. **Client Settings** gets its own tab in the centre menu group beside **Client Data**. Amendment 130.~~
   10e.4 **BUILT 2026-08-31.** The **Edit Client** window now holds the business name, ~~the client code,~~ the client type and the partners, and nothing else. **`client_code` comes off this window at 10d.67, on Paul's ruling of 2026-09-02 that it goes completely and appears nowhere. This sub-step stays BUILT: it describes what was built on 2026-08-31 and step 10d changes it.** Everything a person changes afterwards moved to the new tab. ~~OUTSTANDING. Creating a client keeps the name, the code and the entity type in the **Edit** window, because `chartFor()` cannot build the books without the type.~~
   10e.5 **BUILT 2026-08-31. Nothing moved, which is the requirement.** C16, the period lock date, is still on **Client Data** in the **This Client's Data** card, and the new tab says so in words rather than leaving it to be searched for. ~~OUTSTANDING. **C16, the period lock date, stays on Client Data**, because it is set once a quarter as part of closing a period and moving it would put the lock two clicks from the transactions it locks.~~
   10e.6 **CANCELLED 2026-09-04.** ~~OUTSTANDING. Until the one registry exists, C1, C2 and C3 live in `clients.csv` and the rest in `IntelliBooks-Practice.json`, so the page shows those three **read-only** rather than writing across two files.~~ **Its premise is gone rather than its requirement being met: step 10d built the one registry.** `clients.csv` is renamed `clients.csv.superseded-2026-08-20` and `IntelliBooks-Practice.json` is renamed `IntelliBooks-Practice.json.superseded-2026-09-01`, both read off disk on 2026-09-04. **Every client field is one record in `Intellibills\clients.json`, which IntelliBooks writes and the pipeline only reads**, so C1, C2 and C3 are editable on the Client Settings tab like every other client field and no field is split across two files. **Cancelled rather than closed, because nothing was built for it and nothing should be.** If Paul wants any of the three shown read-only for a different reason, that is a new decision.
   10e.7 **BUILT 2026-08-31.** The **Practice Settings** card is gone from the **Clients** tab. ~~The capture app address and the upload key are now in the **IntelliBooks Settings** card on **Firm Settings**, with the practice root line.~~ **The capture app address, labelled `Phone app address` on screen, is in the **Intellibills Settings** card on **Firm Settings**, with the practice root line and the `Change Practice Folder` button. Moved out of the IntelliBooks Settings card on 2026-09-07 by amendment 257, on Paul's instruction, because section 6 of `2026-08-20_LIST_settings_firm_and_client.md` gives every firm setting to Intellibills and none to IntelliBooks. It sat under the wrong heading from 2026-08-31, when this sub-step put it there, until then. And there is no upload key: sub-steps 10d.5 to 10d.7 retired the shared `UPLOAD_KEY` and it was deleted from Netlify on 2026-09-03, which the paragraph above this sub-step list already records and this line did not.** **This sub-step stays BUILT: it describes what was built on 2026-08-31, and amendment 257 moves it.** The two toasts in `copyCaptureLink()` now name Firm Settings. ~~OUTSTANDING. **The Practice Settings card is removed from the Clients tab**, its capture app address and, until `capture_token` replaces it, its upload key moving to Firm Settings. **Two toasts in `copyCaptureLink()` name that card and change with it.**~~
   10e.8 **BUILT 2026-08-31.** `vatScheme` is deleted, not carried across. Gone from the **Edit Client** window, from `openNewClient()`, `openEditClient()` and `saveClientModal()`, and from the shape comment, which was rewritten to the true shape. Removed from `IntelliBooks-Practice.json` for all six clients, every one holding an empty string, backup `IntelliBooks-Practice.json.bak-before-vatscheme-removal`. ~~OUTSTANDING. `vatScheme` is deleted rather than carried onto the new tab. Amendment 142. Five places in `IntelliBooks-Desktop-v3.html`, being `:348`, `:850`, `:861`, `:884` and the shape comment at `:572`, plus the field in `IntelliBooks-Practice.json`. All six clients hold an empty string, so nothing is lost.~~

   **The two folders, and which is a setting. Amendment 151.**

   10e.9 **BUILT 2026-09-07 by amendment 258, and nothing was built for it: amendments 243, 244 and 252 had already satisfied it, and one clause of it had become false.** **The practice root is not a setting** ~~and does not appear on any page~~ **and it does appear on the Firm Settings page, read-only, in the pipeline's own words. Corrected 2026-09-07: sub-step 10e.10 put it there deliberately by amendment 241 and this line was written before that decision.** The pipeline's configuration is the single authority for it, because the pipeline cannot read a file inside the practice root to learn where the practice root is. **IntelliBooks verifies; it does not decide.**

   **What each clause means, written out because this sub-step was a sentence and a sentence cannot be checked. Amendment 258.**

   **"Not a setting" means there is no field anywhere that sets it, and there is none.** `config.PRACTICE_ROOT` is `_required_root("INTELLIBILLS_PRACTICE_ROOT")`, so it has no default and the pipeline refuses to import without it, per amendment 245. Nothing in `IntelliBooks-Desktop-v3.html` writes a practice root into `firms.json`, `clients.json` or anywhere else the pipeline reads. **It is absent from rows F1 to F17's stored fields on the IntelliBooks side entirely**: what IntelliBooks holds is a browser folder handle in IndexedDB, which is a grant rather than a value, and F1 has said so since 2026-08-20.

   **"Does appear on the page" is now the true half, and it is a display and not a control.** `setRootLabel()` writes `Practice root: <practice_root>` into the Intellibills Settings card from `pipeline-status.json`, which is the pipeline stating its own configured value, and it warns rather than refuses when the browser's folder name differs, per Paul's ruling of 2026-09-06 at 10e.10. **A firm being unable to see which folder its pipeline is configured for was the fault 10e.10 fixed**, so an instruction not to show it would have to undo that.

   **"IntelliBooks verifies; it does not decide" holds, and what it does decide is worth stating so nobody reopens this.** `verifyRoot(h)` opens `Intellibills` without create and refuses any folder holding neither `pipeline-status.json` nor `clients.json`, creating nothing on a refusal, per 10e.11. What IntelliBooks does choose is which granted folder **it itself** opens, through `pickRoot()` and the `Change Practice Folder` button at 10e.13, **and it cannot do otherwise: a browser can only reach a folder a person has handed it, and it can only walk down from there.** That choice writes nothing the pipeline reads and moves no file. **So the two products can be pointed at different folders and the banner and the label are what say so.** That is the accepted position and not an open question.

   **The check, so this sub-step can be failed rather than only agreed with.** It is satisfied while all three hold: no control in either product writes a practice root value; the page shows the pipeline's `practice_root` and not the browser's folder name; and a folder that the pipeline has never run in is refused with nothing created in it.

   10e.10 **BUILT 2026-09-06 by Claude Code, commit `e546d80`. Amendment 244.** **`pipeline-status.json` carries the practice root.** ~~OUTSTANDING. It holds four fields today and no path.~~ It holds five. IntelliBooks displays the practice root from the pipeline's own words rather than from a browser folder name, which `setRootLabel()` does since amendment 243. **The field is named `practice_root` and its value is `str(config.PRACTICE_ROOT)` as configured, not resolved and not normalised. Fixed 2026-09-06 by amendment 241**, because it is the one word two products built by two sessions have to agree on. **Written on every cycle including a failed one**, which `app.py`'s `finally:` block already gives. **Three tests guard it**, being the five-key shape, the value on a successful run and on a failed one, and a third that Claude Code added because the first two could not catch a `.resolve()`. **The live file holds five keys from the next pipeline run onwards; there is no migration.** The brief was `PROMPT_claude_code_2026-09-06_pipeline_status_root.md`, now in `archive\`.
   10e.11 **BUILT 2026-09-06. Amendment 243.** **The folder gate checks before it accepts.** `verifyRoot(h)` opens `Intellibills` **without create** and reads the pipeline's own files from it. **A folder that fails is refused, the gate stays open, and nothing is stored and nothing created.** **Paul's ruling of 2026-09-06: two files count, `pipeline-status.json` or `clients.json`**, tried in that order, because requiring the status file alone would refuse a practice folder restored onto a machine with no pipeline installed. Neither is ever created by IntelliBooks, so either still proves the pipeline has run there. **`unlockRoot()` and the boot path verify but do not refuse.** ~~OUTSTANDING. On being given a folder, IntelliBooks reads `Intellibills\pipeline-status.json` from it and compares the recorded path. **If the read fails the folder is refused and nothing is stored or created.** Today `pickRoot()` at `:741` stores the handle and calls `afterRoot()` first, which creates `IntelliBooks\` and writes a blank practice file, so the order reverses: pick, check, then store and load.~~ **The struck description of the fault was two refactors out of date and is corrected by amendment 243. No blank practice file is written at all**, `PRACTICE_FILE` having been retired at 10d.52. **What was created is `Intellibills\`**, by `pipeDir()` at `:823` with `{create:true}`, reached from `loadPractice()`. `IntelliBooks\Books\` is created later by `loadBooks()`. **The order still reverses and the fault was real; only its description had drifted.**
   10e.12 **BUILT 2026-09-06. Amendment 243.** **The banner stops explaining one failure with one cause.** It now names both: the pipeline has not completed a cycle, or this is not the practice root. ~~OUTSTANDING. `:759` reads "Pipeline status unknown (no status file yet). If the pipeline is running, this appears after its next cycle", and the same read fails when the folder is the wrong one entirely.~~ **A second instance of the same fault was found and deliberately left alone**: `loadClients()` toasts that `clients.json` was not found and that Intellibills creates it at 10d.1, which blames the pipeline. **10e.11's gate makes that message true**, because a wrong folder can no longer reach it.
   10e.13 **BUILT 2026-08-31**, as part of 10e.7 rather than on its own: the **Change practice root folder** button went with the card that held it, and nothing replaced it. ~~Flagged, not fixed: `changeRoot()` is now defined and called by nothing. The first-run gate uses `pickRoot()` and `unlockRoot()`, not `changeRoot()`.~~ **FIXED 2026-09-07 by amendment 252. `changeRoot()` is called by a grey `Change Practice Folder` button in Firm Settings, in the ~~IntelliBooks Settings~~ **Intellibills Settings, moved there 2026-09-07 by amendment 257** card, below the line naming the current root.** It was left unwired for a week because `pickRoot()` then stored the handle before checking the folder; amendment 243 reversed that, so the button inherits the gate and a wrong pick creates nothing. ~~OUTSTANDING. **The Change practice root folder button at `:287` is deleted, not moved.** It was labelled for the practice root and intended for the client top folder, which is F17 and belongs on Firm Settings. **Nothing replaces it:** the first-run gate at `:2702` already asks for the folder when there is no handle, and stale-permission recovery already falls back to it.~~

   10e.14 **BUILT 2026-09-07 by amendment 265, all four pieces, and Paul confirmed on screen that a client's receipts still load.** **F17, the client top folder, is ONE stored field, an absolute path, and the folder name is its last segment. Paul's decision, 2026-09-07, amendment 261.** The field is `client_top_folder` on the firm record in `Intellibills\firms.json`, and **that key is fixed here because two products built by two sessions that cannot see each other have to agree on it**, which is the same reason amendment 241 fixed `practice_root`. ~~**F17, the client top folder, is two stored fields: a name and an absolute path.** Not one value with the name derived. **The two are checked against each other**, so a mistake in either is visible; derive the name and a wrong path becomes the name by definition.~~ **Superseded 2026-09-07 by amendment 261, on Paul's question: when would the two ever disagree? Only if somebody typed one of them wrongly, so the two fields were one fact stored twice and the check existed to catch mistyping a thing the operator was made to type twice. And the typo is already caught somewhere better: 10e.15 verifies the folder the operator GRANTS against the stored name, so a path ending in the wrong word fails at the grant.** It is an absolute path in its own right and **need not sit under the practice root**. `config.py`'s `CLIENTS_ROOT = PRACTICE_ROOT / "Clients"` stops composing it ~~`config.py:42`'s~~ **line number dropped 2026-09-07: `CLIENTS_ROOT` has moved twice since this note was written and the name is the only part that does not move. ~~`:74`~~ read at `:117` on 2026-09-07, recorded here as the reason the number is not being carried rather than as a number to cite**, and the literal `"Clients"` goes from ~~seven places in `IntelliBooks-Desktop-v3.html`~~ **eleven code sites in `IntelliBooks-Desktop-v3.html`, plus six hardcoded on-screen messages and one code comment that carry `Clients\` as their own text. Enumerated and corrected 2026-09-07 by amendment 256; the seven was never enumerated.**

   **The eleven code sites**, read in `IntelliBooks-Desktop-v3.html` on 2026-09-07 and printed whole rather than counted: `:798`, `:1001`, `:1696`, `:1712`, `:2473`, `:2540`, `:2705`, `:3253`, `:3536`, `:3925` and `:4264`. **Ten are `getDir(["Clients", ...])` calls. The eleventh, at `:798`, is `clientFolderPath()`**, the shared helper built by change log item 51 of `IntelliBooks\App\Docs\IntelliBooks-Change-Log.md`, **and it is the one to change first**: its seven callers come right with it, being six messages and one stored `filed_path`. **The numbers are given because they were read today and they will move; what to search for is the literal inside a `getDir` array**, which is a shape and not a number.

   **The six on-screen messages, which nothing scheduled until now. Added 2026-09-07 by amendment 256.** They carry `Clients\` as text rather than calling `clientFolderPath()`, so the original wording of this sub-step did not reach them, **and step 10f does not touch them either**: 10f.11, 10f.13, 10f.14, 10f.25 and 10f.27 all write into `Clients\` and none of them changes where the top folder comes from. The six: the error thrown when a client has no `client_folder_name`; **the first-run gate text, which is the first sentence a new operator reads**; `verifyRoot()`'s refusal, "the folder that holds `Intellibills\` and `Clients\`"; the **Change Practice Folder** confirm box; the DELETE client confirm box; and the toast after a client is deleted. **A seventh occurrence is a code comment and is corrected with them.**

   **All four pieces, 2026-09-07, amendment 265.** ~~Where this sub-step has got to, 2026-09-07, amendment 263. It stays OUTSTANDING and three of its four pieces are done.~~ **Piece 1, built**: `client_top_folder` is a field on the firm record and a box on the Firm Settings page, with the derived folder name shown beneath it. `Save` refuses a value that is not an absolute path. Amendment 262. **Piece 2, done**: Paul typed the value in at 16:32 BST and it was read back off `Intellibills\firms.json`. **Piece 3, built by Claude Code in `7b96a35` and `13e1c6a`**: `config.CLIENTS_ROOT` is the `client_top_folder` field off the single firm record, read by a new `_client_top_folder()`, with no default and no fallback to the old composition. **Piece 4 is the only one left**, being the eleven code sites and six messages in `IntelliBooks-Desktop-v3.html`, and it is the consultant session's. ~~**Piece 1 is built**: ... **Piece 2 is Paul typing the value once.** **Piece 3 is the pipeline reading it instead of composing `CLIENTS_ROOT`, and that is Claude Code's**, so it is a brief.~~ **Superseded by amendment 263; the original wording is in the amendment record.**

   **What piece 3 refuses, so nothing has to read the code to find out.** No firm in `Intellibills\firms.json`; more than one firm; the field absent, empty or blank; or the value not an absolute path. Each raises at import naming the field and the file, and the message says the Firm Settings page is where to set it. **The record is taken because there is exactly one, never by naming `DEFAULT_FIRM_ID`**, which sub-step 10d.19 spent a step stopping being a fallback.

   **Section 18.2b's freeze was not touched and needed no ruling.** The change stayed inside `config.py`, which is where the composition lived, and `get_client_directory()` still reads `config.CLIENTS_ROOT` and simply gets a different value. **Verified here rather than taken: `worker\filing.py` and `app.py` are byte for byte and modification-time identical to the copies staged before Claude Code started.**

   **What is deliberately out of scope, ruled 2026-09-07 by amendment 263 so the question is not asked a third time. Docstrings and code comments are not what this sub-step's completion test is about.** `get_client_directory()`'s docstring still opens "This client's folder under Clients", and the same phrase sits in prose in `app.py`, `worker\database\repository.py`, `worker\extraction_pipeline.py` and `worker\resolution\service.py`, 14 lines in five production files. **The completion test says no path-composing literal and no operator-facing sentence, and a docstring is neither.** **Narrowing the 18.2b freeze for a comment would not be worth the ruling it costs**, which is Paul's decision of 2026-09-07 on Claude Code's second flag.

   **They are inside this sub-step rather than a sub-step of their own.** A message naming a folder the setting no longer points at is the same change as the code that opens it. Split them and the code goes right while the screen still says `Clients\`, which is the fault amendment 250 records in another form: a document that describes what the code does and not what is on screen. **So 10e.14 is complete when no path-composing literal and no operator-facing sentence in either product asserts that the client top folder is called `Clients`.**

   10e.15 **BUILT 2026-09-07 by amendment 266, and NOT in the shape this line describes. Paul's ruling, 2026-09-07: the second permission is asked for only when it is needed.** ~~Two permissions on first run~~ is struck below and the reason is a browser constraint established while building it: `showDirectoryPicker()` opens only from a click, so a second permission cannot be taken at start-up and has to be a button either way. **Making every firm press it for a folder the app can already reach is friction for no gain**, so the order is a stored grant, then the folder of that name inside the practice root opened WITHOUT create, then a refusal naming the button. **The check is unchanged in both directions: the folder's own name must equal the name off `client_top_folder`.** **IntelliBooks takes a second folder grant, for the client top folder**, because the browser can only walk down from a folder it has been given and F17 may sit outside the practice root. **It is verified by name only**, against ~~10e.14's name field~~ **the last segment of 10e.14's `client_top_folder` path, corrected 2026-09-07 by amendment 261 when the name stopped being a field of its own**, because nothing of ours is written there to prove it. Two permissions on first run: our storage, and the firm's filing structure.

   **This grant is the check that 10e.14 no longer does with a second field, and it is the stronger of the two. Amendment 261.** A wrong `client_top_folder` ends in a word that is not the folder the operator hands over, so the grant refuses it. **Two boxes agreeing with each other proves only that the same thing was typed twice.**

   **The grant itself is local only and does not survive the move to AWS**, because there is no browser folder dialogue in a service. **What it is checking is not local**: see 18.11.

   **F17 lives in a settings file in `Intellibills\`**, alongside `clients.json`, `firms.json` and `pipeline-status.json` (~~`clients.csv`~~ **corrected 2026-09-04**), because that is somewhere the pipeline reaches by configuration and IntelliBooks already reaches through its grant. **`Intellibills\firms.json` is that settings file**, built at sub-steps 10d.51 and 10d.52, and it holds ~~four fields today: `firm_id`, `name`, `email` and `phone_app_url`~~ **five from 2026-09-07: `firm_id`, `name`, `email`, `phone_app_url` and `client_top_folder`, which amendment 262 added. The first four were read off the live file on 2026-09-07 before the fifth was written.** **Where the setting is stored and what its value points at are different questions**, and the value being an absolute path elsewhere does not change where it is recorded.

   **After 10d and not before**, because 10d changes where firm configuration is stored, and a page built first would be built twice.

10f. **BUILT 2026-09-11, and the step is COMPLETE: 36 sub-steps built, none outstanding, 2 moved, of 38. Amendment 322, which closed 10f.38 on a live test.** ~~OUTSTANDING.~~ **The publish step, the client folder copy, and duplicates. Amendments 104, 106, 107, 122, 136 and 137, which land together.** Amendment 106 already requires the publish step and the client folder copy to land together. **Amendment 107 joins them because its parts have nowhere to work until they do:** a possible duplicate is detected on the extracted values, marked, routed to Review and never published, which needs the publish step; it is marked as such on the Receipts tab; and deleting one removes its copy from the client folder, which needs that copy to exist.

   **Decomposed into 29 sub-steps 2026-08-21 by amendment 137, a thirtieth added the same day by amendment 140, a thirty-first added 2026-09-07 by amendment 267, a thirty-second and thirty-third added 2026-09-08 by amendment 269, and a thirty-fourth by amendment 271, a thirty-fifth by amendment 274 and a thirty-sixth by amendment 278. It was not a readability exercise.**

   **Six sub-steps are split into a brief of their own and are being built ahead of the rest. Paul's decision, 2026-09-07, amendment 267.** 10f.18, 10f.19, 10f.20, 10f.21, 10f.22 and 10f.23, in `PROMPT_claude_code_2026-09-07_step10f_duplicates.md`. **None of the six depends on the publish step or on the client folder copy**, and 10f.18 is a live fault rather than a missing feature. **They stay OUTSTANDING here until the report is read and the claims in it are verified against the code.** This step named amendments 104, 106 and 107 and **described none of 104's ten parts, none of 106's five, and two of 107's four**, so a brief written from it would have built about a third of what was decided. The reasoning is not repeated below: **amendments 104, 106, 107, 122, 136 and 137 carry the decisions**, 18.2b the client folder rules, 18.3 the handoff and section 0.5.1 of `2026-07-31_PLAN_reset_and_restructure.md` the acceptance test. Read those before starting, not the summary.

   **The publish step. Amendment 104.**

   10f.1 **MOVED 2026-09-10 to item 177 of `2026-08-20_LIST_outstanding_items_and_decisions.md`, section 7, on Paul's instruction, amendment 314. It is deferred and not cancelled, and it is on screen as C20 in the `Decided and not built yet` card on the Client Settings page.** ~~OUTSTANDING, and DEFERRED OUT OF STAGE 1. Paul's decision, 2026-09-08, amendment 279.~~ **The wording below is kept as the record of what it asked for.** One Receipt publishing destination per client, held as a column on the client record. **It is not built with 10f.2 and 10f.3 and it is not cancelled.** **There is exactly one destination today**, IntelliBooks internal, and 18.3 puts the other two at "much later" with no date, **so this column can only hold one value and setting it on every client would say the only thing it can say.** **A column whose every row holds the same value is not a setting yet.** It becomes one the day a second destination exists, which is also the day 10f.2's two-level split starts meaning something. **What is built instead is F14 and F15**, per 10f.2 and 10f.3: the destination's address becomes a setting rather than a hardcoded ~~`IntelliBooks\Inbox\`~~ **`IntelliBooks\Incoming\`** path. ~~and the CSV switch is a real choice today.~~ **Struck 2026-09-08 by amendment 280: F15 and sub-step 10f.3 are deferred alongside this sub-step, so what is built is F14 alone.** **The reasoning against hardcoding, which was the other option: this project has spent three sub-steps removing literals of exactly that kind**, being 10a's folder constants, 10e.14's `Clients` and `config.CLIENTS_ROOT`, and putting one back to save a field is the trade that produced those sub-steps.
   10f.2 **BUILT, and the marker was stale rather than the work undone. Established 2026-09-10 by amendment 311, which read all three pieces.** ~~OUTSTANDING.~~ Each destination's address held separately, as an Intellibills firm setting. **Two levels:** which destination a client uses is a client fact, where that destination is reached is firm configuration.

   **The field, the shape and the key are fixed here. Paul's decision, 2026-09-08, amendment 280, and they are fixed for amendment 241's reason: two products built by sessions that cannot see each other have to agree on the word.**

   - **The field is `publish_destinations`**, on the firm record in `Intellibills\firms.json`. Snake case and descriptive, matching `practice_root`, `client_top_folder` and `phone_app_url`. **Plural because row F14 of `2026-08-20_LIST_settings_firm_and_client.md` reads Multi, one address per destination.**
   - **It is an object keyed by destination, and the internal destination's key is `intellibooks`.** 18.3 makes IntelliBooks one of three destinations with no privileges, and a third party would be keyed by its product, `xero` or `qbo`. **`internal` would make this one the odd member and would put back the privilege 18.3 removes.**
   - **The internal destination's value is a folder name relative to the practice root**, never an absolute path. 18.2a draws the folder inside the `IntelliBooks\` tree that 18.2 gives to IntelliBooks, so an absolute path could point outside a tree the design owns. **F17 is absolute for the opposite reason**, being a folder that belongs to neither module, per amendment 261 and 18.11.
   - **The object's values are heterogeneous by destination type and the key carries the type.** The internal value reads as a folder name and a future `xero` value would read as a URL, so no value has to declare its own kind.
   - **Paul's value, set at piece 2 of stage 1:** `{"intellibooks": "Incoming"}`
   - **`firmSettingsGuard()` in `IntelliBooks-Desktop-v3.html` gains per-field rules and F14 is not in the required set.** Blank is allowed until Paul sets it, and a value present must be a valid folder name. **The enforcement that matters is the pipeline's:** sub-step 10d.19 stopped `DEFAULT_FIRM_ID` being a fallback and amendment 245 made the roots required, so piece 3 refuses to start without a value. **Save staying pressable is correct**, because a disabled button would move the enforcement to the weaker of the two places.
   - **PIECE 3 IS BUILT, 2026-09-09, amendment 287**, by Claude Code in five commits. **The item carries 22 keys: the sidecar's 20, plus `image_base64` and `image_media_type`.** **Those two names are fixed and Desktop must match them exactly at stage 2**, for amendment 241's reason. They are `IMAGE_KEY` and `MEDIA_TYPE_KEY` in `worker\publish.py`. **The trigger is one call site, inside `if validation.status == 'ok'` in `process_extraction_result()`**, verified from the syntax tree.
   - **PIECE 1 IS BUILT, 2026-09-08, amendment 281.** The box, the guard rule, and the field's reader and writer are in `IntelliBooks-Desktop-v3.html`. **This sub-step stays OUTSTANDING because piece 3, the pipeline's reader, is not built.** Piece 2 is Paul typing the value once.
   - **One fact recorded and not decided.** `Intellibills\firms.json`, read whole on 2026-09-08, is an envelope holding `version` and `firms`, with one record carrying `firm_id`, `name`, `email`, `phone_app_url` and `client_top_folder`. **No document named the `version` key until now.** This sub-step adds a sixth key to that record, and whether `version` moves with it, and whether `config.load_firms()` reads it at all, is open. **Answered in part 2026-09-08 by amendment 281.** `saveFirm()` in `IntelliBooks-Desktop-v3.html` re-reads the file, replaces the one firm record and writes the whole envelope back, so **`version` survives a save from this page untouched and nothing bumps it**, proved by driving the real function rather than by reading it. **And `config.load_firms()` copies every key off the record with `dict(record)` and validates none of them**, read on 2026-09-08, so an added field cannot stop the pipeline starting. ~~What stays open is whether `version` SHOULD move when the record's shape changes, and that is Paul's.~~ **CLOSED 2026-09-09 by amendment 282, Paul's decision: `version` stays at 1 and means nothing yet, and it moves the first time a reader has to behave differently for two shapes of the file. Adding a key that older readers ignore is not that.**
   10f.3 **MOVED 2026-09-10 to item 178 of `2026-08-20_LIST_outstanding_items_and_decisions.md`, section 7, on Paul's instruction, amendment 314. That item records it as RAISED WITHOUT A PURPOSE: amendment 104's one sentence is its whole origin and nothing says what the CSV holds, where it goes or when. The live requirement is an on-demand per-client export and is not this.** ~~OUTSTANDING, and DEFERRED WITH 10f.1. Paul's decision, 2026-09-08, amendment 280.~~ An always-on CSV export is a separate switch and **not** a second destination. **It is not built with 10f.2 and it is not cancelled**, and **F15 stays in the Decided and not built yet card on the Firm Settings page.**

   **Why it is deferred.** Nothing in 18.3, in this sub-step, in `2026-08-20_LIST_settings_firm_and_client.md` or in `2026-09-08_PLAN_publish_step.md` says what the CSV contains, where it is written or when. **A switch the pipeline reads with no default and with nowhere to write is a field the reader cannot honour**, which is amendment 279's reasoning about 10f.1 applied to a switch. **And its customer is a standalone Intellibills firm, which does not exist yet.** **So piece 1 of stage 1 is F14 alone.**
   10f.4 **BUILT 2026-09-09 in `8762250`, amendment 287.** ~~OUTSTANDING.~~ **`config.INTELLIBOOKS_PUBLISH_DIR` is that one folder, composed from the practice root, `IntelliBooks` and F14's leaf, and it is one of the six mkdir targets.** The inbox is **one folder**, ~~`IntelliBooks\Inbox\`~~ **`IntelliBooks\Incoming\`**, not one per client. Reversed a same-day proposal: per-client folders leak the receiver's internal organisation into the sender.

   **RENAMED 2026-09-08 by amendment 280, before the folder exists. Paul's decision and Paul's name.**

   **Why, and it is free exactly once.** `Intellibills\Receipt Inbox\` already exists in both codebases and `importToInbox()` in `IntelliBooks-Desktop-v3.html` already opens it, and **at stage 2 of `2026-09-08_PLAN_publish_step.md` Desktop opens both folders.** **18.2a's own precedent is `logs\runs.ndjson` against `data\run.log`**, which also had different parents and still cost time and still became a trap. The folder does not exist on disk, so the rename costs nothing today and is never free again.

   **`Publish Inbox` was considered and rejected.** Two names ending in the same word are less distinct than the pair we already have. **`Incoming` shares no word with `Receipt Inbox`, and it matches its neighbours in that tree**, which are single plain words: `Books\`, `Charts\`, `Backups\` and `Delivery\`.

   **How the collision surfaced, disclosed because the measurement that hid it was the consultant session's.** Section 3 of `2026-09-08_HANDOVER_consultant_session_18.md` recorded the literal `"Inbox"` as zero occurrences in `app.py`, `config.py`, `worker\` and `IntelliBooks-Desktop-v3.html`. **That is true of an exact string literal and false of the substring:** 13 occurrences across those four files, counted as characters on 2026-09-08 with every matching line printed whole, all of them `Receipt Inbox` or `importToInbox`. **The conclusion that `IntelliBooks\Inbox\` is in neither codebase holds.** A narrower measurement read as an answer to a wider claim is the trap section 11 of that handover now carries.
   10f.5 **BUILT 2026-09-09 in `c8a453c`, amendment 287.** ~~OUTSTANDING.~~ The client identity travels **inside the item**, never in a folder name. **`build_item()` copies the sidecar, which carries `client_id`, and the folder is flat, so there is no folder name to read a client from.**
   10f.6 **BUILT 2026-09-09 in `c8a453c`, amendment 287.** ~~OUTSTANDING.~~ **22 keys: the sidecar's 20, plus `image_base64` and `image_media_type`. Compact and pure ASCII, so a reader that guesses an encoding cannot mangle a supplier name.** An item is **one JSON file per receipt with the image embedded as base64**. Not an image plus a sidecar: amendment 57's pairing by filename produced 23 ghost receipts and a 6.1 MB books file.
   10f.7 **BUILT 2026-09-09 in `c8a453c`, amendment 287.** ~~OUTSTANDING.~~ Written by **temp-name-and-rename**, so a half-written file can never be read. **`{receipt_id}.{uuid}.part`, flushed and `fsync`ed, then `os.replace()`d to `{receipt_id}.json`. The part name does not end in `.json`, so a reader globbing the folder cannot see it, and it is removed on any failure.** `os.replace()` rather than a rename, because on Windows a rename over an existing file raises.
   10f.8 **BUILT 2026-09-09, amendment 289.** ~~OUTSTANDING.~~ **`drainInbox()` runs from the two places `scanFiledReceipts()` already runs from, being after a client is selected and when the Receipts tab is shown, and NOT from `scanAllFiledReceipts()`'s per-year loop.** IntelliBooks **drains automatically when a client is opened**, matching what `scanFiledReceipts()` does today so it needs no new habit.
   10f.9 **BUILT 2026-09-09, amendment 289.** ~~OUTSTANDING.~~ **`Check Inbox` in the Receipts tab toolbar, with a count beside it that reports what is WAITING, meaning this client's items not already in the books, so it falls to nothing as they land and a number that will not fall is the sign a removal failed.** A **visible manual control with a waiting count**, because a drain that fails halfway has no retry today short of switching client and back.
   10f.10 **BUILT 2026-09-09, amendment 289, and it caught a design decision going the wrong way rather than being the wording change it looked like.** ~~OUTSTANDING.~~ Section 0.5.1's check 5 is reworded: with one folder, "the inbox is empty" must read **empty of that client's items**.

   **The client folder copy. Amendments 106 and 122.**

   10f.11 **BUILT 2026-09-09 in `7180d40`, amendment 297.** ~~OUTSTANDING.~~ The copy into `Clients\` is **Intellibills' own function and the only writer**, `write_client_copy()` in `worker\client_copy.py`, **image only per 18.2b and once per receipt**. `file_receipt()` is deleted.
   10f.12 **BUILT 2026-09-09, amendments 294 and 297.** ~~OUTSTANDING.~~ **The box is on Firm Settings, Paul's value is `publish`, and the pipeline reads `client_copy_trigger` with no default.** **`post` writes nothing and says so once per run**, because it needs the Post-time message from IntelliBooks Desktop, **which is sub-step 10f.37, given its own number 2026-09-10 by amendment 315.** ~~because it needs 10f.16's message, which does not exist.~~ **That was wrong and so was 10f.16's mirror of it: each named the other and neither held the work.** **Three triggers, set per firm: on successful publish, at Post, or never.** Standalone Intellibills can only offer the first, which is why the function had to move.
   10f.13 **BUILT 2026-09-09 in `7180d40`, amendment 297.** ~~OUTSTANDING.~~ **All three callers are gone and the recovery sweep publishes rather than files.** **Stop `file_receipt()` writing into `Clients\` on arrival.** Amendment 73 decided this on 2026-07-30 and no step ever carried it, which is how the code came to be still filing there on 2026-08-20.

   **THREE CALLERS, not one. Corrected 2026-09-08 by amendment 278, from the syntax tree.** This sub-step named the arrival write and the code has three writers into `Clients\`:

   | Caller | Function | What it is |
   |---|---|---|
   | `worker\extraction_pipeline.py` | `process_extraction_result()` | The arrival write, which is what this sub-step named |
   | `worker\resolution\service.py` | `resolve_receipt()` | A completed review item, which is 10f.14's subject on the Desktop side |
   | `app.py` | **`_file_unfiled_ok_receipts()`** | **A recovery sweep, and nothing in this document records it** |

   **The sweep is the one that was missing and it changes what the other sub-steps have to decide.** It runs at the top of every poll, calls `repo.get_unfiled_ok_receipts()`, and for every receipt that is `ok` with a NULL `filed_path` it categorises the receipt and files it into the client folder, logging "recovering N validated receipts that are not yet filed". **So a receipt that reaches `ok` unfiled by any route is filed within one poll interval by something no design document mentions.**

   **PAUL'S DECISION, 2026-09-08: the sweep survives the publish step, repointed. Option B of three.** It becomes "publish anything that was read and never published" rather than "file anything that was read and never filed". ~~Delete it, because nothing files on arrival any more~~ and ~~leave it filing, which contradicts this sub-step~~ were both rejected. **The reasoning is his and it is the argument against assuming a new path cannot fail:** what the sweep protects against is a receipt that got read and then had nothing happen to it, through a crash part way through a poll, a folder that could not be created, or a defect in one path. **That risk does not disappear when filing becomes publishing. It moves.** **And this morning is the evidence rather than the hypothesis**: the fault sub-step 10f.32 fixed had been swept up by this function for weeks and nobody knew.

   **It needs the marker at 10f.36 to do its job**, because publishing only what was never published requires a record of publishing.
   10f.14 **BUILT 2026-09-09, amendment 299, and INCOMPLETE UNTIL THE PIPELINE HALF LANDS: amendment 306 records that removing `filed_path` from the resolution note broke section 12.2's contract, found live the same day.** **`fileReviewReceipt()` stops writing into `Clients\` at all.** It writes the books row and sends the resolution note; the pipeline makes the copy on the firm's trigger. A completed review item reaches the client folder by the same single route as any other receipt.
   10f.15 **BUILT 2026-09-09, amendment 299.** ~~OUTSTANDING.~~ **The Review queue stops touching the pipeline's folders.** `scanReview()` reads `IntelliBooks\Incoming\` and all four `removeEntry()` calls on the pipeline's files are gone. Review items are published to the inbox marked as needing review, and completion or discard returns as a resolution note. **The mechanism is three folders and a delete, not a read. Established 2026-09-08 from `IntelliBooks-Desktop-v3.html`:** it reaches into `Receipt Inbox`, `Resolutions` and `Review` with `getDir([PIPE_DIR, ...])`, and **calls `removeEntry()` on the pipeline's own Review files at four sites.** That is 18.9's three filesystem operations across a module boundary, counted rather than described. ~~The queue stays on screen where it is; only the mechanism changes.~~ **Corrected 2026-09-08 by amendment 271: only the mechanism changes HERE. The queue itself gains an all-clients view at sub-step 10f.34, which comes after this one.** The old sentence would have sent the next session looking for a screen that had not changed when it had.
   10f.16 **BUILT 2026-09-09, amendment 297, and read what it means here rather than assuming.** ~~OUTSTANDING.~~ **The handoff is closed and the client folder keeps filling on the `publish` trigger, which is what this sub-step protects against.** **The `post` trigger itself has no Desktop-to-pipeline message and writes nothing; that message is sub-step 10f.37, given its own number 2026-09-10 by amendment 315.** ~~that message is not built and is named at 10f.12.~~ **It was named at 10f.12 and 10f.12 named it here, so three places pointed elsewhere and none held it.** **The warning in `worker\client_copy.py` still names this sub-step and is corrected when 10f.37 is built.** **The Post-time write and the closure of the 18.3 handoff land together**, not separately. Close the handoff without the write and the client folders go quiet with nothing reporting it.
   10f.17 **BUILT 2026-09-09, amendment 301. BUILT here means already satisfied and now closed, so this section's status vocabulary stays one word and the counts stay enumerable.** ~~OUTSTANDING, and it is a guard against a misreading rather than work.~~ **Noted 2026-09-08 by amendment 278: `get_client_directory()` is present in `worker\filing.py`, takes `client_folder_name` and composes from `config.CLIENTS_ROOT`, so this sub-step is already satisfied and stays OUTSTANDING only because nothing has closed it.** **`get_client_directory()` is not removed.** Sub-step 10d.14 keeps it and repoints it. Named here because 18.2b's struck-through opening sentence is the reason anyone would think otherwise.

   **Duplicates. Amendments 107, 136 and 137.**

   10f.18 **BUILT 2026-09-07 in `0157561`.** **Both queries are scoped and the first JOINs rather than filtering, because `processed_attachments` carries no `client_id`. The brief said what to achieve and did not say how, and the how was found by reading `schema.py`.** ~~OUTSTANDING.~~ **`find_by_hash()` at `worker/database/repository.py:30` takes the client**, in both of its queries. Today it filters on the hash alone, so two clients who send the same file collide and the second one's receipt is discarded and credited to the first. Amendment 136.
   10f.19 **BUILT 2026-09-07 in `55d0595`.** **`client_id` was already a parameter of the calling function, so nothing new is looked up.** ~~OUTSTANDING.~~ **`find_by_transaction_loose()` at `worker/database/repository.py:598` takes the client.** One caller, `worker/extraction_pipeline.py:139`. **This is amendment 107's "same client" and it was dropped from this step**, which is why two clients sending one document would have reached Review instead of being no duplicate at all.
   10f.20 **BUILT 2026-09-07 in `af01ca8`.** **The site is the embedded-image path and it was the one without the guard, as the brief said.** ~~OUTSTANDING.~~ **`app.py:724` gains the `is_recorded_and_filed()` guard** that `app.py:905` and `app.py:1055` already have. Three call sites, one behaving differently.
   10f.21 **BUILT 2026-09-07 in `0095891`.** **`_move_inbox_pair_to_processed()` already existed and took the two callers, so no second disposal was written. `tests/test_capture_inbox_cleanup.py` was rewritten against the new behaviour and now drives a real `process_once()`, so it covers the two callers as well as the move.** ~~OUTSTANDING.~~ **Nothing gets deleted.** `_remove_inbox_pair()` at `app.py:146` calls `.unlink()`, and its two callers at `app.py:862` and `app.py:909` become moves to `Processed\`, the same as every other outcome at `app.py:1017`. Closes outstanding item 144. Amendment 137.
   10f.22 **BUILT 2026-09-07 in `274dda1`.** **Four routes were driven where this sub-step names three: the embedded-image path is a fourth code path and it is the one 10f.20 changed, so driving three would have missed the one that differed. The verdict is identical on all four and only the disposal differs.** ~~OUTSTANDING.~~ **The duplicate decision is identical on every arrival route**, email, phone and **Add Receipts**. What differs is only the disposal of the thing that arrived: an email is moved to `INBOX.Duplicates`, an inbox file is moved to `Processed\`, and in both cases it is kept.
   10f.23 **BUILT 2026-09-07 in `6994d3c`.** **No caller in production, none in `tests\`, and no dynamic access, re-enumerated with `.history\` excluded.** ~~OUTSTANDING.~~ Delete `find_by_transaction()` at `repository.py:43` and `find_by_transaction_no_date()` at `:50`. Neither has a caller in production or in `tests\`.
   10f.24 **BUILT 2026-09-09 in `20e4003`, amendment 305, and REWORDED the same day by amendment 303.** A possible duplicate is **detected on the extracted values**, marked `possible_duplicate`, **routed to Review and never turned into a books row without a person**. ~~routed to Review and never published~~ **Corrected by amendment 293, which made every status publish.** Review and stop, not warn and proceed. **THE WORK IS CLAUDE CODE'S FLAG 1: the check asks `is_recorded_and_filed()`, which reads `filed_path`, and after stage 4 that exists only on the `publish` trigger, so on `never` and `post` the check stops flagging anything. The marker that replaces it is a `published` row, at the one call site and not inside the shared helper.**
   10f.25 **BUILT 2026-09-09 in `20e4003`, amendment 305.** ~~OUTSTANDING.~~ **The client folder handles its own name collision** rather than renaming through it. **Identical bytes: skip. Different bytes: keep the suffix and flag.** Nearly free, because the filename is already `{date}_{supplier}_{gross}` so a semantic duplicate already collides; only the response was wrong.
   10f.26 **BUILT 2026-09-09, amendment 303, satisfied by 10f.28 rather than by work of its own.** ~~OUTSTANDING.~~ Possible duplicates are **marked as such on the Receipts tab**, by `validationPill()`, on the review row and the books row alike and from one mapping.
   10f.27 **BUILT 2026-09-10 AND TESTED LIVE 2026-09-11, amendment 318. Paul deleted a real receipt with the box ticked and the copy left the client folder.** ~~OUTSTANDING, IN PROGRESS, AND NO LONGER BLOCKED.~~ **NO LONGER BLOCKED. Widened 2026-09-10 by amendment 314 on Paul's decisions: an operator may delete ANY receipt, so the orphaned client folder copy is general and not a duplicate's problem.** The pipeline half is briefed as `PROMPT_claude_code_2026-09-10_discard_deletes_client_copy.md`; the Desktop half is the consultant session's and follows its report. **The case this sub-step describes cannot itself occur**: `copy_for_published_receipt()` returns None on any status other than `ok`, so a receipt marked `possible_duplicate` never gets a copy to delete. ~~BLOCKED ON THE CONSOLE, which is not built. Recorded 2026-09-09 by amendment 303 and NOT cancelled, on the same footing as 10f.1 to 10f.3.~~ **The console was never the only candidate: section 7 of `2026-08-18_BOUNDARY_two_products.md` makes the Receipts tab Intellibills' own front end.** **Deleting a possible duplicate removes its copy from the client folder**, performed by Intellibills' own front end, and **every write and delete is logged**. The log is not only an audit trail: two colliding receipts produce a file and a `-2`, and nothing in the receipt record says which is which.

   **Two smaller items, here for the same reason.**

   10f.28 **BUILT 2026-09-09, amendment 302.** ~~OUTSTANDING.~~ **`validationPill()` is now the only mapping and both rows call it.** The books-receipt pill ~~at line 1878 of `IntelliBooks-Desktop-v3.html`~~ printed the raw status string, so a filed receipt would read `possible_duplicate` with an underscore where a review item reads Possible duplicate. Reachable for the first time when the pipeline can file one.
   10f.29 **BUILT 2026-09-09 by Paul, amendment 291. BUILT here means run and passed, so that this section's status vocabulary stays one word and the sub-step counts stay enumerable.** ~~OUTSTANDING.~~ Section 0.5.1's acceptance test in `2026-07-31_PLAN_reset_and_restructure.md` is run. ~~which **closes amendment 75's interim and releases the rest of the 18.2b freeze.**~~ **Corrected 2026-09-09 by amendment 290, which split those into two events: the five checks release the freeze FOR STAGE 4'S WORK, and check 1, which is stage 4's completion test, closes amendment 75's interim.** See 10f.10 for the one check in it that had to be reworded first.
   10f.30 **BUILT 2026-09-10 by Paul, amendment 313. BUILT here means run and passed, the vocabulary 10f.29 fixed. Check 5 caught a real semantic duplicate twice, on two documents; the case path the fix repairs was NOT exercised, and amendment 313 records why it cannot be arranged on demand.** ~~OUTSTANDING, and part D of its second set is UNRUNNABLE AS WRITTEN, per amendment 304: it uses a folder `scanReview()` no longer reads and names it by client code rather than `client_id`. The rewrite lands with the duplicate work.~~ **The live checks that were waiting for the Receipts tab to settle. Added 2026-08-21 by amendment 140, closing outstanding items 7 and 9.** Two sets. **Change log items 19 to 23**, five Receipts-tab changes built in July and never tested live: items 19, 21 and 23 are exercised by ordinary use, item 20 needs the pipeline deliberately left stale, and item 22's tax-year dropdown needs more than one year on disk. **And part D of section 4 of `IntelliBooks\App\Docs\2026-08-20_REPORT_desktop_menu_groups.md`**, the duplicate pill, which change log item 39 cannot be recorded as passed without. **Both waited for the same reason and it stops applying here:** 10f.24 lets the pipeline produce a real possible duplicate for the first time, so part D needs no fake review item, and 10f.15 and 10f.28 change the mechanism and the pill that items 19 and 39 test. **Parts A, B and C of that check need no setting up and are not waiting on this step.**

   **One more, and it belonged to no sub-step until now. Amendment 267.**

   10f.31 **BUILT 2026-09-07 in `8e77e01`.** **Amendment 1 to the brief, arriving after 10f.18 had landed, and it cost nothing because it is 10f.18 applied to a second function.** ~~OUTSTANDING.~~ **`find_statement_by_hash()` in `worker/database/repository.py` takes the client. Paul's ruling, 2026-09-07 19:35 BST, and it is in the same brief as 10f.18 to 10f.23.** It filters on the hash alone in exactly the way `find_by_hash()` does, **so a statement sent by two clients collides and the second client's statement is credited to the first.** 10f.18 covers the receipt half and **no sub-step covered the statement half**, which is why this is a new number rather than a clause added to an existing one. **Its one caller is the duplicate-statement path in `app.py`'s folder-intake loop, which already holds the client, and that caller is in scope twice over**: 10f.21 names it as one of `_remove_inbox_pair()`'s two callers, so its disposal becomes a move into `Processed\` in the same edit. **`statements` holds 0 rows, so nothing on disk is affected and no migration arises.**

   **Two more, both found by the work of 2026-09-07 and neither belonging to any earlier sub-step. Amendment 269.** **10f.32 is worked first of everything left in step 10f, on Paul's instruction of 2026-09-08.**

   10f.32 **BUILT 2026-09-08 in `9dbb111`.** ~~OUTSTANDING.~~ **It was a deletion plus a call, as amendment 269 predicted: out went `validate()`, the success `save_extraction()`, the `"extracted"` log entry and three smaller calls, and in came the call with all 16 arguments. Verified from the syntax tree here, not from the report.** **The embedded-image intake path calls `process_extraction_result()`, like the other three intake paths.** **It does not today, established 2026-09-07 by parsing `app.py` and listing every call inside the `embedded_images` loop:** `process_extraction_result()`, `file_receipt()` and `categorise()` are all absent. **So a receipt arriving as a photo in the body of an email is extracted, validated and written to the database, and then nothing else happens to it.** It is `status = ok` with `filed_path` NULL. **It is never categorised, never copied into the client folder, and therefore never reaches IntelliBooks, and nothing anywhere reports it.** Three consequences follow from that NULL, each read in the code rather than inferred: the same photo sent twice is extracted twice, because every caller of `find_by_hash()` pairs it with `is_recorded_and_filed()`; `possible_duplicate` can neither be raised against it nor by it, because `find_by_transaction_loose()` requires `r.filed_path IS NOT NULL`; and nothing working from the client folder can find it.

   **Unexercised, and the figure is why this is a step rather than an emergency.** Read live from `C:\Intellibills\db\receipts.db` on 2026-09-07 with sqlite3 in read-only mode: **no receipt is `ok` with a NULL `filed_path`**, 16 receipts in total, 12 `desktop`, 1 `email`, 1 `phone` and 2 `other` both discarded, and the one emailed receipt arrived as an attachment and was filed. **It goes live the first time a client uses a share button rather than a paperclip, and it is silent when it does.**

   **It is a deletion plus a call rather than new machinery.** The loop already holds every argument the shared function takes, checked name by name against its signature: `receipt_id`, `extraction`, `file_path`, `filename`, `firm_id`, `client_id`, `source`, `message_id`, `att_id`, `file_hash`, the repository, the engine, `stats`, `run_id` and `pipeline_version`. **What goes is that path's own `validate()`, `save_extraction()` and `_log_receipt()` block; what arrives is the call, and its returned status feeds the routing built at amendment 268.**

   **Why it comes before the other 24 and not after.** It deletes a second copy of the intake logic, and **the duplication has already been paid for twice inside step 10f**: 10f.21 had to change two disposal callers and 10f.22 had to drive four routes where this step's own wording says three. **Sub-step 10f.13 is the sharper reason.** It stops the on-arrival write into `Clients\`, and that write is inside `file_receipt()`, which is inside the shared function. **Fold this path in first and 10f.13 changes one place; leave it and the rebuilt filing has to be written into two.** **The hard deadline is step 10i**, because that is when real clients start sending.

   **No new behaviour is designed here.** Everything the shared function adds is behaviour the other three paths already have: a receipt needing review goes to `Intellibills\Review\`, the semantic duplicate check runs, and an unresolved client files nothing and goes to Review per 10d.18. **Claude Code's report called this "a behaviour change with a design decision inside it" and the consultant session reads it as one path being brought up to the standard the other three already meet.** If a decision surfaces in the building, it goes to Paul rather than being taken.

   10f.33 **BUILT 2026-09-08 in `1b6d82e`.** ~~OUTSTANDING.~~ **Thirteen `move_email_to_folder()` calls became five, verified here from the syntax tree: one ranked move per email path, the two unknown-sender branches, and the no-attachments branch. Eight became recorded outcomes. Single-item behaviour is unchanged on all eleven path-and-outcome combinations, which the brief named as the main risk.** **One thing the brief got wrong is open at flag 1 below and it is the consultant session's error.** **One ranking for a multi-item email, applied on both email paths.** **Amendment 268 gave the embedded-image path a ranked move after its loop, worst outcome first. The attachment path has no ranking at all**, established 2026-09-08 by measuring indentation from the syntax tree: the attachment loop runs its four-branch routing block **inside** the loop, while the embedded path's ranked move sits **outside** it. **And `move_email_to_folder()` in `worker/email/reader.py` copies, flags `\Deleted` and calls `expunge()`, read in full**, so the uid is gone after the first move and every later move of the same email fails. **On a two-attachment email the first attachment's outcome therefore wins, and on a two-image email the worst one does. The two paths disagree, in the opposite direction from the agreement 10f.22 established.**

   **This subsumes flag 2 of `2026-09-07_REPORT_claude_code_embedded_email_routing.md` rather than answering it.** That flag reported the duplicate branch pre-empting the ranking on a mixed embedded email, which is true and is a per-image decision taken before extraction. **The larger fault is that one path ranks and the other does not**, and fixing only the embedded path's duplicate branch would leave them disagreeing.

   **ANSWERED 2026-09-08. Paul's decision: `ok` beats a plain hash duplicate, so the ranking runs `failed`, `needs_review`, `possible_duplicate`, `ok`, `duplicate`, worst first.** ~~One decision is open and it is Paul's. A plain hash duplicate is not a failure, so where it sits against `ok` decides where an email carrying one duplicate photo and one good receipt lands.~~ **An email carrying one duplicate photo and one filed receipt goes to `INBOX.Processed Receipts`; an email whose every image was a duplicate still goes to `INBOX.Duplicates`, because `duplicate` is then the only outcome to rank.**

   **The reasoning, so it is not re-litigated.** A hash duplicate means the receipt is already held, so it is the most benign state in the set rather than a problem, and every receipt in such an email is accounted for: the new one because it was just filed, the repeat because it already was. **`INBOX.Duplicates` keeps one honest meaning, that nothing in this email produced anything.** **What the decision gives up is named rather than glossed:** a client who habitually re-sends is no longer visible from a folder, only from the log and the run stats. **Paul's own answer to that is not to move an email**, and it is recorded at item 175 of `2026-08-20_LIST_outstanding_items_and_decisions.md`.

   **It comes after 10f.32**, because folding the embedded path into the shared function makes `possible_duplicate` reachable there for the first time and the ranking has to cover it. ~~Briefable once 10f.32 has landed, and not before, because both touch the same routing block.~~ **10f.32 landed 2026-09-08 in `9dbb111` and this is briefed as `PROMPT_claude_code_2026-09-08_one_ranking_both_paths.md`.**

   **The full ranking, worst first, settled 2026-09-08 by amendments 270 and 272. Every rank is Paul's decision.**

   | Rank | Outcome | Folder |
   |---|---|---|
   | 1 | `unsupported` | `INBOX.Unsupported Files` |
   | 2 | `failed` | `INBOX.Failed Processing` |
   | 3 | `needs_review` | `INBOX.Needs Review` |
   | 4 | `possible_duplicate` | `INBOX.Possible Duplicate` |
   | 5 | `ok` | `INBOX.Processed Receipts` |
   | 6 | `duplicate` | `INBOX.Duplicates` |

   **`unsupported` is first, above `failed`. Paul, 2026-09-08, amendment 272.** An unsupported file is a wrong-format problem with one fix, telling the client to send a photo or a PDF, and that conversation attaches to the email whatever else happened in it; a failed extraction is a per-receipt problem. **Keeping the two apart is worth more than ranking them by urgency.**

   **The table is not a list of `validation_status` values**, and the distinction has to survive in the code: `ok`, `needs_review`, `failed` and `possible_duplicate` are statuses, while `unsupported` and `duplicate` are decisions taken before any extraction exists.

   **The attachment path has six outcome kinds where the embedded path has three, found 2026-09-08 by listing every move inside its loop rather than by assuming the two were symmetrical.** Unsupported file, already-handled attachment, hash duplicate, unknown sender, the four-branch routing, and the exception branch. **Five of the six become ranked outcomes. The unknown-sender branch does not**, because `resolve_client_info(email_from)` runs once per message above the loop, so every attachment in one email gets the same answer and ranking it could never change a folder. **That is a no-op the brief requires to be shown rather than asserted.**

   **One ordering case is preserved rather than improved.** The unsupported branch sits before the unknown-sender branch, so an unknown sender who sends an unsupported file records `unsupported` and no unknown-sender alert is sent. **That is today's behaviour under first-wins and it stays today's behaviour under the ranking.**

   10f.34 **BUILT 2026-09-09, amendment 302, as its own table above the receipts list, because that list cannot be cross-client.** ~~OUTSTANDING.~~ **The Review queue can be read across every client as well as one, in one list, with the client as a column.** Paul's decision, 2026-09-08, and it is built before step 10i, the pilot.**

   **What the queue is, stated because the question was asked and the answer decides the design. It is a list of receipts, one row per receipt.** `scanReview()` in `IntelliBooks-Desktop-v3.html` opens `Intellibills\Review\{client_id}\`, lists the files, and makes one row for every `.review.json` sidecar that has its image beside it, reading the supplier, date and amount off that sidecar. **Emails do not appear in it at all.** One email carrying three photographs becomes three rows.

   **That is the argument for it, and it is Paul's.** The capture mailbox is an **email-level** view, so a folder can only ever say one thing about an email holding several receipts, **which is the only reason 10f.33's ranking had to be designed.** A receipt-level view removes the need to summarise rather than improving the summary.

   **What is missing today is the list across clients, and nothing anywhere schedules it.** Searched across the whole of section 16 on 2026-09-08: "all clients" nought occurrences, "practice-wide" nought, "Review tab" nought, and "Review queue" exactly one, being 10f.15. **So the queue exists per client and the operator has to open each client to find out whether anything is waiting.**

   **Two things it needs and no more.** The list read across every client, and the client shown as a column so a row means something out of its own context. **What it does not need is new columns, a filter bar or a status vocabulary of its own**: section 8.2 is the console's Queue page and it specifies all of that, and this sub-step must not pre-empt it. **This is the smaller thing that arrives sooner, and 8.2 replaces it later.**

   **It comes after 10f.15, and that is the whole of the sequencing argument.** 10f.15 changes where the queue reads from, out of the pipeline's Review folder and into the inbox IntelliBooks owns. **Build the all-clients view before it and the view is written against the folder and then rewritten against the inbox. Build it after and it is written once.**

   **Its other half is 10f.32.** A receipt that is `ok` and unfiled appears in no queue at all, because only a receipt whose status is not `ok` reaches `file_review()`. **So the two sub-steps together are what make the queue a complete account of what needs attention**, and neither is sufficient alone. **This closes item 175 of `2026-08-20_LIST_outstanding_items_and_decisions.md`.**

   **One more, correcting 10f.33 rather than extending it. Amendment 274.**

   10f.35 **BUILT 2026-09-08 in `0894f88`.** ~~OUTSTANDING.~~ **A stranger who sends only an unsupported file now reaches `INBOX.Unknown Sender` and gets the alert, where before they heard nothing at all. One folder changed, three cases lost a wasted move, and no `move_email_to_folder()` call sits inside either email loop any more, which has never been true of this code before. Verified here from the syntax tree.** **The unknown-sender check moves above the attachment loop, so any email from an unregistered address reaches `INBOX.Unknown Sender` and gets the registration alert, whatever was attached. Paul's decision, 2026-09-08.**

   **This corrects an error in `PROMPT_claude_code_2026-09-08_one_ranking_both_paths.md` and the error is the consultant session's.** Section 4 of that brief stated as fact that leaving the unknown-sender branch unranked "could never change a folder", because `resolve_client_info()` runs once per message above the loop. **The premise is true and the conclusion does not follow:** the branch is reached only by attachments that got past `is_supported()` and the two duplicate checks, **so one email can record an outcome from one attachment and move immediately from another.** Claude Code measured six combinations before and after and an unknown sender sending a `.docx` and a `.pdf` moved from `INBOX.Unsupported Files` to `INBOX.Unknown Sender`.

   **The order-dependence going was the improvement. Which of the two answers is right was chosen by nobody, and Paul has now chosen a third.**

   **What decided it, in his terms.** The unknown-sender alert is the first and only thing an unregistered sender ever hears back. **Under the branch as it stands, a stranger who sends only an unsupported file gets silence and their email is filed as a format problem**, because the format check sits above the sender check and `continue`s. **That is the outcome this sub-step removes.**

   **Why above the loop rather than ranked.** Whose email this is, is a question about the email rather than about an attachment, and `resolve_client_info(email_from)` already answers it above the loop. **The check sitting inside the loop is where it was written rather than where it belongs**, and ranking it would leave the lone-unsupported-file case exactly as it is.

   **One detail is Claude Code's to settle rather than the design's.** The `unknown_sender` event is logged per attachment today and carries the filename. Above the loop there is one email and no filename, so the log entry changes shape. **The brief asks it to choose and to say what it chose.**

   **What must not change.** `has_alert_been_sent()` still guards against a second alert for one message. Every other outcome and every folder in 10f.33's ranking is untouched, and the eleven single-item combinations that sub-step pinned must stay pinned.

   **One more, and 10f.13's repointed sweep cannot work without it. Amendment 278.**

   10f.36 **BUILT 2026-09-09 in `ee9cb59`, amendment 287.** ~~OUTSTANDING.~~ **The pipeline records that it published, per receipt and per destination.** **It is `publish_events`, an eleventh table on the `resolution_events` pattern:** `event_id`, `receipt_id`, `destination`, `outcome`, `item_path`, `reason`, `created_at`, indexed on `(receipt_id, created_at)`. **No row means never offered, a `failed` row carries the reason, a `published` row carries the path.** **No foreign key on `receipt_id`**, on the same reasoning `schema.py` already gives for `resolution_events`. **Section 18.3 already says it does**, in its own words: "one document, several publishing destinations, and the tool records that it published". **No sub-step of 10f.1 to 10f.10 builds that record**, found 2026-09-08 while checking what Paul's decision at 10f.13 needs.

   **What it is for, and it is not an audit trail.** 10f.13's recovery sweep becomes "publish anything that was read and never published", and **without a record of publishing that question cannot be asked**: the sweep would either publish everything again on every poll or publish nothing. **A `filed_path` served that purpose for filing and there is no equivalent for publishing.**

   **What it must distinguish.** A receipt never published, from one published to a destination that failed, from one published successfully. **Per destination rather than per receipt**, because 10f.1 gives a client one destination now and 18.3 gives the pipeline three eventually, and a record that cannot name the destination is one that has to be rebuilt at that point.

   **It comes with 10f.1 to 10f.10 rather than after them**, because the publish step is where the write happens and adding the record afterwards means finding every publish site twice.

   **Two more, and the first is work that three places already assume exists. Amendment 315.**

   10f.37 **BUILT 2026-09-10 AND TESTED LIVE 2026-09-11, amendment 317. Both halves, and the copy was written because a transaction was posted.** ~~OUTSTANDING.~~ **THE POST-TIME MESSAGE FROM INTELLIBOOKS DESKTOP, which 10f.12 and 10f.16 each said was the other's and which was in neither.** When a receipt is attached to a transaction or a transaction is posted, Desktop tells Intellibills, and Intellibills writes the client folder copy on the `post` trigger. **The mechanism exists already and is not to be invented**: a note in `Intellibills\Resolutions\`, which is how a resolution reaches the pipeline today, per section 12. **Until this lands, `client_copy_trigger` must stay at `publish`**: `post` writes nothing and says so once per run, and switching before the message exists empties the client folders with nothing reporting it, which is what 10f.16 protects against. **Paul's decision of 2026-09-10 is that the trigger moves to `post` once this is built**, and his reason is that the folder should hold the documents that make up the accounts rather than everything the pipeline succeeded on.

   10f.38 **BUILT 2026-09-11 AND PROVED LIVE THE SAME MORNING, amendment 322. Both halves, and the copy reached the client folder because the transaction was posted.** ~~OUTSTANDING.~~ **A DOCUMENT ATTACHED FROM THE BANK TRANSACTIONS TAB, which Intellibills has never seen.** Paul's requirement, 2026-09-10: he is looking at a bank line, he has the document in his hand, and he should not have to go to the Receipts tab to send it first. **IntelliBooks attaches it immediately, so Attach means Attach**, and hands the document to Intellibills to archive into `Intellibills\Documents\` and to file into the client folder on the same trigger as 10f.37. **It is NOT extracted and NOT validated**, because the transaction already carries the date, the amount and the description, and a document that is not a receipt would otherwise fail into Review. **It is never published back and never appears in the receipts list**, which is Paul's own condition: that list is Intellibills' front end showing receipts, and this is not one. **It comes after 10f.37** and uses the same message.

10g. **BUILT 2026-09-11, and the step is COMPLETE: nine sub-steps built, none outstanding, 10g.8 cancelled. Amendment 326, which built 10g.4, the split transaction.** ~~OUTSTANDING.~~ **The VAT work in 18.4 and 18.5, which is specification and not code. Added 2026-08-20 by amendment 118.** Four parts, all decided and none built, found by sweeping the whole of this document for decisions with no step.

   **Decomposed into 10 sub-steps 2026-08-23 by amendment 159, on Paul's instruction, for the reason amendments 137 and 151 give about steps 10f and 10e: a bullet cannot carry a status and a numbered line can.** 10g.1 to 10g.8 are the bullets that were here, renumbered with their text unchanged. 10g.9 and 10g.10 are new.

   10g.1 **BUILT 2026-09-04.** `_VAT_TOLERANCE = 0.01` in `worker/validation/rules.py`, with the reason written where the constant is, **and the note that it is not the same number as `matchScore()`'s half-penny in `IntelliBooks-Desktop-v3.html`, which is a tolerance between two records rather than inside one.** No test asserted the old value, searched before changing it. ~~**The tolerance becomes one penny.** `worker/validation/rules.py:7` still reads `_VAT_TOLERANCE = 0.02` and `:39` still compares against it. Section 18.9 lists this as cancelled and the code has never changed.~~
   10g.2 **BUILT 2026-09-04.** Change log item 60. **The six rate values are the master's `vat_default` vocabulary rather than a new list**, and an imported account already arrives carrying one. The rate is derived from the category and stored on the transaction; an operator's choice is marked and survives a later change of category. **VAT is worked back out of the gross**, so 20% of a £120 line is £20.00. **An uncategorised transaction has no rate and no VAT and says so**, per 18.4. ~~**Bank Transactions gains VAT %, VAT and Net columns**, Net derived, **and where the VAT amount does not agree with the rate the VAT amount is shown in a different colour.** 18.4.~~
   10g.3 **BUILT 2026-09-11, amendment 325.** ~~OUTSTANDING.~~ **`bankFilter()` searches the category name as well as the code. Added 2026-08-21 by amendment 143 on Paul's decision, closing outstanding item 19.** `IntelliBooks-Desktop-v3.html:1706` filters on `t.desc`, `t.ref` and `t.category`, and since the chart adoption `t.category` holds the four-digit code, **so `7300` finds the row and `Fuel and oil` does not, while the name is what the screen shows.** Here because this is the step that opens the Bank Transactions table.
   10g.4 **BUILT 2026-09-11, amendment 326.** ~~OUTSTANDING.~~ **The split transaction.** One transaction becomes several lines, each with its own category, rate, VAT and amount, and the lines must sum to the original so the bank still balances. 18.4 calls it the general answer to one payment covering several things, not a VAT feature.
   10g.5 **BUILT 2026-09-04.** ~~OUTSTANDING.~~ **Built as `categoryReferences()`, one helper used by `delCategory()` and by 10g.9, and it matches the code OR the name rather than the code alone: rows written before the chart adoption still hold a name and they are references too. A code is digits, so it cannot collide with a name.** Change log item 59. ~~**`delCategory()`'s in-use guard compares the code, not the name. Added 2026-08-21 by amendment 145 on Paul's decision, closing outstanding item 15.** `IntelliBooks-Desktop-v3.html:2410` builds `same=v=>String(v||"").toLowerCase()===c.name.toLowerCase()` and counts transactions, receipts and statement rules with it. **Since the chart adoption all three hold the four-digit code, so all three counts are always zero and a category with transactions posted to it deletes with no warning.** `c.code` in place of `c.name`. **The comment above the function says "A category has no id: every reference to it anywhere is a copy of this string", and that is what needs correcting with it:** the code is the id, four digits from a controlled master rather than a name somebody typed.~~ **That comment is replaced and the reason is written where the code is.**
   10g.6 **BUILT 2026-09-04.** ~~OUTSTANDING.~~ **Keyed on the code, with the name kept as a second key for legacy rows.** Change log item 59. ~~**`renderReports()` and the VAT report stop being keyed on the category name. Added 2026-08-21 by amendment 144, closing outstanding item 55.** `catType[c.name]=c.type` at line 2171 of `IntelliBooks-Desktop-v3.html` is read with `\|\|"expenses"` at `:2175`, so with categories keyed on code **every transaction falls to the default and income is reported as expenditure.** Lines 2191 and 2235 carry the same keying. **Here because this is the VAT step and one of the two is the VAT report**, and rebuilding VAT reporting on a report that reads a field which no longer holds what it expects would be building on the fault.~~ **Built before the rest of 10g for exactly that reason.**
   10g.7 **BUILT 2026-09-11, amendment 325, and it is editable, which 18.5a did not say.** ~~OUTSTANDING.~~ **The Receipt Post to Cashbook check, 18.5a.** ~~and only then the Difference check, 18.5b.~~ ~~18.5b is informational, never holds anything up, and **must not be built before 18.5a**.~~ **Struck 2026-09-11 by amendment 324: 18.5b is cancelled, so nothing follows this.** What exists today is `postWarnings()`, which fires at one moment only, posting a receipt to the cashbook.

   10g.8 **CANCELLED 2026-09-11 by Paul, amendment 324, with 18.5b itself. "If I am posting, I have made a decision. I do not need a difference check at that stage."** ~~OUTSTANDING.~~ **18.5b's residual case is the supplier, not the amount. Added 2026-08-21 by amendment 127, after reading `matchScore()` for the first time.** ~~18.5b's one residual case depends on what `refreshMatches()` requires of the amounts~~ **It requires them to agree to within half a penny**, at `IntelliBooks-Desktop-v3.html:1272`, `if(Math.abs(Math.abs(t.amount)-rGross(r))>0.005)return -1;`, which is **tighter than 18.4's penny**. So an app-found match cannot disagree on the amount, and that case does not exist. **What the same function does allow is a match with no supplier agreement at all:** it accepts any date within 21 days and adds 30 per supplier word of three characters or more found in the bank description, with no minimum, so at 21 days with zero matching words the score is 58 and still wins if nothing scores higher. **If 18.5b is built, that is what it is for.**

   **Two more, and they are the category controls rather than the VAT work. Added 2026-08-23 by amendment 159.**

   10g.9 **BUILT 2026-09-04.** ~~OUTSTANDING.~~ **The `not_adopted` guard is in `setCatStatus()`, using `categoryReferences()`.** Change log item 59. **One correction to the description below: it says the dropdown sets the status inline with no check of any kind, and that had already changed. The dropdown calls `setCatStatus()`, which already guarded `archived` against inbound pointers; the `not_adopted` guard alone was missing.** ~~**The per-account status dropdown gains an in-use guard, and it applies to `not_adopted` only.** `IntelliBooks-Desktop-v3.html:2387` reads `onchange="books.categories[${i}].status=this.value;scheduleSave();renderBank();renderRules()"`, so **any status can be set on any account with no check of any kind.** Paul's rule, 2026-08-23: a category may be marked `not_adopted` only where no transactions exist for it. **The rule is not uniform across the three statuses.** `not_adopted` is blocked while any transaction, receipt or statement rule points at the account; **`archived` must be permitted precisely then**, because archived means it was in use and has been retired; and `active` needs no guard. **It comes after 10g.5**, because the counting it needs is `delCategory()`'s and that count is broken until 10g.5 lands. **And it does not make `catOptions()`'s exception redundant:** the filter at `:1676` keeps a non-active account visible when the field already points at it, because otherwise the browser reports the first option and the next write silently reassigns the transaction. Paul's rule removes the `not_adopted` route into that state; the `archived` route is what the exception is for and it stays. Amendment 25 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md`.~~
   10g.10 **BUILT 2026-09-04.** Change log item 60. **The display stays with the number, its description and a tooltip naming the route to change it; only the write goes.** `addCategory()` still applies a default to a hand-added account, which has no master row. ~~**The SA103F box dropdown becomes read-only.** `IntelliBooks-Desktop-v3.html:2386` reads `onchange="books.categories[${i}].sa103fBox=this.value;scheduleSave()"`, with the default applied at `:2401`, box 15 for income and box 30 for expenses. **Paul's decision, 2026-08-23: a mapping column belongs to the master and is read-only everywhere below it**, because it is a fact about tax and reporting rather than a firm or client preference. Left editable, the refresh and the operator overwrite each other with nothing reporting it. **The display stays; only the write goes.** **`mtd_itsa_category` and `frs105_line` need no equivalent change yet**, because neither travels into IntelliBooks today; when they arrive they arrive read-only. Amendments 23 and 27 of `IntelliCharts\2026-08-05_NOTE_master_chart_of_accounts.md`.~~

   **After 10f, because the split and the columns are about transactions.** ~~and 18.5b's one residual case depends on what `refreshMatches()` requires of the amounts, which has never been read~~ **Read 2026-08-21, see the last bullet above.**

10h. **BUILT 2026-09-04.** ~~OUTSTANDING.~~ **First half done 2026-09-03, amendment 188: the root went from 102 markdown files to 17 and 85 are in `archive\`. The step stays OUTSTANDING on its second half, the eight documents.** **First, the spent files leave the repository root. Added 2026-08-21 by amendment 153, on Paul's decision, and it comes before the reconciliation because it is what makes the reconciliation legible.** **~~The 17~~ **The 18** files named below stay in the root. Every other markdown file in the root is spent and moves into one `archive\` folder, tracked, with `git mv` so the history follows them. No total is stated here, by amendment 165.** ~~64 of the 81 markdown files in the root are spent and move. 17 stay.~~ ~~63 of the 80.~~ ~~59 of the 75, 16 stay.~~ ~~Claude Code's report will make 82.~~ **All four struck 2026-09-01 by amendment 165.** **The count belongs to the session that runs the step, not to the document that orders it**, because writing a document into the root changes the count and this document is written into the root. **Count the root when you run the step, and check the answer against the 17 names below rather than against a number written earlier.** For scale rather than for arithmetic: the root held **79** markdown files on the morning of 2026-09-01 and **84** the same evening, five documents having been written into it that day, and every figure this step carried before amendment 165 was overtaken within days of being written. ~~58 of the 74~~ **Corrected 2026-08-22 by amendment 157: the root holds 75, enumerated and printed, and all 16 named below are present, so 59 move.** `2026-08-21_HANDOVER_consultant_chat_9.md` became spent when `2026-08-22_HANDOVER_consultant_chat_10.md` was written, and that handover is the 75th file. **The figure moves every time a file lands in the root, so read it with its date or count again before running the step.** **Spent means executed and superseded, and a standing brief is not spent however old it is:** a `PROMPT_*` file is spent once the session it was written for has executed it and reported, a handover once its successor exists, and a report on delivery, because its findings are carried into the record. **Two files are named because both are easy to get wrong. `PROMPT_intellibooks_desktop_changes.md` stays**: `CLAUDE.md` records it as the brief the IntelliBooks Desktop session works from, so it is live. **`PROMPT_claude_code_step10a_and_10b.md` moves**, being both spent and the one that must never be sent, so archiving it makes that less likely rather than more. **And the current handover moves when its successor exists, not before.** **~~The 16~~ ~~The 17~~ **The 18** that stay, every one named in full 2026-09-01 by amendment 165**, in the order the folder lists them: `2026-07-25_CONSOLE_DESIGN.md`, `2026-07-31_PLAN_reset_and_restructure.md`, `2026-08-03_NOTE_chart_of_accounts_for_paul.md`, `2026-08-18_BOUNDARY_two_products.md`, `2026-08-18_INSTRUCTION_coa_authority.md`, `2026-08-20_LIST_outstanding_items_and_decisions.md`, `2026-08-20_LIST_settings_firm_and_client.md`, `2026-08-20_NOTE_demo_version.md`, `2026-09-01_DESIGN_cloud_multi_firm.md` (added 2026-09-01 by amendment 163), **`2026-09-05_DESIGN_receipt_accounts.md` (added 2026-09-08 by amendment 276)**, `CATEGORISATION.md`, `CLAUDE.md`, `EMAIL_PROCESSING_MICROSTEPS.md`, `MULTIFIRM_EMAIL_FORWARDING_ANALYSIS_AND_FINDINGS.md`, `PAUL_CHECKS_2026-07-30.md`, `PROMPT_intellibooks_desktop_changes.md`, `RECEIPT_CAPTURE_GUIDE.md`, and **the consultant handover that is current when the step runs**, which was `2026-08-24_HANDOVER_consultant_chat_11.md` on 2026-09-01. **~~Sixteen~~ **Seventeen** names and one rule, because the handover that stays is whichever one has no successor yet.** ~~this document, the reset plan, the boundary document, the two lists, the two notes, the COA instruction, the Desktop standing brief~~ **Seven descriptions struck 2026-09-01 by amendment 165 and replaced by the file names above**, under this project's own rule that a file is named in full every time. **All 17 were confirmed present in the root on 2026-09-01, **and all 17 again on 2026-09-08 when the eighteenth was added, enumerated from the folder rather than counted**** by listing the folder and matching each name against the listing, which is also the check to repeat before running the step: a name below that is not in the root means a file has been moved or renamed and the step stops. **The rule for the future is in `CLAUDE.md`**, because it applies to every session and not to this step.

   **~~Six~~ Eight documents are reconciled with the code. Added 2026-08-20 by amendment 119, extended to eight on 2026-08-21 by amendment 120, and it comes before the pilot because the operator guide is what Paul works from during it.** Step 21 covers `RECEIPT_CAPTURE_GUIDE.md` and step 22 covers `CLAUDE.md`, both after the console, and **neither covers the other four or accounts for what step 10d changes.**

   - **`RECEIPT_CAPTURE_GUIDE.md`** describes the document store as `data/files/YYYY/MM/DD/` in four places, gives `firm_id` as "usually INTELLITAX", and names `Client_001` and `Client_003` as live clients. It is the operator guide.
   - **`CATEGORISATION.md`** uses **three-digit GL codes throughout**, which amendment 96 makes provably legacy, and gives `105` two different account names in one file.
   - **`EMAIL_PROCESSING_MICROSTEPS.md`** pins behaviour to thirteen `app.py` line ranges, describes UID-based incremental fetching where `fetch_new_messages()` searches `ALL`, and gives the fallback firm as `INTELLITAX` in one place and `FIRM001` in another.
   - **`CLAUDE.md`'s Database Schema section** omits `categorisations`, `statements`, `client_code`, `source`, `filed_path`, `locked_at`, `duplicate_of`, `receipt_ref_number`, `receipt_time` and `pipeline_version`, states `firm_id` "Defaults to 'INTELLITAX'" at line 607 against `FIRM001` at line 515, and its Workflow section says results are "never modified after".
   - **`IntelliBooks-System-Specification.md`** and **`IntelliBooks-System-Overview.md`** describe the pre-restructure paths, a chart of accounts at `IntelliBooks\chart_of_accounts.csv` that amendment 96 cancelled, and `CLAUDE.md` as the stale one on the document store when both are now stale.
   - **`IntelliBooks\App\Docs\SETUP-v5.md`** and **`IntelliBooks\App\Docs\HANDOFF-Status.md`**, the seventh and eighth. **Added 2026-08-21 by amendment 120 and never added to this list, which is why the heading said eight and the bullets named six. Corrected 2026-09-04 by amendment 201.**

   **Done, five of the eight.** `CATEGORISATION.md` on 2026-09-03, amendment 189. **`CLAUDE.md` on 2026-09-04, amendment 201.** **`RECEIPT_CAPTURE_GUIDE.md` on 2026-09-04, amendment 203.** **`EMAIL_PROCESSING_MICROSTEPS.md` on 2026-09-04, amendment 204.** **`IntelliBooks-System-Specification.md` on 2026-09-04, amendment 206, and step 10i's pilot procedure is rewritten with it.** **`IntelliBooks-System-Overview.md` on 2026-09-04, amendment 207.** **`SETUP-v5.md` and `HANDOFF-Status.md` on 2026-09-04, amendment 208. All eight done and the step is BUILT.**

   **After 10d, because 10d makes four of the six wrong in a new way**: there is no `client_code`, the registry is `clients.json`, and the inbox client comes from the item rather than the folder.

10j. **BUILT 2026-09-06, all 11 sub-steps, and PROVED end to end the same day.** ~~OUTSTANDING.~~ ~~BUILT means the code is built and tested. The one thing not yet observed is a layer 1 match on a real receipt.~~ **Struck the same day by amendment 234: a layer 1 match was observed at 10:09 BST.** `TESTFIXTURE_car_wash_4_match.png` was categorised `match_source=client, confidence=high` from the mapping taught two minutes earlier, with no AI call and no Review. **It is the first matched categorisation this system has ever produced.** **The receipt accounts list, layer 5's context, and the fallback column. Added 2026-09-05 by amendment 225, on Paul's decisions of the same day.** This step exists because item 152 was settled and the work it unblocked belonged to no step. **Nothing here was in section 16 before today**, which is the state the list-versus-order rule exists to prevent: work that is decided, is not scheduled, and therefore does not happen.

   **What layer 5 classifies into is Intellibills' own list of receipt accounts, seeded from the master and then frozen. Not the client's chart, and not an invented taxonomy.** The four-digit master code is the identifier, so nothing new has to be numbered and nothing has to be mapped inside this practice: verified 2026-09-05 across the eight library charts, 927 rows, 0 codes that are not master codes and 0 differences in name, SA103F box or VAT treatment.

   10j.1 **BUILT 2026-09-05.** **Layer 5's `response_format` is a model class and not a schema dict, and until this was fixed layer 5 had never once returned an answer.** `_ai_suggest()` in `worker\categorisation\engine.py` passed a dict; `has_rich_response_format()` in `openai\lib\_parsing\_completions.py` returns False for a dict, so `message.parsed` was always None and every call logged `AI response invalid` while the answer sat unread in `message.content`. Fixed with `AiAccountSuggestion`. **Consultant session, and it is production code, which `CLAUDE.md` assigns to Claude Code. Paul's instruction, and the breach is recorded rather than hidden.**
   10j.2 **BUILT 2026-09-05.** **The supplier name reaches layer 5, not only the normalised key.** "Canary Hand Car Wash" arrives as `canary`, which the model read as software; given the name it answered `7391 Car wash` correctly. Consultant session, same breach, same instruction.
   10j.3 **BUILT 2026-09-05.** **`gross_amount` reaches layer 5.** Claude Code, `PROMPT_claude_code_2026-09-05_layer5_context.md`, commit `76d1f17`. Halfords had been answered `0081 Motor vehicles - cars - additions`, a capitalised car, and the amount removed it: verified by Claude Code that `0081` was in all four pools, so it was offered and refused rather than absent.
   10j.4 **BUILT 2026-09-05.** **`line_items` comes out of the extraction call**, in `_SYSTEM_PROMPT` in `worker\extraction\openai_vision.py`, capped at 40 lines, subtotal, VAT, total, change and payment lines excluded. `max_tokens` raised 500 to 1500 in the same change, because nine fields plus item lines do not fit in 500 and a reply cut mid-JSON makes every field null. **No second call and no second image.**
   10j.5 **BUILT 2026-09-05.** **Layer 5 uses the supplier, the amount and the line items together.** Claude Code, 17 tests added, suite 389 to 406 passed, 190 to 195 subtests, five mutations each caught by the test aimed at it and by no other.
   10j.6 **BUILT 2026-09-05.** **Claude Code, commit `fc526a7`. Neither had been done. The test enumerates the five call sites from the syntax trees rather than listing them.** ~~OUTSTANDING.~~ **`gross_amount` reaches the two `worker\resolution\service.py` call sites**, at `:670` and `:1069`, both of which have `merged["gross_amount"]` in scope at `:657` and `:1055`. **The brief said three call sites and there are five**, enumerated whole on 2026-09-05: `app.py:478`, `worker\extraction_pipeline.py:218`, `service.py:670`, `service.py:1069`, `retroactive_categorise.py:133`.
   10j.7 **BUILT 2026-09-05.** **Amendment 226.** ~~OUTSTANDING.~~ **The fallback column on the master.** Each account names the account it collapses into where a client's chart does not hold it; blank means the receipt goes to Review. **Paul's ruling, 2026-09-05: a car wash goes to `7310 Vehicle repairs and servicing` where `7391 Car wash` is not in the chart.** Maintained in `COA_MASTER_v2.xlsx`, validated by `publish_master.py`, published in the bundle. **It is an accounting fact per account and must not be inferred at runtime.**
   10j.8 **BUILT 2026-09-05.** **Amendment 227, Claude Code, commit `fc526a7`.** ~~OUTSTANDING.~~ **The pipeline reads the fallback** when a code is absent from the client's chart. ~~Counted 2026-09-05: `PHV_DRIVER` holds 30 of the 66, `FIN_ADVISER` 40, `SALE_OF_SERVICES` 44, `SALE_OF_GOODS` 49~~ **CORRECTED 2026-09-05, amendment 227: `PHV_DRIVER` holds 29 of the 66, `FIN_ADVISER` 38, `SALE_OF_SERVICES` 41, `SALE_OF_GOODS` 45.** The struck figures were counted against a 71-account cut, before the five capital additions came out. **So this is the common case and not the edge one, by a wider margin than the wrong figures showed.**
   10j.9 **BUILT 2026-09-05.** **Amendment 226. 66 rows, placed at `worker\categorisation\receipt_accounts.csv` and read back off disk: 66 rows, no asset codes, all four catch-alls present, one fallback.** ~~OUTSTANDING.~~ ~~`worker\categorisation\receipt_accounts.csv` is created~~, 66 rows, columns `code`, `name`, `fallback_code`, `synonyms`, `vat_default`, `vat_variable`, `vat_recoverability`, `sa103f_box`, `mtd_itsa_category`, `vat_explanation`. **Produced 2026-09-05 and not yet placed.**
   10j.10 **BUILT 2026-09-05.** **Amendment 229. Claude Code, commit `09ec7be`.** ~~OUTSTANDING.~~ **Layer 5 chooses from `worker\categorisation\receipt_accounts.py`, which reads the 66 shipped rows, and not from `get_eligible_accounts_for_client()`**, and the resolved code is then checked against the client's chart, falling back per 10j.7 or the row being flagged. ~~chooses from `receipt_accounts.csv` and not from the client's chart~~ ~~or reaching Review~~ **Struck 2026-09-05 by amendment 229, on two counts.** The reader is the module and not the file read directly. And `needs_review` **flags the row and does not move the file**, per amendment 228: what routes a receipt to the Review folder is `validation.status`. **The pool moved from 55 for a client on `SALE_OF_SERVICES`, and 95 for a client with no `chart_code`, to 66 for every client.** Measured before and after by Claude Code and printed whole in `2026-09-05_REPORT_claude_code_layer5_reads_the_66.md`; `ThePoolTest::test_the_pool_does_not_depend_on_the_client` asserts the independence directly rather than by counting. **The reader is package-relative, adds no `config.py` constant, has no modification-time cache because the file ships with the code, and an unreadable file is empty plus one cached ERROR.** **`synonyms` is not read, and two tests hold that**, so the day it stops being empty is a decision. **End to end, and this is the sub-step's evidence:** `IMO CAR WASH MERTON` for `Client_001` on `SALE_OF_SERVICES` chose `7391 Car wash` and resolved to `7310 Vehicle repairs and servicing`, `outcome=substituted`, **which is Paul's ruling of 2026-09-05.** `7391` is the only one of the five codes in that run that the old 55-account pool lacked. **Suite 487 passed, 324 subtests, zero skips. That figure is Claude Code's, from its report and the commit message of `09ec7be`; the consultant session has no pytest and did not run it.**
   10j.11 **BUILT 2026-09-06, both halves.** ~~OUTSTANDING.~~ **The pipeline half is amendment 232, Claude Code, commit `d24da39`. The IntelliBooks Desktop half is amendment 233, built by the consultant session because no IntelliBooks Desktop session existed, on Paul's instruction and disclosed.** **The control on screen is a checkbox in the Complete Review Receipt window reading `Remember this supplier for this client`**, unticked every time the window opens. **The note carries `values.category_code`, `values.category_name` now holding the name, and a top-level `remember_gl_for_supplier`, with `schema` unchanged at `1`.** **Proved end to end on 2026-09-06 at 08:59 BST: the first vendor mapping this system has ever learned.** **All three decisions taken 2026-09-05 by Paul, amendment 231.** **What is built: a `remember this supplier` tick in the Review Receipt window of `IntelliBooks-Desktop-v3.html`; a `category_code` field and a `remember_gl_for_supplier` flag in the 12.2 note payload, `schema` unchanged at `1`; `_resolve_category()` validating `category_code` against the client's chart instead of returning `None`; and `_apply_filed_note()` writing `upsert_client_vendor()` only, when the tick is set and a code resolved.** **The firm table is NOT written.** ~~Item 166.~~ **Item 166 is closed 2026-09-06 by amendment 238, Paul's decision, and the firm table is now step 10m.** **Learning: the opt-in control, a field in the 12.2 payload to carry it, and the write.** ~~None of the three exists~~ **Struck 2026-09-05 by amendment 230, and the correction changes the shape of the sub-step rather than its size. There are two resolution routes, and the learning exists on the one nobody uses.** **`resolve_receipt()` in `worker\resolution\service.py` is the command-line route and it learns**: `Corrections.remember_gl_for_supplier` at `:42`, and the `upsert_client_vendor()` call at `:772` guarded by `if corrections.remember_gl_for_supplier and effective_code:`. **`_apply_filed_note()`, the IntelliBooks Desktop route, has none.** Its learning branch was removed with item 155 on 2026-09-04, and the comment left at `:1119` records why. **The blocker is upstream of the control.** `_resolve_category()` at `:939` returns `None` for the code on every note, always, because the `repo.find_coa_account_by_name()` lookup it used queried `coa_accounts`, **which amendment 96 cancelled and 124 confirmed**. So a Desktop note reaches the write with a category name and no code, and there has never been anything to learn. **All four learned tables hold 0 rows, and nothing in this system has ever learned a vendor mapping.** **Three decisions are needed before either brief can be written, and all three are Paul's.** ~~**One. Does a Desktop resolution learn at all, and on what.** Section 11.3 says never automatically, because one correction against a misread supplier name poisons the mapping table and the exact-match layer then applies the wrong code confidently to every future receipt from that vendor. **Section 12.3 step 6 says a Desktop resolution learns the mapping.** The two disagree and `:1119` says in as many words that nothing here decides it.~~ **Struck 2026-09-05 by amendment 231. There is no disagreement and there has not been since 2026-07-28.** 12.3 step 6's learning clause was struck that day and the correction beneath it reads "11.3 wins: a back-feed note never learns a mapping" and "learning stays opt-in from an operator who ticked a box". **The comment at `service.py:1119` says the two disagree, and amendment 230 repeated it without opening 12.3 step 6.** **Two. Which code is learned. DECIDED 2026-09-05 by Paul: the client table only, and the firm table is deferred.** ~~A car wash for a client whose chart lacks `7391` produces two codes and the two do not agree.~~ **The question was put wrongly and the correction is what made it answerable.** There are two learned tables, not one: `categorisations_client_vendors`, written by `upsert_client_vendor()` at `worker\database\repository.py:343` and read by layer 1, scoped to one client; and `categorisations_firm_vendors`, `upsert_firm_vendor()` at `:390`, read by layer 2 and scoped to a `business_type`. **`service.py:775` writes the client one. The firm one has no writer anywhere.** **What Desktop can supply settles it.** `catOptions()` builds the dropdown from `books.categories`, which is the client's adopted chart, so `7391` is not in the list for a client on `SALE_OF_SERVICES` and Paul cannot pick it. **The only code Desktop can send is the client-chart code, and the client table is scoped to that client, so storing it there is correct and can never reach another client.** `7310` cannot be mapped back to `7391`: `7310`, `7391` and `7392` can all resolve into `7310`, so the reverse is many-to-one. **The firm table would need the receipt account, which is knowable only when Paul's pick matches what the chart check resolved the classifier's suggestion to. That is a separate decision and it is item 166.** **Three. How a code reaches the pipeline from Desktop. DECIDED 2026-09-05 by Paul: a new `category_code` field beside `category_name`, and `schema` stays `1`.** ~~Reported by a second Cowork session and NOT verified here.~~ **Verified 2026-09-05 by reading `IntelliBooks-Desktop-v3.html`, mtime 2026-09-04 17:26 BST.** `catOptions()` at `:2610` writes `<option value="${esc(c.code)}">${esc(c.name)}</option>`, and its own comment reads "The option value is the code. The code is the identifier; the name is display only." `fileReviewReceipt()` at `:3340` reads that value into `category` and `:3392` writes it as `category_name`. **So the code is already being sent, in a field the contract says is a name, and the pipeline discards it.** The alternative, renaming the field and bumping to `schema:2`, was rejected because both halves would have to ship in the same window. **Nothing has been harmed either way**, `Intellibills\Resolutions\processed\` being empty since the reset. **This is the reversal 12.3 step 6 predicted on 2026-08-17: the step stops being name-to-code and becomes a validation that the code exists in the client's chart.** **Section 11.3 forbids learning automatically, so the control is what makes any of 10j worth having.**

   **Before 10i, the pilot**, because the pilot is one real client's receipts and every one of them arrives unmatched until this step is done.

10k. **BUILT 2026-09-11, both halves; see amendment 328 for what was built and for Paul's ruling that the note is sent only where something changed.** **An edit to an already-filed receipt writes a `corrected` resolution note. Added 2026-09-06 by amendment 235, on Paul's decision closing items 36 and 167.** Two halves. **IntelliBooks Desktop**: an edit to a filed receipt writes a note with `action` `corrected`, carrying the same `values` a `filed` note carries plus `remember_gl_for_supplier`, and the `Remember this supplier for this client` tick appears in the window the edit is made in. **The pipeline**: 12.3 gains a `corrected` branch which applies the corrected values and the learning and **does not re-file**, because the receipt is already filed and `filed_path` is already set. **Scope is the receipt record. A recoding made against a transaction in the books is item 35 and is not in this step.** **The assertions come before the writer**: nothing has ever tested what a matched row stores, and every `categorisations` row in existence reads `unmatched`. Carried from item 166, closed 2026-09-06, and stated in full at step 10m. **Before 10i.**

10l. **BUILT AND CLOSED 2026-09-12, BOTH HALVES. Amendments 336 and 342. The live check on screen PASSED, run by Paul: the amber pill alone on the held receipt, the existing pill unchanged, the count saying its two reasons separately, and the reason line in the Complete Review Receipt window.** ~~The live check on screen is outstanding and a test item is left in `IntelliBooks\Incoming\` for it.~~ ~~OUTSTANDING.~~ **A layer 5 categorisation holds the receipt instead of filing it. Added 2026-09-06 by amendment 237, on Paul's decision.** The hold keys on `match_source = 'ai'`. **Not on `confidence`**, which is the constant `low` for every layer 5 answer and is read by nothing, per amendment 236. **`categorisations.needs_review` is not the mechanism either**: it is read by nothing and setting it moves no file, per amendment 228 and `worker\categorisation\fallback.py:220-227`. What routes a receipt to Review is `validation.status` in `worker\validation\rules.py`, which carries an arithmetic meaning today, **How a categorisation outcome reaches that decision was left open here and is DECIDED by amendment 330, 2026-09-11: ~~the hold lives in `categorisations.needs_review`~~ **SUPERSEDED 2026-09-12 BY AMENDMENT 333: the hold keys on `match_source` and holds `fuzzy_client`, `fuzzy_firm` and `ai` and nothing else.** `validation.status` keeps its arithmetic meaning only, the published item carries a second key so `drainInbox()` can hold on either, and the screen carries a second amber pill reading `Category unconfirmed` alongside the existing ones rather than a new value of them.** **AMENDMENT 333, 2026-09-12, and why the body says it rather than only the head-table row: `needs_review` is also True for `unmatched` and is forced True by `resolve_against_chart()` whatever the layer, so 330 as written held 25 of 26 rows on the live database and held every receipt in the practice whenever the client's chart could not be read. An unmatched receipt is not a guess.** **BUILT, the pipeline half: commit `b439b92` on amendment 330's reading and `e27aecc` narrowing it to 333's**, reported in `2026-09-11_REPORT_claude_code_category_hold.md` and `2026-09-12_REPORT_claude_code_category_hold_trigger.md`. **The published key is `category_unconfirmed`, a JSON boolean, on every item, absent meaning not held. The Desktop half is written from section 1 of the first of those reports, which the second confirms has not moved.** **Before 10i**, because the pilot is one real client's receipts and an AI guess must not reach that client's books unseen. **SPLIT 2026-09-11 BY AMENDMENT 331, and the pipeline half goes first.** The second key is written by the publish code, which the consultant session has not read, so naming it is Claude Code's. The pipeline half is `PROMPT_claude_code_2026-09-11_category_hold.md` in the repository root, **held until step 10n reports**. ~~md5 `7c476aede33d6d3f8a5dea6983884372`~~ **STRUCK AND NOT REFRESHED, 2026-09-12.** The brief was edited after that hash was written, to strike its HELD line once 10n reported, so the document asserted a hash nothing matched and the file actually sent was `9f4a47d55a42524961b89d7e60553437`. **Claude Code found the mismatch and flagged it; it is the consultant session's own error.** **A hash of a file that can still be edited is a citation that goes stale, which is the `config.py` line-number rule applied to hashes.** Quote a brief's hash to the session receiving it, at the moment of sending. Do not record it in a document. **The Desktop half, the second amber pill and the reason line, is written from that report and is not started before it.** **THE CONFIRMATION IS SETTLED, 2026-09-12, AMENDMENT 334.** Amendment 330's fourth point required the confirmation to be recorded rather than inferred and named no mechanism. **The act is pressing `File Receipt`** in the **Complete Review Receipt** window of `IntelliBooks-Desktop-v3.html`, changed or not, with no tick and no second control. ~~The act is Save.~~ **Corrected 2026-09-12 by amendment 335: there is no Save in that window, and amendment 334 named a control it had not read.** **NOTHING IS STORED.** ~~The record is a field on the books receipt entry, written at that moment.~~ **Struck the same day by amendment 335.** The pill is drawn on review rows only, `scanReview()` builds those from `IntelliBooks\Incoming\` alone, and completing a receipt makes the books row and removes the item from that folder, so the pill has no route back. **The reason line reads *"The category was suggested by the classifier and nobody has confirmed it. Check it, then File Receipt."*** **Both points are Paul's decisions, 2026-09-12.**

10m. **BUILT 2026-09-12. Amendment 344.** ~~OUTSTANDING.~~ **One helper holds the rule, `upsert_firm_vendor()` has exactly one caller and it is that helper, and a source guard asserts it as a set so a third route cannot skip the rule. The layer 2 assertions came first and the field swap that nothing caught this morning is now caught by two tests. No row can appear until the classifier is on, which is step 10p.** **The firm vendor table, `categorisations_firm_vendors`, gains a writer, and it is written only where a correction confirms the classifier. Added 2026-09-06 by amendment 238, on Paul's decision closing item 166.** **The rule.** The operator's chosen code is compared with the code 10j.8's chart check resolved layer 5's suggestion to. **Equal: both tables are written**, the client table with the operator's code and the firm table with the account out of the 66 that layer 5 named, keyed on `business_type` and `vendor_key`. **Different: the client table only**, because several master codes collapse into one under 10j.7's fallback and the operator's pick cannot be run backwards to a receipt account. **The tick governs both**, per 11.3, and a correction with no tick teaches neither. **AND THE RULE COVERS BOTH LEARNING ROUTES, the IntelliBooks Desktop back-feed and the command-line route. Paul, 2026-09-12, amendment 341**, on Claude Code's question. ~~Amendment 231's point two: a Desktop correction writes the client vendor table only.~~ **Superseded in the confirm case only; outside it 231 stands.** 231 excluded the firm table because a receipt account cannot be recovered from an operator's chart code, which is this rule's own "Different" half, and 238 carves out the one case where it does not need recovering. **One shared rule called from both sites, not two implementations.** **The confirm case arises only on a layer 5 answer**: layers 1 and 2 return a stored code with no suggestion out of the 66 to agree with, and layer 2 already holds anything it would learn. **The writer already exists in name.** `upsert_firm_vendor()` at `worker\database\repository.py:390`, and the table holds 0 rows. **Its only caller was `learn_from_correction()`, deleted at amendment 234, so it has none.** **The assertions are written before the writer, and this is the step's first task, not its last.** No test has ever asserted what a matched row stores at layer 2, because **every `categorisations` row in existence reads `unmatched`**. During the `vendor_key` rename a mutation swapping the two fields at layer 1 left the whole suite green for exactly that reason; two assertions closed it at layer 1 and **the identical swap at layer 2 is still live**. **10l comes first in practice as well as in the order**, because until a layer 5 answer holds for the operator there is almost nothing for a correction to confirm. **Before 10i.**

10n. **BUILT 2026-09-11 by Claude Code, commit `e02fe8f` on `feat/console-phase0`, amendment 332. The BUILT record already stood at the end of this paragraph and this head word contradicted it.** ~~OUTSTANDING.~~ **A capture report, on demand, for one client. Paul's decisions of 2026-09-11, amendments 319 and 327.** **What it answers is his own sentence**: the client says "I sent this receipt, why is it not in the file?" **A list of what reached the books cannot answer that**, because the receipt being asked about is exactly the one that is not there. So it lists **everything captured for that client and what became of each one**: filed, in review, flagged a possible duplicate, failed extraction, or discarded. **Two scopes, and they select on different dates.** A **tax year** selects on the document date, because that is the accounting question. A **range of dates** selects on arrival, because "I sent it last week" is a question about when it was sent and not about what the document says. **The pipeline writes it and not IntelliBooks Desktop**, which is the correction amendment 327 records: Desktop sees what was published to it and nothing else, so it cannot see a failure, a duplicate or a discard. **On demand, so nothing new appears in `Clients\` and 18.2b's single writer is untouched.** **Before 10i**, because answering that question is part of running a real client. **BUILT 2026-09-11 by Claude Code, commit `e02fe8f` on `feat/console-phase0`, amendment 332.** `capture_report.py`, run as `.\.venv\Scripts\python.exe capture_report.py --client <client_id> --tax-year <yyyy-yy>` or `--from <date> --to <date>`. It prints to the screen and writes the same text to `exports\`. **Eight outcomes, not seven**, and the eighth is `bank_attachment`.

10p. **BUILT AND CLOSED, BOTH HALVES, 2026-09-12. Amendment 353.** ~~OUTSTANDING.~~ **The classifier's inputs and its switch. Added 2026-09-12 by amendment 340, on Paul's decisions after running `probe_layer5.py` against the live database.** **Three parts and they belong together, because turning the classifier on without the first two is what the probe showed going wrong.** **ONE, ITEM LINES ARE STORED ON THE EXTRACTION, DESCRIPTION AND AMOUNT SEPARATE.** They are read once and never saved, so of the six production calls to `categorise()` only `process_extraction_result()` passes them; the recovery sweep in `app.py`, both resolution back-feed sites and `retroactive_categorise.py` do not, each because it reads the extraction back out of the database. **So the same receipt is categorised from less on every run after the first, and nothing says so.** The extraction contract changes with it: `line_items` is a list of strings today, description and amount run together and capped at 40, and it becomes description and amount separately, **because strings are enough for the classifier and useless for a split**. **Nothing is backfilled**, per Paul. **TWO, THE CLASSIFIER CALL BECOMES DETERMINISTIC**, `temperature=0` and a fixed seed. It passes `model`, `messages` and `response_format` and nothing else, so it samples at the API default, and the probe returned `7113 Business rates` and `7110 Rates` for the same supplier, the same 3.80 and the same client. **A permanent mapping would be taught from whichever the operator happened to confirm**, which is step 10m. **THREE, THE ON AND OFF BECOMES A PER-FIRM SETTING ON THE FIRM RECORD, DEFAULT OFF**, shown on Firm Settings and read by the pipeline at its one engine construction site; `retroactive_categorise.py` stays hard off. ~~an environment variable in `.env`~~ **Rejected: an environment variable cannot be per firm and the cloud version is one service serving several firms.** **The pipeline half is Claude Code's and the Firm Settings half is the consultant session's.** **Before 10i**, because the pilot is one real client's receipts and this is what decides whether a machine's answer is worth showing the operator at all. **10o is skipped deliberately: it reads as 10 followed by a nought.**

10q. **BUILT AND CLOSED, ALL THREE SETTINGS, 2026-09-12. Amendment 355.** **The capitalisation threshold: a firm default with a per-client override, gated by the client's accounting method. Added 2026-09-12 by amendment 349, Paul's decision closing outstanding item 33.** `0030 Office equipment` and `0035 Computer equipment` in the master carry the note that the threshold below which a purchase is expensed to `7502` or `7552` rather than capitalised is a policy decision, and it had no figure attached until then. **Three settings, in `2026-08-20_LIST_settings_firm_and_client.md`.** **F19**, the firm default, £50. **C21**, the same threshold per client, defaulting to F19 unless the client record overrides it. **C22**, a dropdown naming the client's accounting method, Accruals Basis or Cash Basis. **C21 reads as switched off when C22 is Cash Basis**, because a capitalisation threshold has no effect under cash basis. **The client half was built first, amendment 354**: `accounting_method` and `capitalisation_threshold` as new fields on the client record in `Intellibills\clients.json`, read and written on the **Client Settings** tab. **F19's store was put to Paul rather than guessed at, and he decided it: `Intellibills\firms.json`, the same file Intellibills Settings already uses, amendment 355.** One settings file serves both products, IntelliBooks being built as an enhanced Intellibills rather than a separate product with a store of its own. It is entered on the **Firm Settings** tab's **IntelliBooks Settings** card, which had held no control before that day. Absent reads as 50. **Two faults found by Paul using it were closed the same day.** Amendment 356: C21's box shows the real firm default number rather than a blank box with a placeholder word. **Amendment 357: on Cash Basis the row reads as switched off rather than disabled and still showing a number** — the box empty and disabled, the caption and the £ sign `#c2c8ce` — and `saveClientSettings()` skips the threshold block entirely while the method being saved is `cash`, **without which saving any other field on a Cash Basis client would have read the emptied box as "clear the override" and erased a real per-client figure.**

10r. **OUTSTANDING.** **Exports leave the repository and go to `Intellibills\Exports\`. Paul's decision, 2026-09-12, closing outstanding item 37, amendment 359.** **The logs half of item 37 was already settled by the code and needed no decision**: `config.py` reads `LOGS_DIR = UNSYNCED_ROOT / "logs"` and `RUNS_LOG = LOGS_DIR / "runs.ndjson"`, so `runs.ndjson` and the four process logs named in `ENTRY_POINT_LOGS` are in one place, `C:\Intellibills\logs\`, and the `DATA_DIR` that parented three unrelated things is gone. **What was left open was exports, and it is the same two-locations shape the logs question existed to remove.** `OUTPUT_DIR` is `config.BASE_DIR / "exports"` in both `export_bookkeeping.py` and `capture_report.py`, so the repository holds an `exports\` folder, while the practice root already holds `Intellibills\Exports\`. **What to build.** A config constant derived from `INTELLIBILLS_ROOT` rather than from `BASE_DIR`, joining the mkdir block so an absent folder is not a first-run failure, and both scripts pointing at it. **The reason the two scripts pin `OUTPUT_DIR` at all stands and must survive the change**: each file's own comment says it exists to stop an `exports\` being scattered wherever the shell happened to be standing. A constant anchored on the practice root does that job as well as one anchored on the repository. **Sync is not an objection.** 18.2a's test is whether a file is held open: an export is written once and closed, like the document store and the database backups, so it is safe in OneDrive. **What moves on disk**: `exports\bookkeeping_export.csv`, the one file there, into `Intellibills\Exports\` beside the two CSVs of 2026-08-18. The repository's `exports\` and its empty `logs\` folder go with it. Both are gitignored and neither holds a tracked file, established by `git ls-files logs exports` returning nothing. **Before 10i**, because it changes where the operator looks for a capture report and that should not move during a pilot.

10s. **BUILT 2026-09-12, amendment 360.** **`attachReceipt()` in `IntelliBooks-Desktop-v3.html` carries nothing from the receipt to the transaction. Paul's decision, 2026-09-12, closing outstanding item 35.** **This is 18.5's own opening rule and it was written on 2026-07-30**: "Attach carries nothing and checks nothing. `attachReceipt()` currently copies the receipt's VAT and its category onto the transaction. Both stop." **It never became a sub-step of anything.** Step 10g's scope is "the VAT work in 18.4 and 18.5, plus the two category controls", and none of its ten sub-steps carried this sentence, so 10g was closed COMPLETE on 2026-09-11 with a decision from its own section unbuilt. **What the code did until today**: it filled `t.category` from the receipt where the transaction had none, and `t.vat` from the receipt where the transaction had none and the client was VAT registered. Neither overwrote anything. **Why they still go.** 18.1: the receipt is evidence about a transaction that already exists, and its figures may be wrong while the transaction is right. 18.5: attaching is provisional, and a category on the receipt and not on the transaction is a difference to be reported rather than a value to be copied. **Nothing downstream loses by it**: 18.5a refuses to post a receipt to the cashbook with no category, and an uncategorised transaction has no rate and no VAT at all under 18.4. **What is knowingly given up.** The difference was to have been reported by the Difference check at 18.5b, which Paul cancelled on 2026-09-11 by amendment 324, so nothing reports it and that is a consequence taken deliberately rather than an oversight. **The superseded lines are kept in a comment above the function** rather than deleted. **Verified**: `node --check` passed on the whole extracted script block, and a deliberately broken copy was rejected by the same check; the function was then pulled out verbatim and driven in Node over 19 assertions covering an uncategorised transaction, an already-categorised one, a VAT-registered and a non-registered client, a locked transaction, a missing transaction, a missing receipt row and the four side effects. **The same harness run against the pre-edit function failed 4 of the 19 and passed the other 15**, which is both the proof that it discriminates and the proof that nothing else in the function moved. `IntelliBooks-Desktop-v3.html` md5 `a4edc58af5d549bff7a9a72d958d051b` before, `fa3f0936df8266e91a8fb452af5493cf` after, with `.bak-before-attach-carries-nothing` written first and proved byte-identical.

10t. **OUTSTANDING.** **The reports set. Paul's direction, 2026-09-13, which reframes outstanding item 145 and closes it into this step, and item 151 closes into it the same day on the same reasoning, amendment 370.** **It is a reports job and not an MTD job**: work out the different reports IntelliBooks might need, the MTD ITSA quarterly update being one of them, rather than bolting a second export onto what is there. **Paul's framing, and it is the one to build from: a report is a subset of the same data, output in a particular form.** So the deliverable is one reports module with named reports in it, not several unrelated buttons. **SCOPED GENERALLY AND DELIBERATELY NOT IN DETAIL, on his instruction of 2026-09-13: "I do not think that now is the right time to go into deep detail. Just generally scope it."** The detail is settled when this step is taken up. **WHAT IS ON SCREEN TODAY, enumerated from `IntelliBooks-Desktop-v3.html` on 2026-09-13 rather than recalled.** Four cards on the Reports tab: **Profit and Loss**, **Category Totals**, **VAT Summary**, shown only for a VAT-registered client, and **HMRC Summary Export**. **Two buttons both reading Export CSV and producing unrelated things**: `exportReportCSV()` at the top of the screen writes a transaction listing to the browser, and `exportHMRC()` on the HMRC card writes an SA103F return summary into the client folder and archives the period. Elsewhere: **Download Client Backup** and **Download Practice Backup**, and the pipeline's own capture report at step 10n, which is a command-line tool and not part of this. **THE PROVENANCE OF THE TWO THINGS THAT LOOK DESIGNED AND ARE NOT. Paul, 2026-09-13, and it is the reason this step exists rather than an extension of what is there.** **The HMRC Summary Export card was a first example**, built to show how an MTD export CSV and an SA103F CSV might be produced, and nothing more; it was never scoped as the shape of reporting. **The Archived periods card came from one requirement**: that an MTD export is kept as it was issued, for audit, rather than reconstructed later. **Paul's stated reason is HMRC's digital links rules, and he calls it perceived and probably correct rather than established. It is NOT verified here and must be checked against HMRC before anything is designed on it.** **THE CANDIDATE SET, agreed as roughly right by Paul on 2026-09-13 and not yet settled.** For filing: the MTD ITSA quarterly update, the SA103F annual which exists, and a VAT return at the nine boxes, where today's VAT Summary gives three figures. For the client: a readable profit and loss, which exists on screen with no export, and a year-end pack, for which `Handover Pack\` exists in the tree with nothing writing to it. For the practice: what needs attention, per client and across clients, and a trial balance, which the archive already keeps at account level. **WHAT MUST BE SETTLED IN THIS STEP AND IS NOT SETTLED HERE.** Which reports exist and what each contains, including whether a report carries the account-to-Final-Accounts-product mapping in force when it was made, item 151's question; which are kept as issued and which are produced on demand, and which of several runs for one period counts as the one to trust; which are per-client and which cross the practice; and the naming, because two buttons reading Export CSV for different outputs is what this step exists to stop. **Before 10i**, because it is near-term on Paul's instruction and because a pilot client on MTD would need a quarterly output that does not exist.

10u. **OUTSTANDING.** **The partner code guard, and the ten-partner cap. Paul's decision, 2026-09-13, closing outstanding item 153's remaining half, amendment 371.** `chartFor()` in `IntelliBooks-Desktop-v3.html` is to refuse rather than create a chart carrying a duplicate code, the same behaviour the Import chart control already has. **Partnerships are capped at ten partners** until a real client needs an eleventh, which stays refused rather than silently broken. **Not scheduled and not part of this step**: widening the reserved blocks, or hand-building a chart outside `chartFor()` for the one client that needs it, both raised as possible routes when the cap is actually hit and neither checked against the code.

10v. **OUTSTANDING.** **`COA_MASTER_v2.xlsx`'s Master sheet gains its six missing dropdowns, and the existing one extends to every row. Paul's decision, 2026-09-13, closing outstanding item 162, amendment 372.** Dropdowns are added for `type`, `status`, `applies_to`, `vat_default`, `mtd_itsa_category` and `classifier_eligible`, and `vat_variable`'s existing Yes/No list extends from row 232 to row 241. The permitted values came from Paul directly, from the workbook's own Rules and Read me sheets. **Paul's own hand-edit, not built by either session.**

10w. **OUTSTANDING.** **`worker\categorisation\engine.py:527`'s wrong argument name is renamed, not left. Paul's decision, 2026-09-13, closing outstanding item 54, amendment 373.** `vendor_key` is renamed to `vendor_code` at the three call sites that expect it, `upsert_client_vendor()`, `get_firm_vendor()` and `upsert_firm_vendor()`. Unreachable today, so nothing live changes; the fix matters once something calls the function. **Claude Code's to build.**

10x. **OUTSTANDING.** **`t.category` carrying the literal `"(Transfer)"` is corrected, not documented as a trap. Paul's decision, 2026-09-13, closing outstanding item 56, amendment 374.** A transaction that is one half of a linked transfer holds the string `"(Transfer)"` in `category` rather than a real account code or nothing. **The mechanism is not decided.** One direction raised and not chosen: give a transfer its own marker separate from `category`. Settled when this step is picked up.

10y. **OUTSTANDING.** **The malformed sidecar and the unsidecar'd PDF. Paul's decision, 2026-09-13, closing outstanding item 57, amendment 375.** A sidecar that fails to parse currently loses its image with it; the image is to be claimed before the parse is attempted, so a bad sidecar cannot take it down. A PDF with no sidecar is currently skipped outright by the image-only file-type test; it is to be corrected rather than left, mechanism, accept properly or refuse visibly, not yet settled. **Consultant session's to build.**

10z. **OUTSTANDING.** **The blank-category refusal at posting widens to cover an unknown category. Paul's decision, 2026-09-13, closing outstanding item 59, amendment 377.** Posting a single receipt already refuses with no category at all. Bulk posting already excludes a category with no VAT treatment, so an unmatched code cannot reach it that way. Single posting can still let one through if the operator clicks past the warning label. **The existing refusal widens rather than being replaced**: no category, or a category not in this client's chart, no post. **Consultant session's to build.**

10aa. **OUTSTANDING.** **The To Cashbook button is disabled on a row posting will refuse. Paul's decision, 2026-09-13, closing outstanding item 60, amendment 378.** A receipt with no amount already carries a red "No amount" pill on the Receipts tab; the button beside it does not check for the same thing and currently just leads to a message telling the operator to add an amount first. **The button greys out or hides wherever that pill already shows.** **Consultant session's to build.**

10ab. **OUTSTANDING.** **The rule duplicate check and the three-character minimum each extend to the door missing them. Paul's decision, 2026-09-13, closing outstanding item 61, amendment 379.** Adding a rule already refuses a duplicate pattern; editing an existing rule's pattern has no such check, so a duplicate can be created that way instead. The automatic rule created when an operator ticks "remember" already enforces a three-character minimum; the manual Add Rule box does not. **Both existing checks extend to the door each is currently missing from.** **Consultant session's to build.**

10ac. **OUTSTANDING.** **Deleting a category checks the Review queue too. Paul's decision, 2026-09-13, closing outstanding item 62, amendment 380.** Deleting a category already refuses if a transaction, a receipt or a statement rule points at it. Items in the Review queue are read live from disk, held separately from `books.receipts`, and are not covered, so a category still assigned to a pending Review item can be deleted with nothing said. **The same check extends to the Review queue.** **Consultant session's to build.**

10ad. **OUTSTANDING.** **A practice backup includes an orphaned books file rather than excluding it. Paul's decision, 2026-09-13, closing outstanding item 63, amendment 381.** A books file whose client has been removed from the practice list is currently found, excluded, and named in the message afterwards as not backed up. **It is included instead**, on the reasoning that a removed client is not the same as data deliberately discarded. Same message, worded as included. **Consultant session's to build.**

10ae. **OUTSTANDING.** **The Bank Transactions account filter keeps its selection across a redraw. Paul's decision, 2026-09-13, closing outstanding item 68, amendment 384.** `renderAccounts()` rebuilds the account filter dropdown with no attempt to keep the current selection, and runs before `renderBank()` reads it. **The current value is captured before the rebuild and restored after.** **Consultant session's to build.**

10af. **OUTSTANDING.** **A failed file read reports itself rather than staying silent. Paul's decision, 2026-09-13, closing outstanding item 69, amendment 385.** There is no in-app revert; it means copying one of many `.bak-before-X` files over the live one, none a complete snapshot. Several file reads sit inside a `try/catch` that swallows the error, so a bad read shows nothing. **File reads report a failure rather than doing nothing.** What, if anything, to do about the `.bak` files themselves is left to Paul, not scheduled. **Consultant session's to build.**

10ag. **OUTSTANDING.** **The `RESOLUTIONS_DIR` environment override is removed from the pipeline. Paul's decision, 2026-09-13, closing outstanding item 70, amendment 386.** `config.py` currently lets `RESOLUTIONS_DIR` be overridden by an environment variable; Desktop hardcodes the same folder and cannot read the pipeline's `.env` at all, so if the variable were ever set the two would silently disagree. **The override is removed, leaving one fixed location.** **Claude Code's to build.**

10ah. **OUTSTANDING.** **The stale three-digit test fixture becomes a real four-digit example. Paul's decision, 2026-09-13, closing outstanding item 71, amendment 387.** `test_vendor_import_requires_client_id.py` carries a `"103 Fuel,,,"` fixture. `seed_client_vendors.py`'s own parser accepts a GL code of any digit length, so the three digits were never a real constraint, only a stale example value, and this project's rule is that any three-digit code found anywhere is legacy. **The fixture is updated to a real four-digit example.** Whether the manual seeding tool itself stays in use is a separate question, left to Paul. **Claude Code's to build.**

10ai. **OUTSTANDING.** **The per-client capture report moves into the client's own folder. Paul's decision, 2026-09-13, closing outstanding item 73, amendment 388.** Step 10r moved exports out of the repository to one shared `Intellibills\Exports\` folder and left open whether they belonged in each client's own folder instead. `export_bookkeeping.py` writes one whole-database CSV across every client and firm with no client filter, and stays where it is. `capture_report.py` writes a report for one client alone. **It moves into that client's own folder, `Clients\{name}\...`, matching where receipts and statements already live.** **Claude Code's to build.**

10aj. **OUTSTANDING.** **An end-to-end test proves a statement can arrive and be filed. Paul's decision, 2026-09-13, closing outstanding item 74, amendment 389.** `file_statement()` is live and already the subject of real decisions since 10d.55. The unconfirmed part is the intake side: `scan_inbox()` treats an item as a statement purely on a `"type": "statement"` key in its sidecar, and nothing in this repository ever writes a sidecar carrying that key. The only plausible producer is the separate Uber statement parser project, and whether that link is wired up and has ever been exercised is unconfirmed from here. **A crafted statement sidecar is dropped into the inbox and the test confirms `scan_inbox()` and `file_statement()` carry it through to a filed document and a `statements` row.** Whether the Uber parser project itself actually produces one in practice is separate and outside this project. **Claude Code's to build.**

10ak. **OUTSTANDING.** **The phone capture app's Statements screen, alongside step 10aj. Paul's decision, 2026-09-13, closing outstanding item 81, narrowed, amendment 393.** The item this replaces claimed PHV mode across the capture app, statement routing and statement filing was all unbuilt. Statement filing is not: it is the same live code closed under item 74 and step 10aj. **What is genuinely still open is the phone capture app's own Statements screen**, a separate product, untouched by 10aj's test. **Paul asked whether the two steps should merge; they do not, since 10aj is a synthetic pipeline-side test and this is about whether the phone screen exists at all, but the two are looked at together.** **Consultant session's to build.**

10al. **OUTSTANDING.** **The four remaining Desktop checks, run on screen. Paul's decision, 2026-09-13, closing outstanding item 86, amendment 398; item 83 merged in the same day by amendment 402, closing outstanding item 83.** Seven checks were written after a 2026-08-17 fix. Three were already confirmed by reading the books files on disk, and the partnership check was run twice and passed. **Checks A, F, and the dialog-wording check the source report calls E have never been run.** **The source report itself carries a letter mismatch**: it calls the partnership check both E, in its own prose, and G, in its own numbered list; recorded here rather than silently corrected. **A fourth check joins these three, from change log item 30**: categorise an APPLE BILL transaction in TEST and confirm the toast's wording, then repeat on a supplier with no amount rules and confirm nothing extra shows. **Consultant session's to build.**

10am. **OUTSTANDING.** **A synthetic test forces a receipt through the Review seam. Paul's decision, 2026-09-13, closing outstanding item 88, amendment 399.** The Review seam has never been tested against a receipt the pipeline itself flagged for review, and the item as raised waited for one to occur naturally, which has no end point. **Decided: build and run a synthetic test that forces a receipt through Review deliberately**, the same shape as step 10aj's crafted statement test. **Consultant session's to build.**

10an. **OUTSTANDING.** **`handoverPack()`'s six files, run against a real client's books. Paul's decision, 2026-09-13, closing outstanding item 89, amendment 400.** The function writes transactions, receipts, accounts and a categories/mapping file, among others, into a client's Handover Pack folder, and has never been checked against a real client's books. **Decided: run it against Test Sole Trader and check the six files on disk.** **Consultant session's to build.**

10ao. **OUTSTANDING.** **The remaining untested Receipts-tab changes, run on screen. Paul's decision, 2026-09-13, closing outstanding item 90, amendment 401.** Change log items 19 and 21, and two of the three changes bundled in item 22, the Import Receipts modal and the removal of Post Unattached to Cashbook, have never been tested live. The other parts of items 19 to 23, and the weekly Netlify log review item 90 also named, are already covered elsewhere: three tested live on 2026-09-09, recorded in the change log, and the Netlify review inside step 10i's own exit criteria. **Consultant session's to build.**

10ap. **OUTSTANDING.** **`setup_auth.py` deleted, `RECEIPT_CAPTURE_GUIDE.md`'s two references corrected. Paul's decision, 2026-09-13, closing outstanding item 111, amendment 404.** The script imports a function that does not exist, `get_token`, and reads a config value that does not exist, `config.SHARED_MAILBOX`; it is written for the old Microsoft Graph route and the live pipeline reads email over IMAP. **Decided: delete it**, and correct the guide's two mentions of running it. **Claude Code's to build.**

10aq. **OUTSTANDING.** **The recovery sweep routes a fallback to Review instead of publishing it. Paul's decision, 2026-09-13, closing outstanding item 112, amendment 405.** The sweep for receipts marked `ok` but never published invents `invoice_date`, `supplier`, `gross` and `currency` silently when the extraction row it re-reads is missing them, and the invented date decides the tax year the receipt is filed under. **Decided: any of the four falling back sends the receipt to Review instead of publishing it**, the same gate the normal validation path already applies to a missing supplier, date or gross. A warning is logged either way, naming the field. **Claude Code's to build.**

10ar. **OUTSTANDING.** **`claimed_client_id` removed, dead since `capture_token` replaced a phone-supplied claim. Paul's decision, 2026-09-13, closing outstanding item 117, amendment 407.** The field is written into every sidecar and passed `None` at all four call sites; nothing populates it and nothing reads it, and the phone can no longer claim a client at all since `capture_token` took over identifying it. **Decided: remove it**, from `make_enriched_sidecar()` and its four call sites. **Claude Code's to build.**

10as. **OUTSTANDING.** **The toothless duplicate test in `test_extractor_name.py` is deleted. Paul's decision, 2026-09-13, closing outstanding item 121, amendment 410.** `test_name_matches_engine_recorded_on_a_result` repeats the assertion of the test above it and tests nothing about a failure path, despite its name and its comment. The real regression coverage already exists in `test_failure_path_engine.py`. **Decided: delete it.** **Claude Code's to build.**

10at. **OUTSTANDING.** **`export_bookkeeping.py`'s latest-attempt columns are read from one row, picked by timestamp. Paul's decision, 2026-09-13, closing outstanding item 126, amendment 413.** The export groups by receipt but leaves several columns, supplier, dates and amounts, unaggregated, so SQLite may take them from any one of several extraction attempts; and `MAX()` on `validation_status` and `validation_notes` approximates 'latest' by sorting the text alphabetically rather than by time, so a receipt whose latest attempt failed can export with an earlier attempt's higher-sorting status. **Decided: select each receipt's single latest extraction attempt by `extracted_at`, and read supplier, dates, amounts and status from that one row.** **Claude Code's to build.**

10au. **OUTSTANDING.** **The delivery-log reconciliation check is specified and built. Paul's decision, 2026-09-13, closing outstanding item 131, amendment 417.** Amendment 73 decided the shape: whether each client's folder matches IntelliBooks' own delivery log at `IntelliBooks\Delivery\{CODE}.log`, one entry per document copied out at posting, and moved section 13A's reconciliation to IntelliBooks when step 10b left the pipeline's build order. Nothing since has written what the check does on screen or scheduled it. **Decided: specify and build it.** **Consultant session's to build.**

10av. ~~**OUTSTANDING.**~~ **CANCELLED 2026-09-13, later the same day. Duplicate of item 114. Nothing to build.** **`write_review_file()` is deleted from `worker/filing.py`. Paul's decision, 2026-09-13, amendment 443.** Confirmed dead code: no caller anywhere in `app.py` or `worker/`, and the `{stem}.review.json` name it writes cannot be found by Desktop's `scanReview()` in any case. `file_review()` is the writer actually used, producing `{stem}{ext}.review.json`, and it works. Confirmed dead three times in this document, and amendment 443 claimed it had never been scheduled for deletion. ~~That claim was wrong.~~ **It was scheduled and built as item 114 of the 2026-09-12 dead-code brief, committed `8a30daa`, before amendment 443 scheduled it a second time. Caught reading `worker/filing.py`'s own comment at the deletion site and `2026-09-12_REPORT_claude_code_dead_code_and_docstring.md` directly, 2026-09-13.**

10aw. **OUTSTANDING.** **The null-id filename fallback is fixed. Paul's decision, 2026-09-13, amendment 444.** Recorded at 12.3 step 2 and amendment 48: when a back-feed note carries no `receipt_id`, the pipeline falls back to matching `original_review_files` against `receipts.filename`. `file_review()`'s uniqueness function can suffix the on-disk filename to avoid a collision; `receipts.filename` holds the name without that suffix; so the match can fail on a genuine note, which is misrouted to `Resolutions\failed\` rather than applied. Only bites when both hold: no id on the note, and a filename collision occurred. **Claude Code's to read the code and decide the fix; the mechanism is not prescribed here.**

10ax. **OUTSTANDING.** **`copy_for_published_receipt()`'s call to `repo.mark_receipt_filed()` gets the same error handling as the write beside it. Paul's decision, 2026-09-13, amendment 446.** Found checking whether the pipeline's single client-folder writer shared 17.4's Desktop finding on a partial filing window; it does not, because it writes one file only. But `write_client_copy()` at `worker/client_copy.py:544` is wrapped in try/except and the very next call, `repo.mark_receipt_filed()` at line 568, is not, though the function's docstring says it never raises. A database failure there would leave a correctly-copied document with `filed_path` still NULL. Briefed in `PROMPT_claude_code_2026-09-13_client_copy_filed_path_error_handling.md`. **Claude Code's to build.**

10i. **OUTSTANDING.** **The pilot.** One real client for two weeks before wider rollout. ~~10g~~ ~~10h~~ **Renumbered twice on 2026-08-20, by amendments 118 and 119.** **Its procedure is already specified** in section 8 of `IntelliBooks\App\Docs\IntelliBooks-System-Specification.md`, which sets the exit criteria and names three tests nothing else does: a deliberately bad photograph through the Review path, an airplane-mode retry from the queue, and a Netlify log review at weeks one and two. **Nothing above may be deferred past this point**, because after it the data is a real client's and every change becomes a migration.

**Console:**

11. **OUTSTANDING.** Schema additions. ~~including `coa_accounts`~~ **Struck 2026-08-21 by amendment 124 on Paul's instruction: `coa_accounts` is not created and loading the chart into the database stays cancelled until he says otherwise.** The chart is read from `IntelliCharts\Chart Library\Master_COA.csv`, corrected 2026-09-02 by amendment 173 from `COA_MASTER_v1.csv`. Verify `init_db()` is still idempotent against the live DB.
12. **CANCELLED.** ~~Load the app default chart into `coa_accounts` with `scope='app_default'` and `firm_id` NULL.~~ ~~No file is named, because no app default chart exists yet.~~ **Cancelled 2026-08-17, amendment 96. There is nothing to load and nowhere to load it.** ~~Load `chart_of_accounts_APP_DEFAULT_2026-08-03.csv`.~~ ~~Load `chart_of_accounts_DRAFT.csv` with `scope='default'`.~~ **Both struck 2026-08-03 on Paul's instruction, amendment 95.** The first named a file built without being asked for, since deleted. The second names a file that is not a chart of accounts at all. ~~**This step is blocked until an agreed app default chart exists. Do not substitute a file for it.**~~ **Amended 2026-08-17, amendment 96: an app default chart now exists, and this step is cancelled rather than unblocked.** `coa_accounts` is not created, so there is nothing to load it into. The master is `IntelliCharts\COA_MASTER_v2.xlsx` and the published charts are read from `IntelliCharts\Chart Library\`, corrected 2026-09-02 by amendment 173. **If the console ever needs its own copy, that is a new decision and this step number is free.**
13. **OUTSTANDING.** Token usage capture and `extraction_usage`.
14. **OUTSTANDING.** Auth: `console_users`, `create_console_user.py`, login, deny-by-default, CSRF, `?next=`. Tests 33 to 35, 37.
15. **OUTSTANDING.** Read queries, status page, queue page.
16. **OUTSTANDING.** Receipt detail page, correction form, GL control. Tests 18, 19, 36.
17. **OUTSTANDING.** `tax_year_bounds()`, then the browse page. Tests 29 to 32. **Carries a decision from 17.4, amendment 448, 2026-09-13: the browse page exports CSV, reusing `export_bookkeeping.py`'s logic, taken after step 10at's fix to it lands rather than as the script stands today.**
18. **OUTSTANDING.** Intake panel and the registry reload. ~~the clients.csv reload mechanism~~ **Corrected 2026-08-21 by amendment 125:** step 10d replaces `clients.csv` with `clients.json`, so the step can no longer be written against the CSV. ~~The mechanism itself is specified at 8.6. **Whether it applies to `clients.json` at all is open, see outstanding item 142**, and that question belongs with the step 10d briefs.~~ **Settled the same day by amendment 132 and no longer open: the pipeline re-reads the registry on modification time, built at sub-step 10d.35, and 8.6's marker file is struck.** So by the time this step is reached the reload exists and what remains here is the intake panel. **Carries a decision from 17.4, amendment 449, 2026-09-13: the panel must filter out `receipt_id` values `recent-receipt` and `56b29977`, three rows the test suite wrote into the live event logs before its isolation bug was fixed. The logs themselves are left untouched; the panel excludes these two values when it reads them.**
19. ~~**OUTSTANDING.**~~ **CANCELLED 2026-09-13 by amendment 415, Paul's decision closing outstanding item 128.** Costs API client and the spend panel. **Paul will monitor OpenAI spend directly at openai.com instead**, so the two open credential questions this step was waiting on, an org-level Admin key on this workstation and a dedicated key or project for clean cost attribution, no longer need answering. Section 9.3 stays as written, a design that is not being built.
20. **OUTSTANDING.** Billing-error classification, 3.9.
21. **OUTSTANDING.** `RECEIPT_CAPTURE_GUIDE.md`: starting the console, login, resolving through the UI, the GL override, finding a client's receipts for a tax year, that both tools can resolve safely, and OpenAI auto-recharge as a setup step.
22. **OUTSTANDING.** `CLAUDE.md`: new tables, the resolution service boundary, the back-feed contract, and the rule that the domain layer stays free of web imports. **Also the Claude Code permissions setup**, added 2026-07-28: the pre-approved command list lives in `.claude/settings.local.json`, because **allow rules in `.claude/settings.json` are ignored unless the workspace is trusted, while the local file's are not**. That cost three attempts to find. `.claude/settings.json` holds the same content and is committed, so a fresh checkout can recreate the local file from it with a single copy; until someone does, every command prompts. `.claude/settings.local.json` is gitignored and machine-local by design. This belongs here rather than in `RECEIPT_CAPTURE_GUIDE.md`, which is written for the day-to-day operator and has no business carrying developer environment setup. Leave the existing AUTOMATIC Task Mode section alone while doing this step.

Steps 1 to 10 are worth doing even if the console slips. They fix live bugs and close the divergence.

---

### 16A. Appendix: superseded headline history

Moved out of the live headline line above, 2026-09-14, so that line stays short and readable in ordinary tools. Nothing here is edited: each entry is exactly as it stood in the live line before this move, struck text struck exactly as before, standing notes standing exactly as before. Kept in original order. This appendix itself only grows by appending.

~~**Where we are, as at 2026-09-07. 23 built, 17 outstanding, 1 cancelled, 1 moved out of this order, 42 steps.**~~

~~**Where we are, as at 2026-09-12. 29 built, 13 outstanding, 1 cancelled, 1 moved out of this order, 44 steps. Amendment 347. Counted from the table's own 44 rows, not carried from the line above: steps 10k, 10l, 10m and 10n went BUILT since this was last written, and 10p was added by amendment 340. The thirteen outstanding are 10p, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-12. 29 built, 14 outstanding, 1 cancelled, 1 moved out of this order, 45 steps. Corrected the same day by amendment 349, which added step 10q. Counted from the table's own 45 rows, not carried from the line above. The fourteen outstanding are 10p, 10q, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-12, later the same day. 30 built, 13 outstanding, 1 cancelled, 1 moved out of this order, 45 steps. Corrected by amendment 353, which built step 10p's Firm Settings half and closed the step. Counted from the table's own 45 rows, not carried from the line above. The thirteen outstanding are 10q, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-12, later still. 31 built, 12 outstanding, 1 cancelled, 1 moved out of this order, 45 steps. Corrected by amendment 355, which built step 10q's remaining piece, F19, and closed the step. Counted from the table's own 45 rows, not carried from the line above. The twelve outstanding are 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-12, later still again. 31 built, 13 outstanding, 1 cancelled, 1 moved out of this order, 46 steps. Corrected by amendment 359, which added step 10r. Counted from the table's own 46 rows, not carried from the line above. The thirteen outstanding are 10r, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-12, last of the day. 32 built, 13 outstanding, 1 cancelled, 1 moved out of this order, 47 steps. Corrected by amendment 360, which added step 10s and built it in the same hour. Counted from the table's own 47 rows, not carried from the line above. The thirteen outstanding are unchanged: 10r, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, later again. 32 built, 42 outstanding, 3 cancelled, 1 moved out of this order, 78 steps. Corrected 2026-09-13, later again, when amendment 443 was found to have scheduled already-completed work a second time: step 10av is a duplicate of item 114, built 2026-09-12 as commit `8a30daa`. Amendment 443 is struck and corrected in place. Counted from the table's own 78 rows, not carried from the line above. The forty-two outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10aw, 10ax, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more since all of that. 32 built, 43 outstanding, 3 cancelled, 1 moved out of this order, 79 steps. Corrected by amendment 450, which added step 10ay: `_retry_failed_receipts()`'s failure handler no longer assumes no extraction row was written this attempt. Counted from the table's own 79 rows, not carried from the line above. The forty-three outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10aw, 10ax, 10ay, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-14. 33 built, 42 outstanding, 3 cancelled, 1 moved out of this order, 79 steps. Corrected by amendment 451, which reclassified step 10y BUILT: its fix was made, but the function it fixes has no caller today, so it is not live work and not carried as outstanding. Counted from the table's own 79 rows, not carried from the line above. The forty-two outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10aw, 10ax, 10ay, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-14, later the same day. 46 built, 29 outstanding, 3 cancelled, 1 moved out of this order, 79 steps. Corrected by amendments 452, 453 and 454, which took thirteen steps to BUILT in one pass: 10z, 10aa, 10ab, 10ac, 10ad, 10ae by the consultant session, and 10ag, 10ah, 10ap, 10ar, 10as, 10ax, 10ay by Claude Code. Counted from the table's own 79 rows, not carried from the line above. The twenty-nine outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10af, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10aq, 10at, 10au, 10aw, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-14, later again. 48 built, 28 outstanding, 3 cancelled, 1 moved out of this order, 80 steps. Corrected by amendments 455 and 456: 10u and 10af to BUILT, 10au narrowed to a Desktop-side check blocked on a new step, 10az, which schedules the delivery-log writer Claude Code has not yet built. Counted from the table's own 80 rows, not carried from the line above. The twenty-eight outstanding are 10r, 10t, 10v, 10w, 10x, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10aq, 10at, 10au, 10aw, 10az, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, later once more. 32 built, 43 outstanding, 2 cancelled, 1 moved out of this order, 78 steps. Corrected by amendment 446, which added step 10ax, scheduling error handling for `mark_receipt_filed()` in `copy_for_published_receipt()`. Counted from the table's own 78 rows, not carried from the line above. The forty-three outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10av, 10aw, 10ax, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.** ~~

~~**Where we are, as at 2026-09-13, and again after that. 32 built, 42 outstanding, 2 cancelled, 1 moved out of this order, 77 steps. Corrected by amendment 444, which added step 10aw, scheduling the null-id filename fallback fix. Counted from the table's own 77 rows, not carried from the line above. The forty-two outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10av, 10aw, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more again, and once more still, and once more after that, and once more besides, and once more since. 32 built, 41 outstanding, 2 cancelled, 1 moved out of this order, 76 steps. Corrected by amendment 443, which added step 10av, scheduling the deletion of `write_review_file()`. Counted from the table's own 76 rows, not carried from the line above. The forty-one outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10av, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more again, and once more still, and once more after that, and once more besides. 32 built, 40 outstanding, 2 cancelled, 1 moved out of this order, 75 steps. Corrected by amendments 415 and 417, which cancelled step 19 and added step 10au, closing outstanding items 128 and 131. Counted from the table's own 75 rows, not carried from the line above. The forty outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10au, 10i, 11, 13, 14, 15, 16, 17, 18, 20, 21 and 22.**~~

~~Where we are, as at 2026-09-13, once more again, and once more still, and once more after that. 32 built, 40 outstanding, 1 cancelled, 1 moved out of this order, 74 steps. Corrected by amendment 413, which added step 10at, closing outstanding item 126. Counted from the table's own 74 rows, not carried from the line above. The forty outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10at, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.~~

~~Where we are, as at 2026-09-13, once more again, and once more still. 32 built, 39 outstanding, 1 cancelled, 1 moved out of this order, 73 steps. Corrected by amendment 410, which added step 10as, closing outstanding item 121. Counted from the table's own 73 rows, not carried from the line above. The thirty-nine outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10as, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.~~

~~**Where we are, as at 2026-09-13, once more again, later yet again. 32 built, 38 outstanding, 1 cancelled, 1 moved out of this order, 72 steps. Corrected by amendment 407, which added step 10ar, closing outstanding item 117. Counted from the table's own 72 rows, not carried from the line above. The thirty-eight outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10ar, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more again, later yet. 32 built, 37 outstanding, 1 cancelled, 1 moved out of this order, 71 steps. Corrected by amendment 405, which added step 10aq, closing outstanding item 112. Counted from the table's own 71 rows, not carried from the line above. The thirty-seven outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10aq, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more again, later still. 32 built, 36 outstanding, 1 cancelled, 1 moved out of this order, 70 steps. Corrected by amendment 404, which added step 10ap, closing outstanding item 111. Counted from the table's own 70 rows, not carried from the line above. The thirty-six outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10ap, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, once more again. 32 built, 35 outstanding, 1 cancelled, 1 moved out of this order, 69 steps. Corrected by amendments 400 and 401, which added steps 10an and 10ao, closing outstanding items 89 and 90. Counted from the table's own 69 rows, not carried from the line above. The thirty-five outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10an, 10ao, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, still later again, and again, and once more, and again after that. 32 built, 33 outstanding, 1 cancelled, 1 moved out of this order, 67 steps. Corrected by amendments 398 and 399, which added steps 10al and 10am, closing outstanding items 86 and 88. Counted from the table's own 67 rows, not carried from the line above. The thirty-three outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10al, 10am, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, still later again, and again, and once more. 32 built, 31 outstanding, 1 cancelled, 1 moved out of this order, 65 steps. Corrected by amendment 393, which added step 10ak, closing outstanding item 81. Counted from the table's own 65 rows, not carried from the line above. The thirty-one outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10ak, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, still later again, and again. 32 built, 30 outstanding, 1 cancelled, 1 moved out of this order, 64 steps. Corrected by amendments 387, 388 and 389, which added steps 10ah, 10ai and 10aj, closing outstanding items 71, 73 and 74. Counted from the table's own 64 rows, not carried from the line above. The thirty outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10ah, 10ai, 10aj, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, still later again. 32 built, 27 outstanding, 1 cancelled, 1 moved out of this order, 61 steps. Corrected by amendments 384, 385 and 386, which added steps 10ae, 10af and 10ag, closing outstanding items 68, 69 and 70. Counted from the table's own 61 rows, not carried from the line above. The twenty-seven outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10ae, 10af, 10ag, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, still later. 32 built, 24 outstanding, 1 cancelled, 1 moved out of this order, 58 steps. Corrected by amendment 381, which added step 10ad, closing outstanding item 63. Counted from the table's own 58 rows, not carried from the line above. The twenty-four outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10ad, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, last of the day so far. 32 built, 23 outstanding, 1 cancelled, 1 moved out of this order, 57 steps. Corrected by amendments 378, 379 and 380, which added steps 10aa, 10ab and 10ac, closing outstanding items 60, 61 and 62. Counted from the table's own 57 rows, not carried from the line above. The step-letter family passes 10z and continues 10aa, 10ab and so on, by amendment 378. The twenty-three outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10aa, 10ab, 10ac, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, later again. 32 built, 20 outstanding, 1 cancelled, 1 moved out of this order, 54 steps. Corrected by amendments 375 and 377, which added steps 10y and 10z, closing outstanding items 57 and 59. Counted from the table's own 54 rows, not carried from the line above. The twenty outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10y, 10z, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, later still. 32 built, 18 outstanding, 1 cancelled, 1 moved out of this order, 52 steps. Corrected by amendments 372, 373 and 374, which added steps 10v, 10w and 10x, closing outstanding items 162, 54 and 56. Counted from the table's own 52 rows, not carried from the line above. The eighteen outstanding are 10r, 10t, 10u, 10v, 10w, 10x, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13, later the same day. 32 built, 15 outstanding, 1 cancelled, 1 moved out of this order, 49 steps. Corrected by amendment 371, which added step 10u, the partner code guard and the ten-partner cap. Counted from the table's own 49 rows, not carried from the line above. The fifteen outstanding are 10r, 10t, 10u, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~**Where we are, as at 2026-09-13. 32 built, 14 outstanding, 1 cancelled, 1 moved out of this order, 48 steps. Corrected by amendment 369, which added step 10t, the reports set. Counted from the table's own 48 rows, not carried from the line above. The fourteen outstanding are 10r, 10t, 10i, 11, 13, 14, 15, 16, 17, 18, 19, 20, 21 and 22.**~~

~~As at 2026-09-06. 22 built, 18 outstanding, 1 cancelled, 1 moved out of this order, 42 steps.~~

**Corrected 2026-09-07 by amendment 266, which built sub-step 10e.15 and with it the whole of step 10e. Counted from the table's own rows, not carried from the line above. This is the first change to the step counts since amendment 172 built step 10a on 2026-09-02.**

**Unchanged by amendment 265, which built sub-step 10e.14: step 10e stays OUTSTANDING because 10e.15 is, and it is the last one.**

**Unchanged by amendment 259, which built sub-steps 10e.1 and 10e.2: step 10e stays OUTSTANDING because 10e.14 and 10e.15 are, and they are the only two left.**

**Unchanged by amendment 258, which built sub-step 10e.9: step 10e stays OUTSTANDING while any sub-step of it is OUTSTANDING, and 10e.1, 10e.2, 10e.14 and 10e.15 are. The named BUILT sub-step list moves with it.**

**Unchanged by amendment 243, which built sub-steps 10e.11 and 10e.12: step 10e stays OUTSTANDING while any sub-step of it is OUTSTANDING, and six are. The BUILT sub-step list below moves with it.**

~~22 built, 17 outstanding, 1 cancelled, 1 moved out of this order, 41 steps.~~

**Corrected 2026-09-06 by amendment 238, which added step 10m.**

~~22 built, 15 outstanding, 1 cancelled, 1 moved out of this order, 39 steps.~~

**Corrected 2026-09-06 by amendments 235 and 237, which added steps 10k and 10l. Counted from the table's own rows.**

~~As at 2026-09-05. 21 built, 16 outstanding.~~

**Corrected 2026-09-06 by amendment 233, which built 10j.11 and with it the whole of step 10j. Counted from the table's own rows, not carried from this line.**

**Re-counted from this table's own 39 rows on 2026-09-05 by amendment 229 and unchanged. 10j.10 going BUILT moves nothing here, because step 10j stays OUTSTANDING while 10j.11 is OUTSTANDING.**

~~21 built, 15 outstanding, 1 cancelled, 1 moved out of this order, 38 steps.~~

**Corrected 2026-09-05 when step 10j was added by amendment 225.**

~~As at 2026-09-03. 20 built, 16 outstanding.~~

**Corrected 2026-09-05, counted from the table rather than from this line. Step 10h went BUILT on 2026-09-04 by amendment 208 and this head line was not moved with it, which is the thing it says immediately below must happen in the same edit.**

~~As at 2026-09-02. 19 built, 17 outstanding, 1 cancelled, 1 moved out of this order, 38 steps.~~

~~As at 2026-09-01. 18 built, 18 outstanding.~~

~~The step counts are unchanged since 2026-08-21 and are correct; the date was not.~~

**Both struck 2026-09-02 by amendment 172, which built step 10a. That is the first change to the step counts since 2026-08-21.**

**Below the table, six steps are decomposed into sub-steps: 10a, 10d, 10e, 10f, 10g and 10j.**

~~five steps are decomposed into sub-steps: 10a, 10d, 10e, 10f and 10g.~~

**Corrected 2026-09-05 by amendment 229: amendment 225 decomposed 10j into 11 sub-steps earlier the same day and this line was not moved with it.**

**No sub-step total is stated here, by amendment 166. The BUILT sub-steps are named, not counted, for the same reason: 10a.1, 10a.2, 10a.3, 10e.1, 10e.2, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.9, 10e.10, 10e.11, 10e.12, 10e.13, 10e.14, 10e.15, **

10f.18, 10f.19, 10f.20, 10f.21, 10f.22, 10f.23, 10f.31, 10f.32, 10f.33, 10f.35,

** and 10j.1 to 10j.11.**

**The seven 10f sub-steps were added 2026-09-07 by amendment 268. Step 10f stays OUTSTANDING because 24 of its 31 sub-steps are, so the step counts do not move.**

**Corrected 2026-09-07 by amendment 266, which built 10e.15: every sub-step of 10e is now BUILT or CANCELLED and the step is BUILT.**

~~10a.1, 10a.2, 10a.3, 10e.1, 10e.2, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.9, 10e.10, 10e.11, 10e.12, 10e.13, 10e.14, and 10j.1 to 10j.11.~~

**Corrected 2026-09-07 by amendment 265, which built 10e.14. 10e.15 is the only sub-step of 10e left.**

~~10a.1, 10a.2, 10a.3, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.9, 10e.10, 10e.11, 10e.12, 10e.13, and 10j.1 to 10j.11.~~

**Corrected 2026-09-07 by amendment 259, which built 10e.1 and 10e.2.**

~~10a.1, 10a.2, 10a.3, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.10, 10e.11, 10e.12, 10e.13, and 10j.1 to 10j.11.~~

**Corrected 2026-09-07 by amendment 258, which built 10e.9.**

~~10a.1, 10a.2, 10a.3, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.11, 10e.12, 10e.13, and 10j.1 to 10j.11.~~

**Corrected 2026-09-06 by amendment 244, which built 10e.10.**

~~10a.1, 10a.2, 10a.3, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8, 10e.13, and 10j.1 to 10j.10.~~

**Corrected 2026-09-06 by amendment 243, which built 10e.11 and 10e.12. 10j.11 was built by amendment 233 and this line was not moved with it.**

~~10a.1, 10a.2, 10a.3, 10e.3, 10e.4, 10e.5, 10e.7, 10e.8 and 10e.13.~~

**Corrected 2026-09-05 by amendment 229. 10j.1 to 10j.9 were built earlier the same day by amendments 224, 226 and 227 and were never named here; 10j.10 is built by this amendment.**

~~10e.3, 10e.4, 10e.5, 10e.7, 10e.8 and 10e.13, and 10a.3.~~

**Corrected 2026-09-02 by amendment 172: every sub-step of 10a is now BUILT.**

Every other sub-step of those five steps is OUTSTANDING.

** ~~Six sub-steps are BUILT and every other sub-step of those five steps is OUTSTANDING.~~ **

Struck 2026-09-02 by amendment 171, which built 10a.3 and made the six seven within the hour. That is the third derived count on this line to go stale in two days.

** ~~four steps are decomposed into sub-steps: 10d, 10e, 10f and 10g~~ **

Corrected 2026-09-02 by amendment 170, which decomposed 10a.

** ~~Below the table, 105 sub-steps: 6 BUILT and 99 OUTSTANDING.~~ **

Struck 2026-09-02 by amendment 166. Both figures were right until amendment 164 added 10d.51 and 10d.52 and made them 107 and 101, and the amendment that moved the count did not strike the line that stated it.

** ~~The six are~~ **

Those six of the seven are

** 10e.3, 10e.4, 10e.5, 10e.7, 10e.8 and 10e.13, built 2026-09-01 by the consultant session, which Paul had put on `IntelliBooks-Desktop-v3.html`. **

Step 10e stays OUTSTANDING because sub-steps of it are still OUTSTANDING

**, which is why the step counts did not move. ~~because nine of its fifteen sub-steps are~~ **

Struck 2026-09-02 by amendment 167, on the same ground as amendment 166: it is a derived count in a step that is still being decomposed. It was correct when written.**

~~As at 2026-08-21.~~

---

## 17. Documents and remaining questions

### 17.1 Ownership

| Document | Owner |
|---|---|
| This file | Cowork design session |
| `IntelliBooks-System-Specification.md` | Cowork design session, bump to v1.1 |
| `IntelliBooks-System-Overview.md` | Cowork design session |
| `CLAUDE.md`, `RECEIPT_CAPTURE_GUIDE.md` | Implementation session, steps 21 and 22 |
| `IntelliBooks-Change-Log.md` | The IntelliBooks build session, items 24 to 26 only |

Two sessions editing whole-system documents independently is how the drift began.

### 17.2 Corrections needed to the specification

It is v1.0 of 15 July and describes itself as the single source of truth. Bump to v1.1, keep superseded wording visible with its reason, so the decision trail survives.

- Review holding is ~~`Clients\{Name}\Review\`, not `Receipt Inbox\{CODE}\Review\`~~ **`Intellibills\Review\{client_id}\`**. Sections 3 and 5.3 item 6. Change log item 18 removed `Receipt Inbox\{CODE}\Review\`. **Struck 2026-09-02 by amendment 171.** **This row corrected one wrong path to another wrong path: 18.2a moved Review out of the client folder altogether and 10d.54 keys it on the client id.**
- Books live at `IntelliBooks\Books\{CODE}-books.json`. Section 3 table and 5.4 item 1. Item 12 moved them.
- Section 3 predates item 12's `IntelliBooks\` versus `Clients\` split, so Receipt Inbox is `IntelliBooks\Receipt Inbox\{CODE}\`.
- Section 5.4 item 6's "Import Client's Receipt Folder" was removed in item 22.
- Section 4.3's Corrections rule: record the original, then the back-feed and the three defects that forced it.
- Section 4.3's lifecycle diagram says "human fixes, reprocess", which matches neither implementation.
- Section 2: add the console as a sixth component.
- Section 5.3 item 4's internal store path may have diverged from `CLAUDE.md`. Verify rather than assume.
- Nothing records `possible_duplicate`, `retry_exhausted`, receipt locking, the 7-day retry cap or the IMAP Message-ID fix.

### 17.3 Corrections needed to the overview

More current, 19 July, but: line 28 describes Desktop's review-and-file flow as *the* way to complete reviews; line 36's "the desktop app never writes to receipts.db" needs the note-based nuance; neither `resolve_receipt.py` nor the console appears.

### 17.4 Open questions for Paul

> **Amended 2026-07-30.** Section 18 closed a number of the questions below and 18.10 carries the three that remain live and urgent: categories in receipts and transactions, ~~extending `chart_of_accounts_DRAFT.csv`~~, and whether a filed receipt gets a correction route. **The middle one was closed 2026-08-17 by amendment 96 and the file was deleted 2026-09-01. Struck 2026-09-02 by amendment 167. Two of the three remain.** Read 18.10 first; several items below are settled and kept only for the trail.

- ~~Confirm the revised answer to the category-conflict question, section 14, bullet 5: receipt wins on high confidence, rule wins on low, flag either way.~~ **Closed 2026-07-30, amendment 67. The rule is cancelled and the bullet deleted. Do not reopen.**
- ~~Extend `chart_of_accounts_DRAFT.csv` with income, equity and remaining balance sheet accounts.~~ **Struck 2026-09-02 by amendment 167.** ~~Not blocking; the 23 expense accounts cover the receipts module.~~ **Corrected 2026-08-03, amendment 90, after parsing the file rather than repeating this line.** It is 23 data rows in total, not 23 expense accounts: **20 `expenses`, 2 `assets` and 1 `liabilities`**, which is a statement about blob `46c04a03` and stays true. ~~And it is blocking, per 18.10.~~ **Struck 2026-09-02 by amendment 167. Closed 2026-08-17 by amendment 96 without anything being extended, and the file was deleted 2026-09-01.**
- ~~Issue a dedicated OpenAI API key or project for this app, needed for clean cost attribution (9.3).~~ **Moot, amendment 436. Amendment 415 already closed outstanding item 128 and cancelled step 19: Paul monitors OpenAI spend directly at openai.com, so this question does not need answering.**
- ~~Whether an org-level OpenAI Admin key on this workstation is acceptable. If not, 9.3 is skipped and the local ledger stands alone.~~ **Moot, amendment 436, same ground as the line above.**
- ~~Whether `export_bookkeeping.py` needs the effective-GL-code treatment (11.2).~~ **Answered 2026-07-27: it cannot need it, because it exports no category at all.** The script is 45 lines and selects `receipt_id`, `firm_id`, `client_id`, `supplier_name`, `invoice_date`, `net_amount`, `vat_amount`, `gross_amount`, `currency`, `status`, `MIN(extracted_at)`, `MAX(validation_status)` and `MAX(validation_notes)`. No `categorisations` join, no nominal code. So 11.2's effective-code rule has no route into it, and whether a bookkeeping export **should** carry the GL code is a new and separate question. It probably should, given that is the point of categorising, but it is a change of purpose rather than a bug fix.
  Two real defects found in it while answering, both out of scope and neither touched. First, `e.supplier_name` and the other bare `e.*` columns sit outside the `GROUP BY`, so SQLite takes them from an arbitrary row in the group: a receipt with three extraction attempts can export the supplier from one attempt and the amounts from another. Second, `MAX(e.validation_status)` and `MAX(e.validation_notes)` are aliased `latest_*`, but `MAX` on text is alphabetical, not latest, and `MAX('ok','failed')` is `'ok'`, so a receipt whose most recent attempt failed can export as `ok`. Both matter more than the missing GL code, because both silently produce a wrong figure or a wrong status in something called a bookkeeping export. **Decided and scheduled, amendment 437: Paul's decision, 2026-09-13, closing outstanding item 126, is to select each receipt's single latest extraction attempt by `extracted_at` and read supplier, dates, amounts and status from that one row. Section 16's new step 10at, OUTSTANDING, Claude Code's to build. Not yet built.**
- ~~Whether the browse page should export CSV. `export_bookkeeping.py` already exists and two divergent export formats is worse than one; if yes, reuse its logic.~~ **Decided, amendment 448. Paul, 2026-09-13: yes, reuse `export_bookkeeping.py`. Carried onto section 16 step 17's own entry.**
- ~~Confirm IntelliBooks change log item 19 has been tested end to end before the back-feed is built on it. Note that testing it creates the divergence deliberately, so reset the receipt's DB status afterwards.~~ **Confirmed, amendment 438. Confirmed live 2026-07-28, on receipt `c5a3fccd`: the four form validations behave, the image and sidecar are written correctly, the books entry carries the corrected figures, and Desktop removed the Review pair itself, recorded in the body of this document.**

Added 2026-07-26, neither blocking phase 0:

- ~~**How should the CLI express "clear this field"?** `parse_corrections` in 4.2 distinguishes omission from an explicit clear, where `""` means clear. `resolve_receipt.py`'s interactive prompt already treats a blank answer as "keep existing", which is documented in `RECEIPT_CAPTURE_GUIDE.md` and must not change. So the CLI currently has no way to clear a wrongly extracted reference number, while the console will have one. Options: a typed sentinel, a `--clear field` flag, or accept the asymmetry and leave clearing to the console. Do not invent a sentinel without deciding.~~ **Decided, amendment 442. Paul, 2026-09-13: not built. Desktop's Edit Receipt window already covers clearing net and VAT, and is the route actually used; the CLI's flag mode already clears a field with an empty string, and nothing shows the CLI's interactive mode is used at all.**
- ~~**Should `add_validation_note()` stop mutating extraction rows?** It runs `UPDATE extractions SET validation_notes = ?` on the latest row, at `repository.py:571`. `CLAUDE.md` says extractions are append-only, and 4.3 step 6 tells the resolution service to call it on a `still_invalid` outcome, so the service as specified mutates an append-only row. Either notes are agreed to be mutable metadata while values are not, and `CLAUDE.md` says so explicitly, or the `still_invalid` path appends a new `manual_correction` row carrying the notes instead. Decide before step 8. Phase 0 step 1 already took the second route for the missing-file branch, see 3.1.~~ **Decided, amendment 439. `add_validation_note()` is retired: a `still_invalid` outcome appends a new extraction row carrying the notes instead of mutating one. Confirmed BUILT at section 16 step 9.**
- ~~Should the `category` key be backfilled in the 32 sidecars already filed?~~ **Asked and answered 2026-07-27: no. Paul confirms every filed receipt and sidecar on disk is test data, on test clients and his own record, not a third party's books.** So there is nothing to preserve and nothing to correct. Step 5 fixes the writer, the existing files stay as they are, and if they are ever in the way they can be deleted rather than rewritten. Anyone reading 3.7's table later should read it as forensic evidence of how the field drifted, not as a client data problem.
- ~~Fix the four small defects the step 6 move exposed, or carry on?~~ **Decided 2026-07-27: fix them now, as step 6b, before step 7.** Findings 1, 2, 6 and 7 in 10.2. Findings 3, 4 and 5 stay deferred. Original reasoning kept below. Finding 2 is the one that argues for doing it: for a receipt that prints an ISO date, the day-first fix does not apply at all, and it fails silently, so nobody would find it without reading the code. Finding 1 puts a false label on the note an operator reads to decide whether to trust a date. Finding 6 is three `logger.warning` calls that change nothing on the happy path. Finding 7 is two lines of test hygiene, now unblocked because step 6 is committed. Together they are one small commit each in a single area, with tests, and they are cheaper to do now while the code is fresh than after the console is built on top of it. Against: it is not in the agreed phase 0 list and it delays step 7. Findings 3, 4 and 5 should wait either way.
- ~~**A receipt filed with no category: where the risk actually is.** Added 2026-07-28, found during the item 19 test, and **corrected by Paul the same day.**~~
  ~~Superseded framing, recorded because it was wrong in a way worth not repeating: I first wrote that an uncategorised receipt is invisible in the HMRC and P&L reports it should appear in. That is not how this system works. **Receipts do not map to HMRC boxes or to the P&L. Transactions do.** A receipt is a document; a receipt can be attached to a transaction, and a transaction can be created from a receipt. The accounting record is the transaction.~~
  ~~So filing a receipt with a blank category is not itself an accounting gap. The risk is one step removed, at `postReceiptToCashbook()`, `IntelliBooks-Desktop-v3.html:1659`, which creates a transaction from a receipt and does `t.category = r.category || ""`. A blank category on the receipt therefore produces a transaction with a blank category, and **that** is the record the reports read. The toast it shows says "Review the category, then Post", so the flow expects the operator to fix it, which is a prompt rather than a control.~~
  ~~The other route is attaching a receipt to an existing transaction, where the transaction already has its own category from a statement rule. That is the disagreement handled by the category-conflict rule in section 14: receipt wins on high confidence, statement rule wins on low, flag either way, never auto-update the rule.~~
  ~~So the question narrows usefully: not "should Desktop require a category before filing", but **"should a transaction be allowed to reach the books with a blank category"**. Paul's call, it lives in Desktop rather than here, and it belongs in the brief for the IntelliBooks session.~~ **Answered, amendment 440. Paul, 2026-07-28, section 14: the gate sits on the transaction, not the receipt, and a transaction must not be posted with a blank category. Section 18.5a builds this as a hard rule rather than a prompt: a transaction cannot be posted blank because a blank category means no rate, and a VAT amount cannot exist without one. Section 18.9's cancellation table records this as closing the same question under its amendment 53 framing.**
Added 2026-07-28, from verifying the IntelliBooks half of the back-feed:

- ~~**Which sidecar shape wins after a Desktop-filed receipt?**~~ **Decided 2026-07-28: both stay, readers key on the discriminator.** See the 12.4 amendment for the reasoning and the binding rule.
- ~~**`clients.csv` gives `Client_004` to two different clients**, `Test` and `She Run's It! Ldn Ltd`, so anything keyed on `client_id` conflates them.~~ **Fixed 2026-07-28**, ahead of test 41 on Paul's instruction rather than after it, on the principle of not generating new rows against a broken registry. `SHERUNSIT` is now `Client_005`. No existing row moved: all 6 `Client_004` receipts belong to `TEST`, and `She Run's It! Ldn Ltd` had none, nor any vendor mappings. `clients.csv.bak-2026-07-28` sits beside it. The loader parses the result with no duplicate id, code or email.
- ~~**`write_review_file()` has no caller** in `app.py` or `worker/`, and the name it writes, `{stem}.review.json`, is invisible to Desktop. `scanReview()` derives the image name by stripping `.review.json` and skips the item when no file of that name exists, so `receipt.review.json` yields `receipt`, which is not a file. `file_review()`, the writer that is used, produces `{stem}{ext}.review.json` and works. Probably dead code. Confirm and delete rather than leave a second writer whose output cannot be worked.~~ **Scheduled, amendment 443. Paul's decision, 2026-09-13: delete it. Section 16's new step 10av, Claude Code's to build.**
- ~~**The null-id filename fallback can miss.** Recorded at 12.3 step 2 by amendment 48. Flagged, not fixed.~~ **Scheduled, amendment 444. Paul's decision, 2026-09-13: fix it. Section 16's new step 10aw, Claude Code's to read the code and decide the fix.**
- ~~**A note-write failure in Desktop leaves the Review pair deleted and the pipeline uninformed**, so the receipt keeps `needs_review` and the retry pass re-extracts it at one OpenAI call until the 7-day cap. This is a fallback to pre-back-feed behaviour rather than new harm, and the IntelliBooks session chose it deliberately over rolling back a filing. There is no retry and no queue. Accept, or ask for a retry on the next Desktop load.~~ **Decided, amendment 445. Paul, 2026-09-13: accept as is. Not built.**
- ~~**A partial filing window in Desktop.** Inside one `try`, the image copy commits before the sidecar write. If the sidecar write throws, the copied image is already in `Receipts\` while the toast says "Filing failed, nothing was deleted" and the Review pair remains, so re-filing produces a `-2` duplicate. Pre-existing, unrelated to the back-feed, and the toast is accurate about the Review pair and misleading about the image.~~ **Moot, amendment 447. `fileReviewReceipt()` no longer writes into `Clients\` at all, per sub-step 10f.14; the pipeline's single writer copies one file only. The race this bullet described cannot happen in the code as it now stands.**
- ~~**`PKPH-books.json` exists in `IntelliBooks\Books\` with no client in either registry.**~~ **Answered 2026-07-29: delete it.** It is a 1,174-byte stub, zero transactions, zero receipts, one bank account and Desktop's default chart of 21 categories, so there is nothing in it to preserve and OneDrive version history covers the reversal. Worth noting before it goes: those 21 categories are Desktop's built-in chart ~~and are relevant when `chart_of_accounts_DRAFT.csv` is extended at step 12~~. **Struck 2026-09-02 by amendment 167: step 12 is CANCELLED by amendment 96 and the file was deleted 2026-09-01.** Nothing in the pipeline's database refers to PKPH, so the reset never touched it and this is purely a file on disk.
- ~~**A deprecation that becomes a failure on a future Python.** Added 2026-07-30, found by Claude Code running the suite and flagged rather than fixed. `DeprecationWarning: The default datetime adapter is deprecated as of Python 3.12` fires 166 times across roughly fifteen test files. The sites are the receipt locking code in `worker/database/repository.py`, around line 588 where `stale_lock_cutoff` is passed into a query and around line 680 where `locked_at` is set, both passing a Python `datetime` object straight to SQLite as a parameter. Harmless noise today. The adapter is slated for removal, so a Python upgrade turns it into a failure across the suite rather than in one place. The fix is to pass ISO strings, which is what the rest of the codebase already stores.~~ **Fixed, amendment 441. Sub-step 10d.28 is BUILT: `acquire_receipt_lock()` at `repository.py:675` passes `.isoformat()` rather than a `datetime` object, and the comparison this bullet flagged changes with it.**
- ~~**The three synthetic rows in the live event logs.** Written by the test suite before `2d19521` fixed the isolation. Identifiable by `receipt_id` values `recent-receipt` and `56b29977`. They will show in the intake panel at 8.6 unless removed or filtered. Removing lines from an operational log is Paul's call.~~ **Decided, amendment 449. Paul, 2026-09-13: leave the logs untouched, filter these two `receipt_id` values when the intake panel is built. Carried onto section 16 step 18's own entry.**


### 17.5 The clean-slate reset, added 2026-07-28

Paul intends to clear the database and the test clients and start testing afresh, soon. Every filed receipt, sidecar and books entry on disk today is test data, which is why several decisions above are "no backfill" and "no migration". This section records what the reset must and must not touch, because two parts of it are not disposable and one of them costs money to get wrong.

**Do not clear: the vendor mappings.** `categorisations_client_vendors` holds 100 rows for `Client_001` and one for `Client_003`. That is real practice knowledge, imported from `categorisations_client_vendors_cleaned.csv`, and it is what makes the categorisation engine's layers 1 and 3 work at all. It is recoverable from that CSV if lost, and `import_vendor_csv.py` and `seed_client_vendors.py` exist for the purpose, but it should be preserved deliberately rather than rebuilt by accident. `categorisations_firm_vendors` and `categorisations_client_rules` are both empty, so nothing to protect there.

**Do not clear `processed_attachments` without checking the mailbox first, and this is the one that costs money.** It holds 20 rows, and it is the only record that a given email attachment has already been extracted. `fetch_new_messages()` selects `INBOX` and searches `ALL`, at `worker/email/reader.py:47` and `:171`, so it re-reads everything sitting in `INBOX` on every poll and relies on `processed_attachments` to know what to skip. Emails that were processed have been moved out to `INBOX.Processed Receipts` and the other routing folders, so in principle `INBOX` is empty and a reset is free. **Verify that before clearing, not after**: anything still in `INBOX` will be re-extracted at one OpenAI call per attachment, and with a fresh database there is nothing to stop it.

**Safe to clear:** `receipts`, `extractions`, `categorisations`, `resolution_events`, `statements` (empty), `email_alerts` (3 rows, all test alerts), and `email_delta`. On disk: `Clients\Test\`, `Clients\Test 2\`, the two `Books\TEST*-books.json` files, `data/files/`, and the filed receipts under `Clients\Paul Keating\Receipts\`, which are Paul's own and also test material.

**The order matters.** Stop the pipeline first, so nothing is mid-write. Back up `data/receipts.db` before touching it, `repo.backup_db()` exists for this. Then clear, then run `init_db()` once to rebuild an empty schema, then re-import the vendor mappings if they were lost, then start the pipeline and confirm one clean cycle before creating any new test fixture.

**Scheduled at step 10c**, after the live round trip and before any console work, agreed 2026-07-28. ~~and **moved again on 2026-07-29 to sit after the four Desktop changes A to D**, per amendment 54: change D's manual check posts an unreconciled receipt from the books, and this reset empties the books, so testing has to come first.~~ **That precondition is gone: change D is cancelled, amendment 68, and there is nothing left in the lettered series waiting on the books being populated.** Not earlier, because step 10 changes the pipeline and resetting either side of a code change means two variables at once. Not later, because the console should be built against a clean slate rather than against 27 test receipts and a books file full of import artefacts. **Superseded 2026-07-30 by 17.5a above**, which combines this reset with the practice root restructure into one six-stage operation. The paths quoted in this section are the old ones; 18.2a has the new layout.

**Supervised, not handed over.** Paul has asked the consultant session to run this stage by stage. That means: a plan enumerating every stage before anything is deleted; a database backup and a file-tree listing captured first; the state verified before and after each stage rather than once at the end; nothing deleted in the same stage as anything else; and the mailbox checked before `processed_attachments` is touched. If a stage does not verify, stop there rather than continue and reconcile later.

**Worth treating as a test in its own right.** A clean start exercises the paths nobody has run since May: an empty database, `init_db()` from nothing, `clients.csv` resolution with no history, and Desktop opening a books file it has to create. If any of those are broken, better to find out deliberately than on the first real client.

**Folder paths in this section are stated in their current form and most of them change.** Read 17.5a before running anything from this section.

### 17.5a The reset and the restructure are one operation, agreed 2026-07-30

**The reset comes first, then the restructure, then the code change.** Paul's decision, on the grounds that moving several thousand test files into the new layout and then deleting them is worse than deleting them and creating the new layout empty. ~~**All of `Clients\Paul Keating\` and `PAUL-books.json` are confirmed disposable**~~ **Corrected 2026-08-01, and this wording was dangerous. `Clients\Paul Keating\` is NOT disposable.** It holds 37 files including engagement letters at its top level, `letter-of-engagement-ke001_*.pdf`, plus `Document Requests\` and `Misc\` from another tool. Those are real firm records. **Only `Receipts\` and `Review\` inside it go**, which is how 17.5 above scoped it correctly before I widened it in a summary. `PAUL-books.json` is disposable. So after the reset almost nothing is left to move.

**Combining them is safer than doing them separately, not riskier**, which is the opposite of what steps 10a and 10c assumed. What made those cautious was the coordinated flip: both modules have to change their paths at the same moment or receipts stop arriving. On an empty tree that risk disappears, because there is nothing on disk to strand and one clean cycle proves it either way.

**The plan these six stages required has been written: `C:\LastingImpact\receipt_capture\2026-07-31_PLAN_reset_and_restructure.md`, written 2026-07-31 and amended 2026-08-01.** It carries the verified before-state, a gate, actions, a verification and a stop condition for each stage, and nine decisions that must be answered before stage 3. **One of the nine is decided: the interim in its section 0.5, amendment 75**, without which stage 5 is a feature build and stage 6 cannot run at all.

**Six stages, in this order, and this supersedes the order stated above.**

1. **Stop the pipeline.** Nothing mid-write.
2. **Back up.** The database, and a full file listing of the practice root and every client folder, so there is a record of what was there.
3. **Reset.** Clear the tables, delete the test client folders, the test books files and the pipeline's document store. The two precautions above carry over unchanged and both still bite: **keep the vendor mappings**, and **check `INBOX` is empty before touching `processed_attachments`**, because anything sitting there is re-extracted at one OpenAI call each.
4. **Restructure.** Create the layout in 18.2a empty. What survives stage 3 and has to move is small: `clients.csv`, `firms.csv`, `pipeline-status.json`, the practice registry and the `App` folder. Move the live database **out** of OneDrive and point `backup_db()` into it.
5. **Change the code, both sides together.** Pipeline config and Desktop's folder calls. Last rather than first, so it is written against a tree that is already the right shape rather than one it has to create.
6. **Start, run one clean cycle, confirm.** One receipt end to end, then a Review item, then a post.

**Doing the restructure first would be the wrong way round**, because every path in the reset plan would then have to be written twice.

**And the supervision requirement below applies to the whole operation, not just the reset**, which now matters more: a plan enumerating every stage before anything is deleted, state verified before and after each stage rather than once at the end, and nothing deleted in the same stage as anything else. If a stage does not verify, stop there.

---

